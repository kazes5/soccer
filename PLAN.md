# Web-First Soccer Carpool MVP Plan

Build a responsive, bilingual web application first for the high-frequency coordination workflows: closed-roster onboarding, schedules, collection points, atomic claims, releases, simple swaps, notifications, reminders, emergency coverage, and auditable admin operations. Establish a platform-neutral TypeScript API and shared contracts from day one; add a React Native client only after the web MVP has been piloted and hardened.

---

## Current Status (read this first)

**Active stage: Stage 5 — Admin Operations, Reporting, and MVP Completion. Admin member management and removal cleanup are complete as of 2026-08-12; the read-only audit-log viewer is next. An approved 2026-08-13 execution plan then adds parent password onboarding/login and an isolated system-admin control plane without changing existing team-role semantics.**

| Stage | Status | One-line note |
|---|---|---|
| 0 — Product & Architecture Foundation | Partial, non-blocking | ADRs/threat model/notification-template docs not written yet; nothing here has blocked code so far |
| 1 — Repo, Environments, Quality Gates | Mostly done | Branch protection on `main` now on (verified 2026-08-09); "staging" defined 2026-08-10 as the local `docker compose` stack — no external hosting vendor, by explicit user decision |
| **2 — Identity/Membership/i18n/Design** | **Done (2026-08-10)** | Backend + onboarding UI loop, i18n foundation, backend hardening, and design tokens/accessible primitives all built and verified. Native-speaker Hebrew review done 2026-08-10 (approved as-is, no changes). Staging/journey/RTL exit criteria satisfied via the local-stack redefinition. Automated a11y/responsive tooling explicitly re-homed to Stage 6, its documented owner. |
| **3 — Schedule & Atomic Shift Core** | **Done (2026-08-11)** | All 7 checkpoints landed and tested: schema/recurrence/atomic claim-release/parent Schedule page, Home workspace, template-edit backend, `/admin/collection-points`, player-roster endpoint, the schedule-template wizard, and session management UI + `GET /teams/:teamId/roster`. Closed out with a full desktop/tablet/narrow-mobile EN+HE manual regression pass that caught and fixed one real bug (a `Tooltip` overflow causing horizontal scroll) — see the Stage 3 closing Progress note. "Bulk-edit all future sessions" remains confirmed-deferred to the backlog. |
| 4 — Swap/Notification/Reminder/Escalation | Complete for MVP — Checkpoints 1-9 done (2026-08-11, 2026-08-12 ×7); Checkpoint 10 removed from MVP scope 2026-08-12 | Notification/event/recipient ADR recorded; `PracticeSession.startsAt` now stores real UTC instants converted through each team's IANA timezone, not pseudo-UTC wall time. Admin-configurable swap expiry/reminder offsets/escalation lead and team quiet hours, plus per-member overrides and per-category push opt-out, now exist as real settings (`/admin/notification-settings`, `/settings/notifications`). The transactional-outbox/BullMQ-worker foundation (durable `OutboxEvent`/`UserNotification`/`NotificationDelivery`/`ScheduledTask` models, a separate worker process, idempotent recipient fan-out, crash-safe startup reconciliation) is implemented and tested. All 5 team-changing route files now write outbox events on every mutation, fanning out to a real `/notifications` center with unread/dismiss state. As of Checkpoint 5, `/notifications` updates live via a team-scoped SSE stream (Redis pub/sub fast path + periodic fallback poll, Web Locks leader election across tabs) instead of only pull-refreshing, and notification deep links now scroll/highlight the right row on `/schedule` instead of just landing on the right team. As of Checkpoint 6, opted-in browser push actually delivers: VAPID + an injectable `WebPushProvider`, quiet-hours/collapse/throttle-aware delivery per ADR 0001, a service worker (suppressing the OS notification when a tab is already focused), and a Settings > Notifications subscribe/unsubscribe UI. As of Checkpoints 7-8, one-way shift swaps are fully live end to end: a `SwapRequest` lifecycle (pending/accepted/declined/expired/cancelled) with version-CAS acceptance, a database-enforced one-pending-request-per-shift constraint, scheduled-task-driven expiry capped at session start, and a new `/swaps` page (plus a real-time "Request swap" action on `/schedule` and a live pending-count widget on Home). As of Checkpoint 9, durable pre-shift reminders are fully live: one `ScheduledTask` per (shift, offset) pair, resynced on every assignee/session-time change, quiet-hours-deferred or suppressed at execution, and rendered with full session/point/player detail. Checkpoint 10 ("Emergency and closure" — `cannotMakeIt`, emergency-open shifts, auto-escalation) was removed from MVP scope at the product owner's explicit request; deferred to post-MVP (CLAUDE.md Roadmap v1.1). See the Checkpoint 1-9 and Checkpoint 10 scope-decision Progress notes below. |
| **5 — Admin Ops & Reporting** | **In progress (plan expanded 2026-08-13)** | Admin member management is live at `/admin/members`: phone/email invites, current-member search/role filters, confirmation-gated promotion/demotion/removal, final-admin disabled controls, and serialized backend mutations that preserve the last-admin invariant under concurrency. Removal cleanup now reopens scheduled future shifts while preserving past attribution. Audit-log viewer is next, followed by the staged parent-password and system-admin plan recorded under Stage 5. |
| 6 — Verification/Security/Performance | Not started (this is an ongoing gate, not a one-time stage) | |
| 7 — Post-MVP Web Expansion | Not started | |
| 8 — Native Mobile | Not started | |

### What's done right now

- **Data model:** `User`, `Team`, `TeamMember`, `Player`, `PlayerParent`, `Invite`, `Passkey`, `WebauthnChallenge`, `Session`, `NotificationPreference`, `AuditLog` — migrated (`apps/api/prisma/migrations/20260806223652_identity_membership_core`, later revised by `20260810065615_passkey_authentication` and `20260810065735_invite_accepted_by_user`, which drop `OtpChallenge`/`OtpChannel` entirely — see the 2026-08-10 passkey-authentication Progress note below) and seeded.
- **Backend API, all tested:**
  - `POST /teams` — bootstrap a team + its first admin, returns a session.
  - `GET /teams/:teamId` — basic team metadata, available only to an authenticated member of that team.
  - `POST /teams/:teamId/invites` — admin-only, audited.
  - `GET /invites/:code` — preview an invite (team name) before accepting.
  - `POST /invites/:code/accept` — atomic accept: creates the parent + linked players, or joins an existing multi-team user; race-safe against double-accept.
  - `POST /invites/:code/passkey/register/options` / `.../verify` — a new parent's first passkey, scoped to the invite code (not a bare user ID) within a bounded post-acceptance window; completes onboarding and issues a session in one step.
  - `POST /auth/passkey/register/options` / `.../verify` — an already-authenticated user (e.g. the admin right after team creation, or anyone adding a second device) registers an additional passkey.
  - `POST /auth/passkey/login/options` / `.../verify` — identifier-first (phone/email) login on a device with a registered passkey; issues a session token.
- **Web UI:** `/teams/new`, `/login`, `/invite/[code]`, and `/home` pages, calling the API above through a typed client (`apps/web/src/lib/api.ts`). Component-tested and manually walked through end-to-end in a real Chrome browser: create team → immediate passkey registration prompt → land on team-aware home, gated correctly by role (only admins see the invite form). Login and invite-accept follow the equivalent passkey flow — see the 2026-08-10 Progress note.
- **CORS:** `@fastify/cors` is now registered on the API (`WEB_ORIGIN` env var, default `http://localhost:3000`). This was missing and the manual browser walkthrough caught it immediately — every cross-origin request from the web app failed silently into a generic "Something went wrong" message, even though the same requests worked fine over `curl`. A regression test (`apps/api/test/cors.test.ts`) now guards it. This is exactly the failure mode the Checkpoint process's "manual verification" step exists to catch — the full automated gate was green the whole time this bug existed.
- **i18n foundation:** a new shared package `packages/i18n` holds the message catalog (English + Hebrew, TypeScript-enforced key parity — `he` is typed `Record<MessageKey, string>` against `en`'s keys, so a missing/extra translation is a compile error, not a runtime surprise) plus `Locale`, `isRtl`/`directionFor`, and `Intl`-based `formatDate`/`formatNumber` helpers. `apps/web` wraps the app in a `LocaleProvider` (`apps/web/src/components/locale-provider.tsx`) that persists the choice to `localStorage`, exposes a `t()` function, and syncs `document.documentElement.lang`/`dir` on change — switching languages is instant, no page reload, exactly as `CLAUDE.md` §3.10 requires. All 5 existing pages are converted; a `LanguageToggle` (EN/עב) is on every page. Manually verified in a real browser: toggling to Hebrew correctly mirrors the *entire* layout (button order, alignment, toggle position) via flex + logical CSS (`end-4` instead of `right-4`), with zero extra RTL-specific styling needed beyond what was already flex/gap-based — and the choice persists across page navigation.
- **Known i18n gaps:** server-generated error messages (e.g. "You haven't been added to a team yet…") are still English-only, since localizing those would mean threading a locale through every API request — a real backend i18n project, not a client-side one; form-field example placeholders ("Dana Cohen", "+15551234567") aren't translated, only labels/headings/buttons. (The Hebrew catalog itself is native-speaker reviewed and approved as of 2026-08-10 — see the Stage 2 closure Progress note; Heebo font and date-formatting helpers are both in active use since the design-tokens and Schedule checkpoints.)
- **Backend hardening:** sessions are now delivered as an httpOnly `soccer_session` cookie plus a readable `soccer_csrf` cookie (double-submit CSRF, enforced on all cookie-authenticated mutating requests; bearer-token requests remain exempt since browsers don't auto-replay them), with a new `GET /auth/me` for the web client to check auth state instead of caching anything in `localStorage`. `TRUST_PROXY` (defaults `false`, so a spoofed `X-Forwarded-For` can't be used to evade IP-based checks unless a real reverse proxy is deliberately configured) still exists but no longer gates an OTP-specific rate limit — that limit was removed along with OTP itself (see the 2026-08-10 passkey-authentication Progress note); general per-endpoint rate limiting remains an open Stage 6 item, same as before. `PATCH /teams/:teamId/members/:userId/role` and `DELETE /teams/:teamId/members/:userId` (plus `GET /teams/:teamId/members`) implement promote/demote and removal, both blocked from touching the last remaining admin; removal deactivates the user globally only if they have no other team memberships left, and revokes their sessions. A `PushSubscription` table plus `POST`/`DELETE /push-subscriptions` model browser-push registration (delivery itself is still Stage 4). All of this replaced the web app's `localStorage`-token session model — `apps/web/src/lib/session.ts` is deleted; the home page now authenticates via `api.me()` against the cookie.
- **Caught by manual browser verification, not by tests:** logging out threw a 500 (masked by the UI's best-effort error handling, so it looked fine) because a real `fetch()` sends `Content-Type: application/json` even with no body, which Fastify's JSON parser rejects — and the API's error handler was falling through to a generic 500 instead of passing through that parser error's own 400. Fixed on both sides (client stops sending the header when there's no body; server error handler now passes through any error's own 4xx `statusCode`) and regression-tested.

### What's missing (Stage 2)

Frontend:
- Localized server error messages (currently English-only regardless of user locale).
- Automated keyboard/screen-reader/contrast/responsive tooling (axe, Playwright) — the primitives were built accessibly (semantic roles, `aria-live`, focus rings, `aria-describedby`) and manually spot-checked at three viewports, but nothing automated runs this repeatably yet.

Process:
- None outstanding. Branch protection on `main` (the one remaining Stage 1 process item) is done, verified 2026-08-09.

### Immediate next steps

1. Both Stage 2 PRs (backend hardening #8 and design tokens #9) are merged to `main`; the frontend is fully on the cookie-based session flow. Branch protection on `main` is on (verified 2026-08-09 via the GitHub API — scoped correctly to `main` only, after an initial misconfiguration that accidentally protected every branch was caught and fixed).
2. Stage 2's native-speaker Hebrew review is done (2026-08-10, approved as-is — no changes needed) and "staging" is now defined as the local `docker compose` stack (2026-08-10, explicit user decision, no external hosting vendor) — both formerly-blocking exit criteria are now satisfied by the accumulated manual verification already done throughout Stage 2/3. The one remaining Stage 2 gap is automated a11y/responsive tooling (see above).
3. Stages 3 and 4 are complete for MVP. Stage 5 is active: admin member management and removal cleanup are complete; the read-only audit-log viewer is the next checkpoint. After that, execute Stage 5 Checkpoints 3–10 for parent password authentication and the system-admin control plane in dependency order.

### Plan to Close Stage 3 (decided 2026-08-11)

Three checkpoints remain before Stage 3's checklist and exit criteria can be closed, continuing the numbering from the four checkpoints already landed (schema/atomic-shift-core+Schedule page, Home workspace, template-edit backend, admin collection-points). Order and scope agreed with the user 2026-08-11.

**Checkpoint 5 — Player-roster endpoint (backend prerequisite). Done 2026-08-11 — see the Stage 3 Progress note below.**
- Add a `GET` route returning each player's name (and their session/collection-point context) for a team. The `packages/contracts/src/player.ts` schema already exists but has no route behind it yet — this checkpoint wires one up.
- Blocks both Checkpoint 7's per-session player-override UI and its "player names instead of counts" / "available drivers" work — done first so neither has to stub around a missing endpoint.

**Checkpoint 6 — Schedule-template wizard UI (admin). Done 2026-08-11 — see the Stage 3 Progress note below.**
- Admin create/edit flow for `ScheduleTemplate`, driving the already-shipped `POST /teams/:teamId/schedule-templates` and `PATCH .../schedule-templates/:templateId` backend (including collection-point selection, reusing UI patterns from the already-shipped `/admin/collection-points` screen).
- Closes the wizard portion of Stage 3's open "Implement an admin schedule-template wizard…" checklist item. The rest of that item (session edits/cancellation, per-session overrides) is Checkpoint 7's scope, not this one.

**Checkpoint 7 — Session management UI (admin) + Schedule page follow-through (parent-facing). Done 2026-08-11 — see the Stage 3 Progress note below.**
- Admin: individual session edit/cancel UI (driving the already-shipped `PATCH /teams/:teamId/sessions/:sessionId` and `.../cancel`), and per-session collection-point/player override UI (driving `PATCH .../points/:pointId`, now backed by Checkpoint 5's player-roster endpoint).
- Parent-facing: Schedule page shows player *names* (not counts) per collection point, closing "visibility of the full assigned player list before a driver claims" from the collection-point-management checklist item, plus an "available drivers" listing to close the remaining piece of the Schedule checklist item.
- Bundled in: mark historical/past sessions read-only at the API level (today only the web UI avoids editing them; the API itself doesn't reject it) — a small guard added naturally while touching the session-edit endpoints in this checkpoint. This closes the read-only half of the "read models for current/upcoming/historical" checklist item. The other half (a dedicated split into separate current/upcoming/historical read *endpoints*, vs. today's single `GET /sessions` covering all three at this data scale) stays an accepted, documented functional-equivalent gap — not planned work, same treatment as Stage 2's re-homed a11y tooling.
- Last checkpoint expected to touch new Stage 3 UI surface.

**Closing Stage 3 (after Checkpoint 7). Done 2026-08-11 — see the "Stage 3 closing regression pass" Progress note below.**
- One manual regression pass across every Stage 3 screen (Home, Schedule, `/admin/collection-points`, the new template wizard, the new session-management screens) at desktop, tablet, and narrow-mobile viewports, in both English and Hebrew — the same rigor Stage 2's design-tokens checkpoint used. This closes Stage 3's last open exit criterion. Confirmed with the user 2026-08-11: no automated Playwright coverage is pulled forward for this — that stays Stage 6's job per the existing Testing Strategy table.
- Each of Checkpoints 5–7 still went through the full existing per-checkpoint gate (quality gate, new tests, `/code-review medium`, PLAN.md update) — this plan only fixed their scope and order, not the process. **One exception, not originally planned:** manual browser verification was skipped for both Checkpoint 6 and Checkpoint 7, at the user's explicit direction each time — this plan's own Progress notes for both flag it as an open risk. The closing regression pass below is therefore not routine confirmation for those two checkpoints; it's the first time their UI will be seen running at all.

**Explicitly deferred out of Stage 3 (backlog, not blocking closure — confirmed with the user 2026-08-11):**
- "Bulk-edit all future sessions to time X" — CLAUDE.md §3.1 calls this out as its own acceptance criterion, distinct from template-edit-regenerates-sessions (which only ever adds sessions, never overwrites existing ones' content). Ships after Stage 3 closes, as its own later checkpoint (candidate: early Stage 4, or Stage 7).
- The "dense operational tables and side panels" wording for admin screens — collection-points already shipped as `DataList` cards, a deliberate, previously-flagged deviation. Keep deferring: Checkpoints 6–7's new admin screens follow the same `DataList` convention rather than introducing a table component now.

### Always run before calling anything "done"

```bash
pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build
```

This must pass locally (with `docker compose up -d` running) before opening a PR, and CI re-runs the same sequence. It is necessary but not sufficient — see the Checkpoint process immediately below for what else is required before a checklist item gets checked off.

---

## Checkpoint & Code Review Process

A **checkpoint** is one PR — normally one Stage sub-item or one coherent vertical slice of it (this plan has been shipping one PR per checkpoint so far: schema+auth, then invite acceptance, etc.).

Every checkpoint must clear all of these before its Stage checklist item is marked `[x]` or its Progress note calls it "done":

1. **Quality gate passes locally**: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build`, against a real Postgres/Redis (`docker compose up -d`) — not just CI, before you even open the PR.
2. **Tests exist for the new behavior** — see Testing Strategy below for what kind, per layer. A checkpoint that only adds code with no new tests is not done.
3. **Code review pass**: run `/code-review` (or the equivalent review skill) against the branch/diff before merging. Fix findings, or explicitly note in the PR/Progress entry why a finding was accepted as-is.
4. **Manual verification for anything with a UI or an externally observable effect**: click through it in a browser, or exercise the API with `curl`, and say so in the Progress note — a passing test suite is not the same claim as "I watched this work."
5. **PLAN.md updated in the same PR**: the relevant Stage checklist item gets its evidence note, and the "Current Status" section at the top of this file is updated if the active stage or immediate-next-steps changed.

Record the outcome of steps 2–4 in that stage's Progress note (see the "Progress Update Template" near the end of this file, which includes a `Code review:` field).

---

## Testing Strategy

What kind of test to write depends on what you're touching. This is the standing policy; Stage 6 is where it gets audited and hardened project-wide, not where it starts.

| Layer | Test type | Pattern to follow |
|---|---|---|
| `packages/contracts` schemas | Unit test per schema file (`*.test.ts` colocated) | `packages/contracts/src/invite.test.ts` — valid/invalid payload assertions via `.safeParse` |
| `apps/api` routes | Integration test via Fastify `.inject()` against the real dev database (no mocked DB) | `apps/api/test/auth.test.ts`, `invites.test.ts`, `invite-acceptance.test.ts` — build a fresh `app`, inject requests, assert on response + DB state, clean up created rows in `afterEach` |
| `apps/api` external providers / real-crypto ceremonies (WebAuthn, future SMS/email) | Inject a fake via `buildApp({ webauthnVerifier })` rather than mocking network calls or fabricating crypto | `apps/api/src/lib/webauthn.ts` (`WebauthnVerifier` interface, real `SimpleWebauthnVerifier`) + `apps/api/test/support/fake-webauthn-verifier.ts` |
| `apps/web` pages/components | React Testing Library component test, mocking the `api` module (not `fetch` directly) | `apps/web/src/app/page.test.tsx` is the only current example (static content); new interactive pages need mocked-`api` tests exercising the happy path and at least one error path |
| Cross-cutting flows (claim races, swap accept-vs-change, multi-team isolation) | Targeted integration tests once Stage 3/4 land | Not needed yet — nothing to race against until shifts exist |
| Full end-to-end (English+Hebrew, desktop+mobile, keyboard, a11y) | Playwright, per Stage 6 | Deliberately deferred — there's no schedule/shift UI yet to make e2e coverage meaningful |
| Load/security/concurrency hardening | Per Stage 6 | Deliberately deferred to before pilot release, not before every PR |

**Rule of thumb:** if you added a new API route, it needs an integration test in `apps/api/test/`. If you added a new page or interactive component, it needs a component test in `apps/web`. If you added a pure function/schema, it needs a unit test next to it. Don't wait for Stage 6 to backfill these — Stage 6 is for the things that are genuinely expensive to run on every PR (e2e, load, security scans), not for basic coverage.

---

## Working Conventions

- Use this document as a living checklist. Mark a completed item `[x]` and add a dated note below it for blockers, decisions, or links to the relevant issue or pull request — see the Checkpoint & Code Review Process above for exactly what "completed" requires.
- Keep user-facing work behind short-lived feature branches, require review, and use a staging environment with synthetic data before production.
- Every behavior-changing feature needs an acceptance test, audit event, authorization check, and localized copy before it can be marked complete.
- Treat the server as authoritative. The web client, future native client, scheduled jobs, and AI assistant must invoke the same command layer.
- Do not store credentials in the repository. Validate required environment variables at process startup and retain only `.env.example` templates.

## Recommended Technical Baseline

- TypeScript `pnpm` monorepo so web, API, shared contracts, and future mobile code evolve together.
- `apps/web`: Next.js App Router with React, TypeScript, TanStack Query, React Hook Form, Zod, and accessible primitives with Lucide icons.
- `apps/api`: modular Fastify service with Zod request validation, explicit domain modules, and an OpenAPI contract generated from the same schemas. NestJS is an acceptable alternative only if the implementation team prefers its module and guard conventions.
- `packages/contracts`: API DTOs, domain enums, validation schemas, localization identifiers, and generated client types shared by web and future mobile.
- PostgreSQL as the system of record; Prisma migrations plus reviewed conditional updates or transaction-safe SQL for high-contention shift transitions.
- Redis and BullMQ only for retries, scheduled reminders, swap expiry, escalation processing, and transactional-outbox delivery. Do not split this 100-user product into microservices.
- Hybrid credentials: parent-level access supports a user-chosen password stored with Argon2id, while WebAuthn passkeys (`@simplewebauthn/server` + `@simplewebauthn/browser`) remain available and are required for team-admin/system-admin access. Record the authentication method/assurance on each session because one user can be a parent in one team and an admin in another; authorization remains team-scoped and server-side. SMS/email OTP is not a login method, though an injectable email/SMS provider is still required for password recovery.
- Browser push plus real-time in-app notifications for the web MVP; an adapter abstracts APNs and FCM device delivery for the native phase.
- Object storage for CSV exports and seasonal archives; select managed Postgres, Redis, web, and API hosting during environment setup rather than encoding vendors into business logic.

## Architecture Decisions to Record Before Implementation

- [ ] Use a modular monolith, not microservices. Domain modules are Auth, Teams, Roster, Scheduling, Shifts, Swaps, Notifications, Escalations, Audit, Reporting, and AI.
- [x] Store roles on `team_members`, not globally on a user, because one parent can be an admin in one team and a parent in another. *(Implemented: `TeamMember.role`, see Stage 2.)*
- [ ] Store the global `system_admin` capability separately on `User`; never add it to `TeamRole` and never make it an implicit bypass inside `requireTeamRole`. System-wide access must use dedicated `/system/*` routes guarded by `requireSystemAdmin`.
- [ ] Treat password and passkey authentication as different session-assurance levels. Password-authenticated sessions may perform parent actions; team-admin and system-admin reads/mutations that expose privileged data or controls require a passkey-authenticated or freshly passkey-stepped-up session.
- [x] Store an IANA `timezone` on each team, defaulting to `Asia/Jerusalem`; schedule sessions and escalation thresholds in team time. *(Implemented: `Team.timezone`, see Stage 2.)*
- [ ] Keep `SessionPointAssignment` as player-to-collection-point configuration and `Shift` as the separately claimable unit. Generate one shift for each valid `(session, point, direction)` pair, including two separate shifts for a `BOTH` point.
- [x] Model every write as a command in a database transaction: authorize, validate state transition, mutate, append audit entry, insert an outbox event, then deliver notifications asynchronously with retry and idempotency keys. *(Implemented for team/invite/OTP writes; the outbox/async-delivery half doesn't apply yet since there are no notifications to deliver.)*
- [x] Use compare-and-set optimistic locking for claim, release, and swap acceptance. The update must predicate on the expected version and valid current state; an affected-row count of zero becomes a friendly conflict response. *(Pattern proven on invite `pending → accepted`; needs re-applying to shifts in Stage 3.)*
- [ ] Limit the MVP swap flow to a one-shift transfer request. Multi-shift trade offers belong to a later release because they need atomic multi-shift locking and more conflict states.
- [ ] Web MVP offline behavior is cached read-only schedule access. The durable offline mutation queue belongs to the native phase; the API already returns canonical conflict responses for it.
- [ ] Build English and Hebrew, including logical CSS and RTL behavior, from the first screen. Do not defer localization architecture to a late polish phase. *(Not yet honored — the web pages built so far are English-only. Flagging as a real gap, not a future nice-to-have: fix before Stage 2's UI checkpoint is considered done, or explicitly accept the debt in writing.)*

## Scope Boundaries

- **Web MVP includes:** invite-only access, parent password sign-in by normalized phone or email, passkey-protected team-admin/system-admin access, teams and multi-team data isolation, roster and collection-point management, recurring schedule templates, individually editable sessions, independent to-practice/from-practice shifts, atomic claim and release, transparent schedule visibility, simple swaps, audit logging, in-app/browser notifications, reminders, urgent coverage escalation, Hebrew/English core flows, and responsive web access.
- **Web MVP excludes:** native iOS/Android binaries, APNs/FCM device push, durable offline writes, multi-shift trade offers, AI chat actions, advanced fairness reporting, full archival UI, and sophisticated email digests. The data model and notification interfaces must leave room for them.
- **Post-MVP web includes:** fairness dashboards and CSV export, richer audit filtering/archive views, email digests, native push adapters, AI chat, and pilot-driven improvements.
- **Native phase includes:** Expo/React Native clients, device token registration, APNs/FCM delivery, encrypted local cache, queued offline mutations with server-wins reconciliation, iOS/Android accessibility tests, and store distribution.

## MVP Success Definition

- An admin can invite a parent, configure players and collection points, create a recurring practice schedule, and amend or cancel one session without corrupting future or historical records.
- A parent can switch teams, view the whole schedule, understand which collection-point shifts are covered, claim or release one direction without affecting the other, request a simple swap, and receive a clear conflict message if someone else wins the race.
- Every important change produces an append-only audit entry and a durable notification event; every recipient sees the change in the app and browser-push-capable users receive a web notification.
- Hebrew and English users can complete all MVP workflows with correct direction, locale-aware dates/times, keyboard support, readable responsive layouts, and no reliance on color alone.

## Stage 0: Product, Architecture, and Delivery Foundation

Depends on the completed requirements review. This stage blocks all build stages.

- [ ] Create ADRs for the selected monorepo, API, auth, notification, hosting, analytics/error reporting, and localization approaches.
- [ ] Define the pilot team, success metrics, operational owner, support route, and production data/privacy responsibilities.
- [ ] Convert each MVP acceptance criterion into an issue with a clear actor, preconditions, success response, conflict response, audit event, notification event, and test case.
- [ ] Establish domain terminology: use `TO_PRACTICE` and `FROM_PRACTICE` consistently in code; use parent-friendly pickup/drop-off copy only at the presentation layer.
- [ ] Create a permission matrix for every API command and query, including multi-team boundary rules, last-admin safeguards, removed-user behavior, and AI future scope.
- [ ] Define notification event templates, recipient selection rules, rate limits, quiet-hour behavior, deep-link format, and provider fallback strategy.
- [ ] Define a retention policy: append-only audit log, production backups, export access, personal-data request process, and archive process.
- [ ] Write a lightweight threat model covering passkey/WebAuthn abuse (e.g. the invite-scoped registration window, credential theft, RP ID/origin misconfiguration), invite leakage, insecure direct-object references/multi-team access, account removal, audit tampering, notification spam, and AI permission escalation.

### Stage 0 Exit Criteria

- [ ] ADRs and scope boundaries are approved.
- [ ] MVP backlog is ordered by dependency and each item has acceptance criteria.
- [ ] The pilot team, staging environment, and release owner are known.

## Stage 1: Repository, Environments, and Quality Gates

Depends on Stage 0. Web and API scaffolding can proceed in parallel after the monorepo decision.

- [x] Create `pnpm-workspace.yaml`, root TypeScript configuration, shared lint/format configuration, and package scripts.
- [x] Create `apps/web`, `apps/api`, `packages/contracts`, `packages/config`, and `packages/ui-tokens` with strict TypeScript boundaries.
- [x] Set up environment validation with server-only secrets, `.env.example`, local Docker Compose for PostgreSQL and Redis, and zero real credentials in the repository.
- [ ] Add database migration tooling, seed factories using synthetic families/teams, and repeatable development reset scripts.
  - Migration tooling (Prisma CLI, `prisma.config.ts`, `db:generate`/`db:migrate` scripts) is wired and verified end-to-end. Seed factories now exist for the identity/membership model (`apps/api/prisma/seed.ts` seeds a synthetic team/admin/parents/players). A repeatable "wipe and reseed" reset script still doesn't exist.
- [ ] Add GitHub Actions checks for dependency install, format, lint, type check, unit tests, API integration tests, migration validation, and production build.
  - `.github/workflows/ci.yml` covers install/format/lint/typecheck/unit-test/build against real Postgres and Redis service containers, plus `prisma validate` and `prisma generate`. API integration tests now exist (Stage 2's auth/invite/team routes) and run in this same CI job.
- [ ] Add preview/staging/production configuration with separate databases, rate limits, test push credentials, and release-safe feature flags.
  - Blocked on the Stage 0 open decision to confirm hosting/managed-service vendors.
- [ ] Add structured logs, health/readiness endpoints, error tracking with PII scrubbing, performance traces, and alert thresholds for worker failures and notification backlog.
  - Structured logging (pino via Fastify) and `GET /health` + `GET /ready` (live DB connectivity check) are in place. Error tracking, PII scrubbing, performance traces, and alert thresholds need a chosen vendor (Sentry or similar) and are deferred with the hosting decision.
- [ ] Add a contribution guide, architecture overview, API versioning policy, and changelog/release process.
  - `CONTRIBUTING.md` covers setup, everyday commands, and an architecture overview. Versioning/changelog process is explicitly deferred pre-1.0 (noted in `CONTRIBUTING.md`).

**Progress (2026-08-07):**
- **Status:** In progress — monorepo skeleton complete and verified; several sub-items intentionally deferred (see notes above).
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` all pass from a clean checkout; `apps/api` boots and serves `GET /health` (200) and `GET /ready` (200, live Postgres check via `@prisma/adapter-pg`) against `docker compose up -d`; `apps/web` boots and serves the placeholder home page.
- **Code review:** Not run — predates the Checkpoint & Code Review Process being formalized.
- **Decisions made during scaffolding:**
  - Plain pnpm workspaces (no Turborepo), Tailwind CSS v4, ESLint (flat config) + Prettier + Vitest, per explicit choice.
  - TypeScript pinned to `^6.0.3` repo-wide, not the newer `7.x` native/Go compiler — `typescript-eslint@8.66.0` does not yet support TypeScript 7 (see https://github.com/typescript-eslint/typescript-eslint/issues/10940). Revisit this pin once that support lands.
  - Prisma 7's default generator (`prisma-client`, replacing `prisma-client-js`) requires an explicit driver adapter — `PrismaClient` is constructed with `@prisma/adapter-pg` reading `DATABASE_URL`, and CLI operations read the URL from `prisma.config.ts` (`dotenv/config`) rather than a `datasource { url = env(...) }` block. Domain work in Stage 2/3 should follow this same pattern.
  - Root `.env.example` only holds Docker Compose credentials; `apps/api/.env.example` and `apps/web/.env.example` hold their own app-scoped variables, matching how Next.js and dotenv actually resolve `.env` files per directory in a monorepo (neither searches parent directories automatically).
- **Blocker or risk:** None blocking further scaffolding work. Vendor selection (hosting, error tracking, SMS/OTP provider) remains an open Stage 0 decision and blocks the preview/staging/production and observability sub-items above.
- **Next concrete action:** Turn on branch protection (see Stage 1 Exit Criteria below — this is the one remaining item that isn't blocked on a vendor decision).

### Stage 1 Exit Criteria

- [x] A fresh checkout can start web, API, PostgreSQL, Redis with one documented command sequence (`CONTRIBUTING.md` → "First-time setup"). Seed data now exists for the identity/membership model (`pnpm db:seed`).
- [x] Pull requests cannot merge with type, lint, migration, or test failures.
  - `.github/workflows/ci.yml` enforces the checks; GitHub branch protection on `main` (require the `ci` status check) is now on, verified 2026-08-09 via the GitHub API (`branches/main` reports `protected: true`, and no other branch does). Note for next time: the first attempt used a branch-name pattern that matched every branch, not just `main` — caught by re-verifying after setup rather than assuming the UI did what was intended, then corrected.
- [ ] Staging deploys are reproducible and production secrets are never exposed to the client.
  - Not started; depends on the hosting vendor decision.

## Stage 2: Identity, Membership, Localization, and Web Design System

Depends on Stage 1. Identity and design-system work can run in parallel once the contract package exists.

- [x] Implement `User`, `Team`, `TeamMember`, `Player`, invite, passkey/WebAuthn-challenge, session, browser subscription, notification-preference, and audit tables with indexes and foreign keys.
  - `User`, `Team`, `TeamMember`, `Player`, `PlayerParent`, `Invite`, `Session`, `NotificationPreference`, and `AuditLog` are modeled in `apps/api/prisma/schema.prisma` and migrated (`20260806223652_identity_membership_core`), with indexes/unique constraints per this plan's architecture decisions (role on `team_members`, IANA `timezone` on `team`). `PushSubscription` (browser-push registration) landed in the backend-hardening checkpoint (`20260809095433_stage2_backend_hardening`) with `POST`/`DELETE /push-subscriptions`; actual push *delivery* is still Stage 4. `OtpChallenge` originally lived here too — dropped and replaced by `Passkey`/`WebauthnChallenge` in the 2026-08-10 passkey-authentication checkpoint (Progress note below).
  - **Tests:** covered by the route integration tests below (no schema has its own test beyond `prisma validate` in CI); `apps/api/test/push-subscriptions.test.ts` covers the subscription endpoints directly.
- [ ] Implement invite-only onboarding: admin creates expiring invite, recognized invitee verifies phone/email, completes profile and linked-player details, then reaches the team-aware home view.
  - Backend done and tested: `POST /teams`, `POST /teams/:teamId/invites`, `GET /invites/:code`, `POST /invites/:code/accept`.
  - Frontend done: `/teams/new`, `/login`, `/invite/[code]`, `/home` pages, component-tested and manually walked through end-to-end in a real browser. Required `@fastify/cors` on the API (`WEB_ORIGIN` env var) — cross-origin requests from the browser were silently failing before this, caught by the manual walkthrough, not by any automated test; see `apps/api/test/cors.test.ts` for the regression guard.
- [x] Enforce passkey-ceremony/challenge expiry, invite-scoped registration windows, hashed/revocable sessions, secure cookies, CSRF protection where applicable, and immediate deactivation on removal.
  - Originally written against OTP (expiry + per-phone/per-IP rate limits); OTP was replaced by passkeys on 2026-08-10 (Progress note below), which carries its own equivalent guards: `WebauthnChallenge` expiry (`WEBAUTHN_CHALLENGE_TTL_MINUTES`), single-use consumption, and a bounded invite-scoped registration window (`PASSKEY_REGISTRATION_WINDOW_MINUTES`) instead of a rate limit (WebAuthn's own device-bound crypto makes brute-force irrelevant the way it was for a 6-digit OTP code). Session/CSRF/cookie/deactivation behavior below is unchanged by that migration: hashed session tokens (SHA-256), session expiry (`SESSION_TTL_DAYS`), `POST /auth/logout` (revokes `Session.revokedAt`), secure httpOnly session cookie (`soccer_session`) plus a readable double-submit CSRF cookie (`soccer_csrf`, checked against an `x-csrf-token` header on every cookie-authenticated mutating request; bearer-token requests are exempt since browsers don't auto-replay them), and deactivation-on-removal (see the member-management item below — a removed user with no other team memberships left has `User.isActive` set `false` and their sessions revoked).
  - The web client no longer stores a session token anywhere JS-accessible (`apps/web/src/lib/session.ts` deleted) — it authenticates via the httpOnly cookie and checks status via the new `GET /auth/me`, closing the XSS-exposure trade-off this item previously carried as accepted debt.
  - **Tests:** `apps/api/test/session-cookies.test.ts` (cookie issuance, CSRF accept/reject, `/auth/me`, logout clearing), `apps/api/test/auth.test.ts` (per-IP limit, spoofed-`X-Forwarded-For` resistance).
- [x] Implement team switching and server-side authorization helpers that scope every query and command to active membership; add last-active-admin transaction checks for removal and demotion.
  - `requireAuth`/`requireTeamRole` (`apps/api/src/lib/authorization.ts`) scope mutations to an authenticated user's role on a specific team; every login/registration endpoint's response (`authSessionResponseSchema`, formerly `verifyOtpResponseSchema`) returns all of a user's team memberships (the data team-switching UI needs). `GET /teams/:teamId/members`, `PATCH /teams/:teamId/members/:userId/role` (promote/demote), and `DELETE /teams/:teamId/members/:userId` (remove) are new in `apps/api/src/routes/members.ts`; both the demote and remove paths run a transaction-scoped count of other admins and reject with `409` if the target is the last one. Manually verified via `curl`: demoting/removing the sole admin returns `409`.
  - **Tests:** `apps/api/test/members.test.ts` — list, promote, block-demote-last-admin, block-remove-last-admin, remove-revokes-and-preserves-audit-trail.
- [x] Build a central message catalog using stable message identifiers, `Intl` formatting, Hebrew and English translations, locale persistence, and RTL-aware formatting tests.
  - `packages/i18n`: flat `MessageKey`-keyed catalog with compile-time EN/HE parity (excess-property checking on `Record<MessageKey, string>` — a missing or extra Hebrew key is a `tsc` error), `translate()` with `{param}` interpolation, `isRtl`/`directionFor`, and `Intl`-backed `formatDate`/`formatNumber`. Locale persisted client-side via `localStorage` (`apps/web/src/components/locale-provider.tsx`). Hebrew is AI-drafted, not yet native-reviewed (`CLAUDE.md` §3.10 requires human review before pilot — flagged inline in the source).
- [x] Set document `dir` at the root and build components with logical CSS properties, semantic directional icons, correct focus order, and locale-independent IDs. Verify language switching without a full page reload.
  - `LocaleProvider` syncs `document.documentElement.lang`/`dir` on every locale change — no reload. All 5 existing pages use flex/gap layouts with no hardcoded `left`/`right` (confirmed by grep before starting this work), plus one logical-inset utility (`end-4` for the language toggle position) — so RTL mirroring came essentially free once `dir="rtl"` was set. Manually verified in a real browser: full-page mirroring (button order, alignment, toggle position), persisted across navigation. Not yet done: semantic directional icons (none of the current pages have directional icons to worry about) and locale-independent IDs (no IDs used yet either) — both apply once the design-system primitives (icons, forms with `id`/`htmlFor`) land.
- [x] Create design tokens for color, spacing, elevation, typography, motion, focus, and status semantics. Use a compact fieldside utility aesthetic: ink and soft neutral surfaces, field green for owned/confirmed assignments, coral for urgent needs, amber for attention, and teal for pending states. Pair every color with text and icon semantics.
  - `packages/ui-tokens` now holds the semantic contract (`status.ts` maps each of the six shift/session tones — `mine`/`covered`/`open`/`urgent`/`attention`/`pending` — to a Tailwind class string *and* an icon key, so no component can express a status by color alone; `typography.ts`, `motion.ts`, `elevation.ts`, `focus.ts` hold the portable literal values for a future React Native consumer). The actual CSS values live in `apps/web/src/app/globals.css` as `@theme`-mapped custom properties (light + dark), since Tailwind v4 themes are CSS, not JS — the two files are linked by shared token names, documented with a comment pointing each way.
- [x] Use a Hebrew-capable display/body family such as Heebo across both languages, with tabular numerals for date/time and assignment counts. Do not rely on browser default typography.
  - `next/font/google`'s `Heebo` (`subsets: ['latin', 'hebrew']`) replaces Geist as `--font-sans` in `layout.tsx`/`globals.css`; a `.tabular-nums` utility (`font-variant-numeric: tabular-nums`) is defined and ready for Stage 3's date/count displays (nothing renders a date yet, so nothing consumes it today).
- [x] Build accessible primitives: application shell, desktop sidebar, compact mobile-web bottom navigation, team switcher, data table/list, status badge, icon button with tooltip, dialog, form fields, toast, loading/empty/error states, and confirmation flows.
  - All in `apps/web/src/components/ui/`: `shell.tsx` (`AppShell` — desktop sidebar + mobile top bar/bottom tab bar from one `navItems` prop, skip-to-content link), `team-switcher.tsx` (renders nothing for single-team accounts — nothing to switch between), `data-list.tsx`, `status-badge.tsx`, `icon-button.tsx` + `tooltip.tsx` (dependency-free, `aria-describedby`-linked), `dialog.tsx` (native `<dialog>` + `showModal()` for a free focus trap and Escape-to-close, with a feature-detected fallback to the plain `open` attribute where `showModal` isn't implemented — e.g. jsdom in tests), `confirm-dialog.tsx` (built on `dialog.tsx`, wired to the "Log out" flow), `toast.tsx` (`ToastProvider`/`useToast`, `aria-live="polite"`, wired to a real "copy invite link" action), `states.tsx` (`LoadingState`/`EmptyState`/`ErrorState`, applied to the invite-accept page's loading/expired/not-found branches). `form-controls.tsx` rewritten on top of the new tokens with a shared 44px-minimum `buttonBaseClassName`.
- [x] Establish visual QA at desktop, tablet, and narrow mobile-browser sizes. Ensure 44px minimum controls, no horizontal scrolling, no nested decorative cards, and no clipped Hebrew strings.
  - Manually verified in a real Chrome browser at 1440px (desktop sidebar), 820px (tablet — still desktop layout, confirming the `md` breakpoint choice), and 390px (mobile — top bar + bottom tab bar). Checked in both English/LTR and Hebrew/RTL: full mirroring (sidebar to the right, icons/toggle order flipped, no clipped text). `document.documentElement.scrollWidth === clientWidth` confirmed at 390px (no horizontal scroll). Every interactive control's `getBoundingClientRect()` checked ≥44×44px (language toggle, logout, invite-submit, remove-player). Exercised real data end-to-end: admin invite-create → copy-link (tooltip + toast) → logout confirm dialog (cancel and confirm paths), and the parent (non-admin) view, and the invite-accept join form, and the invite-not-found `ErrorState`.

**Progress (2026-08-09):**
- **Status:** In progress. Backend contract for team bootstrap, invite issuance/acceptance, and OTP login is built and tested. Web onboarding UI (`/teams/new`, `/login`, `/invite/[code]`, `/home`) is built, component-tested, and manually verified end-to-end in a real browser. Code review complete and findings resolved — ready for PR.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (42 tests across all packages), and `pnpm build` all pass from a clean checkout, against a real Postgres via `docker compose up -d`. Manually verified twice: once via `curl` (team creation → invite → accept → OTP login → audit log rows confirmed in Postgres), and once by driving the actual `apps/web` UI in Chrome through the same journey end-to-end.
- **Code review:** Ran `/code-review medium` against the full diff. 3 findings:
  1. **Fixed** — "Log out" only cleared client-side `localStorage`, never revoked the session server-side, so a captured token stayed valid up to 30 days after "logout." Added `POST /auth/logout` (revokes `Session.revokedAt`), wired the button to call it, added `apps/api/test/cors.test.ts`-style regression coverage in `auth.test.ts` (2 new tests) plus a web test asserting the button calls `api.logout` before clearing local state.
  2. **Accepted as tracked debt, no change** — bearer token in `localStorage` is readable by any script on the page (XSS exposure). Already documented above as a deliberate trade-off with an explicit "revisit cookie vs. bearer" note; not redesigning session transport in this checkpoint.
  3. **Fixed** — invalid player age input (e.g. non-numeric) was sent as `NaN` to the server, which correctly rejected it, but the form showed a generic "Something went wrong" with no indication of which field was wrong. Added client-side `parsePlayerAge()` mirroring the server's bounds (positive integer, max 25); invalid input is now silently omitted rather than blocking the whole submission. Added a regression test.
- **Decisions made during this slice:**
  - OTP delivery goes through an `OtpProvider` interface (`apps/api/src/lib/otp-provider.ts`); the default `ConsoleOtpProvider` logs the code instead of sending it, since the SMS/email vendor is still an open Stage 0 decision. `buildApp()` accepts an `otpProvider` override so tests can capture codes without the API ever returning a code in a response body.
  - Sessions are bearer tokens (SHA-256 hash stored, raw token returned once on login), not cookies — simplest correct option while no web client existed. The web login page now stores this token in `localStorage` (`apps/web/src/lib/session.ts`); revisit cookie vs. bearer (and CSRF) as a deliberate decision rather than letting it ride by default.
  - `packages/contracts/src/index.ts` is a barrel over per-domain files (`health.ts`, `enums.ts`, `team.ts`, `user.ts`, `player.ts`, `invite.ts`, `auth.ts`). Request DTOs with Zod `.default()` fields (`CreateTeamRequest`, `CreateInviteRequest`, `AcceptInviteRequest`) are typed via `z.input<...>`, not `z.infer<...>` — `z.infer` resolves to the *output* type where defaulted fields are non-optional, which is wrong for a caller constructing the request. This surfaced as real `tsc` errors in the web client, not a style nitpick.
  - Invite acceptance identifies the contact (phone or email) from the *invite record*, not from the request body, so an accepted invite can't be hijacked into registering a different phone/email than the one actually invited.
  - The invite `pending → accepted` transition uses a count-checked conditional `updateMany` inside the transaction — the same compare-and-set pattern this plan's architecture decisions call for on shift claims later.
  - `@fastify/cors` added to the API (`WEB_ORIGIN` env var, default `http://localhost:3000`) — missing CORS silently broke every browser-originated request while `curl` and the automated test suite stayed green throughout. Found only by the manual browser walkthrough; regression-guarded by `apps/api/test/cors.test.ts`.
- **Blocker or risk:** None blocking. Known gaps: (1) the audit-log schema requires a non-null `teamId`, so "login attempt by a non-registered user" (a `CLAUDE.md` §5 loggable event) currently isn't audit-logged — no team to attribute it to; (2) the web UI is English-only with no design tokens, both explicitly deferred rather than accidental; (3) the home page reads `localStorage` via a lazy `useState` initializer on a client-only page with no SSR data — functionally fine, but can produce a one-time React hydration console warning on first authenticated load, not addressed with `useSyncExternalStore` to keep this slice's scope contained.
- **Next concrete action:** Commit and open the PR for this checkpoint. After that, either start the i18n/design-system track or close the remaining backend gaps (per-IP rate limit, secure-cookie/CSRF session transport).

**Progress (2026-08-09 — i18n foundation):**
- **Status:** Done for this checkpoint. Message catalog, RTL wiring, and locale switching are built, tested, code-reviewed with findings resolved, and manually verified in a real browser across all 5 existing pages — including the specific reload scenario the review flagged. Design tokens and accessible primitives (the other half of this Stage 2 track) haven't started.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (53 tests across all packages), and `pnpm build` all pass from a clean checkout. Manually verified in Chrome twice: (1) toggled English → Hebrew on the landing page, confirmed the *entire* layout mirrors correctly (button order, text alignment, toggle position via `end-4`), navigated to `/login`, confirmed the choice persisted; (2) after the cookie-based fix below, set Hebrew then did a full page **reload** and inspected the console diff Next prints for hydration mismatches — `lang="he" dir="rtl"` had no diff marker (only the pre-existing, unrelated browser-extension-injected attributes did), confirming the fix actually closes the gap rather than just looking right.
- **Code review:** Ran `/code-review medium`. 3 findings, all addressed:
  1. **Fixed (real bug, not cosmetic)** — locale was read from `localStorage` inside a `useState` lazy initializer, which runs identically during SSR (always resolving to `'en'`, since `window` is undefined there) and during client hydration (resolving to the real stored value). For any user who'd previously chosen Hebrew, this meant a genuine React hydration **text** mismatch on every `t(...)` call, on every page load — not just a visual flash. Fixed by moving locale resolution to the server: `layout.tsx` is now `async`, reads a `soccer.locale` cookie via `next/headers`'s `cookies()`, and renders `<html lang={locale} dir={directionFor(locale)}>` directly. `LocaleProvider` now takes an `initialLocale` prop (no longer reads storage itself) and `setLocale` writes the cookie (dropped `localStorage` entirely — one source of truth, readable by both server and client). Verified live per the evidence above. **Trade-off accepted knowingly:** `cookies()` in the root layout opts every route out of static prerendering (all pages now build as `ƒ` dynamic instead of `○` static) — acceptable now since Stage 3+ needs per-request auth-aware rendering everywhere anyway.
  2. **Fixed as part of #1** — the `<html lang="en">` FOUC/flash finding was the other half of the same root cause and is closed by the same server-side fix; there's no longer a client-side correction step needed on first load.
  3. **Fixed** — the invite-preview fetch effect (`apps/web/src/app/invite/[code]/page.tsx`) called `t()` inside its `.catch` handler while only depending on `[code]`, so a locale switch during a pending request would show the error in the pre-switch language (narrow window, but a real stale-closure bug). Fixed by storing the raw failure state (`previewFailed`, `previewErrorDetail`) and calling `t()` only at render time, which always sees the current locale.
- **Decisions made during this slice:**
  - New shared package `packages/i18n` (not `apps/web`-only) since Stage 8's native mobile plan explicitly calls for a "shared translation catalog" — same reasoning as `packages/contracts`.
  - EN/HE parity is enforced at compile time (excess-property checking on a `Record<MessageKey, string>` assignment), not via a runtime test — a stronger guarantee, since a missing or extra key fails the build immediately rather than needing someone to remember to run a parity test.
  - No i18n *library* (next-intl, etc.) — CLAUDE.md ties language to the `User.languagePreference` account setting with instant in-place switching, not URL-prefixed routes (`/en/...`, `/he/...`), which is what most Next.js i18n libraries are built around. A plain Context + cookie + `document.documentElement` sync is simpler and a better fit than fighting a routing-oriented library into a non-routing use case.
  - Locale storage is a plain (non-`httpOnly`) cookie, not `localStorage` — deliberately readable by both the server (for correct SSR) and client JS (for the existing `ApiError`/ `t()` plumbing), read via `document.cookie` on the client and `next/headers` on the server.
- **Blocker or risk:** None blocking. Every route in `apps/web` is now dynamically rendered (lost static prerendering) as a consequence of reading cookies in the root layout — a deliberate, documented trade-off, not an oversight. Other known gaps are listed above under "What's missing (Stage 2)" — AI-drafted (not native-reviewed) Hebrew, English-only server error messages, untranslated placeholder examples, no Heebo font yet.
- **Next concrete action:** Commit and open the PR for this checkpoint. After that: design tokens + accessible primitives, or the remaining backend gaps.

**Progress (2026-08-09 — design tokens + accessible primitives):**
- **Status:** Done for this checkpoint. All four remaining Stage 2 frontend checklist items (tokens, Heebo font, accessible primitives, visual QA) are built, tested, code-reviewed with every finding addressed, and manually verified end-to-end in a real browser across desktop/tablet/mobile and both locales.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (73 tests across all packages, 31 in `apps/web` — up from 53 total before this checkpoint), and `pnpm build` all pass from a clean checkout. Manually verified in a real Chrome browser, driving actual seeded data through the API (not mocks): admin invite-create → copy-link (tooltip + toast) → logout confirm dialog (both cancel and confirm paths) → parent (non-admin) view → invite-accept join form → invite-not-found `ErrorState`; checked at 1440px (desktop sidebar), 820px (tablet, confirming the `md` breakpoint choice), and 390px (mobile top bar + bottom tab bar); checked in both English/LTR and Hebrew/RTL (full mirroring — sidebar side, icon/toggle order, no clipped text); confirmed no horizontal scroll (`scrollWidth === clientWidth` at 390px) and every interactive control ≥44×44px via `getBoundingClientRect()`.
- **Code review:** Ran `/code-review medium` against the full diff. 8 findings, all fixed:
  1. **Fixed (real a11y regression)** — the `attention` and `pending` status tones both used the `timer` icon, so two different states rendered identically to anyone not relying on color — directly defeating the file's own stated purpose. Gave `attention` a distinct `clock` icon; added a test asserting all six tones have distinct icons (the prior test only checked each tone *has* an icon, not that they differ).
  2. **Fixed (real bug)** — `handleCopyInviteLink` showed a "Copied to clipboard" success toast even when the copy failed (the failure was silently swallowed by an empty `.catch`), and would throw synchronously, uncaught, if `navigator.clipboard` was `undefined` (non-HTTPS context) before the `.catch` could even attach. Now guards for `navigator.clipboard` existence and shows a distinct error toast (new `home.inviteCopyFailed` key, en+he) on any failure.
  3. **Fixed (real a11y gap)** — `TeamSwitcher` used the ARIA tabs pattern (`role="tablist"`/`"tab"`/`aria-selected`) but had no arrow-key navigation, which screen-reader and keyboard users expect from that pattern. Added roving `tabindex` (only the active tab is in the Tab order) plus Arrow/Home/End key handling — arrow-key direction is RTL-aware via `isRtl(locale)` from `@soccer/i18n`, not hardcoded to LTR.
  4. **Fixed** — the seed script's team id changed (see decision below) to a schema-valid UUID, but the old upsert-by-id would silently *create a second duplicate team* for any developer who'd already seeded under the old id, rather than updating it. Added a one-time `deleteMany` for the legacy id before the upsert.
  5. **Fixed** — the seed script's `teamMember.upsert` used `update: {}`, so re-running it against a membership row with a stale/wrong role would never correct it. Changed to `update: { role }`.
  6. **Fixed (minor correctness)** — the home page's `activeTeamId` initializer called `loadSession()` a second time instead of reusing the `session` state already loaded one line above — redundant `localStorage` I/O and a latent divergence risk if `loadSession()` were ever not perfectly idempotent. Now reuses `session` directly.
  7. **Fixed (DRY)** — five call sites hand-rolled a byte-identical `<p role="alert" className="text-sm text-status-open-on">{error}</p>` block instead of a shared component, so a future styling change (e.g. adding an icon, per CLAUDE.md §3.8's color-blind-friendly requirement) would need five independent edits with no compiler signal if one were missed. Extracted a `FormError` component in `form-controls.tsx`, used at all five sites. (Not folded into `Field`'s existing `error` prop, since these are form-level submission errors, not validation errors scoped to one specific input — reusing `Field`'s per-input error slot for a form-level error would misattribute it.)
  8. **Fixed (minor leak)** — `Toast`'s auto-dismiss `setTimeout` was never cleared on unmount, risking a "state update on an unmounted component" warning if `ToastProvider` ever unmounted with a toast still pending (low risk in production, where it's mounted at the app root, but real in principle and in tests). Tracked pending timers in a ref, cleared on unmount.
- **Decisions made during this slice:**
  - Token architecture is split across two files by design, not accidentally out of sync: `packages/ui-tokens` (TS) holds the *semantic* contract — which Tailwind class name and icon key each status tone maps to — portable to a future React Native consumer; `apps/web/src/app/globals.css` (CSS) holds the *actual* color/shadow values as `@theme`-mapped custom properties, since Tailwind v4 themes are authored in CSS, not JS. The two are linked by shared token names (documented inline in both files) rather than a cross-package CSS `@import`, to avoid taking on Tailwind v4's cross-package CSS resolution as a new source of build risk in this slice.
  - `Dialog` is built on the native `<dialog>` element and `showModal()` (free focus trap + Escape-to-close + backdrop), with a feature-detected fallback to the plain `open` attribute where `showModal`/`close` aren't implemented — discovered this was necessary because jsdom (the test environment) doesn't implement them, and the component would otherwise throw in every test that opens a dialog.
  - No new UI library dependency beyond `lucide-react` (icons) — `Tooltip` and `Toast` are implemented from scratch rather than reaching for a component library, since CLAUDE.md's target stack (§8.1, architecture doc) only commits to Lucide icons, not a full component/primitive library.
  - Heebo replaces Geist as the sole `--font-sans` (not layered alongside it) — Heebo covers both Latin and Hebrew glyphs from one family, so the UI never swaps fonts mid-session when the locale switches.
  - Home page's team list became a `TeamSwitcher` + single active-team card instead of stacking every membership as separate cards — this is what CLAUDE.md §3.15 actually specifies ("selecting a team shows only that team's schedule and shifts"), not just a design-primitive demo; it degrades to rendering nothing (falls back to the single card) for the common single-team case.
  - Manual QA surfaced two bugs unrelated to design tokens, fixed in this same PR rather than deferred, mirroring how the onboarding and i18n checkpoints handled bugs manual verification caught: (a) the demo seed script's hardcoded team id (`00000000-0000-0000-0000-000000000001`) wasn't a schema-valid UUID under the app's own strict UUID regex (needs a version/variant nibble), so `verifyOtp`'s response failed its own Zod validation — logins for the seeded admin were completely broken; (b) the seed script's `user.upsert` only ever attached a `teamMembers` row inside its `create` branch, never on the `update` branch, so re-running seed against a database where those users already existed (a normal workflow step) silently left them with zero team memberships. Both fixed directly in `apps/api/prisma/seed.ts` (see also code-review findings #4–5 above for the follow-up correctness fixes).
- **Blocker or risk:** None blocking. Known, explicitly-deferred gaps: (1) no automated accessibility scan (axe, screen-reader smoke test) has run — the primitives were built with semantic roles/`aria-live`/focus rings/`aria-describedby` and manually spot-checked, but that's Stage 6's job per the Exit Criteria below, not this checkpoint's; (2) the dev-mode hydration-mismatch warning some browsers show on this app is caused by a `data-testim-*` attribute a browser extension injects into `<body>` before React hydrates — confirmed absent on a production build with no such extension present, not an app bug.
- **Next concrete action:** Commit and open the PR for this checkpoint. After that: reconcile the frontend onto the cookie-based session flow (PR #8 now merged to main), then get a native speaker to review the Hebrew catalog and stand up a staging environment — the two remaining Stage 2 exit criteria.

**Progress (2026-08-09 — backend hardening):**
- **Status:** Done for this checkpoint. Closes every remaining Stage 2 backend gap: httpOnly cookie sessions + CSRF, per-IP OTP rate limiting, member promote/demote/remove with a last-admin safeguard, and a `PushSubscription` table with register/unregister endpoints. PR open on `stage2-backend-hardening`.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (71 tests across all packages), and `pnpm build` all pass from a clean checkout against a real Postgres (`docker compose up -d`). Manually verified twice: (1) via `curl` with a cookie jar — team creation sets an httpOnly `soccer_session` cookie and a readable `soccer_csrf` cookie; a mutating request with the cookie but no `x-csrf-token` header gets `403`, the same request with the header gets `201`; `GET /auth/me` round-trips the current user; demoting/removing the sole admin returns `409`; (2) by driving the actual `apps/web` UI in Chrome: create team → land on `/home` authenticated purely via cookie (no `localStorage` involved) → send an invite (a CSRF-protected mutation, confirming the browser-side cookie-read-and-header-set path works, not just `curl`) → full page reload preserves the session → log out → confirm `/home` now redirects to `/login` (session actually revoked, not just client state cleared).
- **Code review:** Ran `/code-review medium` against the branch diff. 3 findings:
  1. **Fixed** — per-IP OTP rate limiting used `request.ip` with no `trustProxy` configured, so behind any real reverse proxy every user would appear to share the proxy's IP and legitimate users would get locked out once any user hit the shared limit. Added a `TRUST_PROXY` env var (default `false`) wired into Fastify's `trustProxy` option; documented in `.env.example` as something to flip only when actually deployed behind a proxy that sets `X-Forwarded-For`. Added a regression test confirming a spoofed `X-Forwarded-For` cannot be used to evade the limit while `TRUST_PROXY` is off (the default).
  2. **Fixed** — bearer-or-cookie session-token extraction was duplicated verbatim between the auth plugin and the logout handler. Extracted a shared `resolveSessionToken()` helper in `apps/api/src/lib/cookies.ts`, used by both.
  3. **Accepted as-is, no change** — `POST /push-subscriptions` upserts by `endpoint` alone and reassigns ownership to whoever posts it, with no check that the endpoint didn't already belong to a different user. This is deliberate: Web Push endpoint URLs are high-entropy, unguessable tokens (not user-facing or logged), and the same endpoint legitimately recurs when a *different* user logs into the *same* shared browser/device without unsubscribing first — in that case reassigning ownership to the newly-logged-in user is the correct behavior, not a bug. Restricting reassignment would break that legitimate case for a theoretical risk that requires the attacker to already have obtained a secret they shouldn't have. Revisit if Stage 4's actual push-delivery work surfaces a concrete reason to.
- **Found only by manual browser verification, not by any automated test:** logging out threw a server error. The web client's `fetch()` always sent `Content-Type: application/json` even for logout's bodyless request; Fastify's JSON parser correctly rejects an empty body declared as JSON with a `400`, but the API's error handler had no fallback for arbitrary framework errors and turned it into a misleading `500` — and the UI's `.catch(() => {})` "best-effort" logout handling silently swallowed it, so the button *looked* like it worked (redirected to `/`) while the session stayed live server-side. Confirmed via `read_network_requests` in the browser session, then reproduced and root-caused from the API's dev log. Fixed on both sides: the web client now omits `Content-Type` when there's no body, and the API's error handler passes through any error's own 4xx `statusCode` instead of defaulting to `500`. Regression-tested (`apps/api/test/auth.test.ts`: "reports a bodyless request with a declared JSON content type as a client error, not a 500").
- **Decisions made during this slice:**
  - CSRF uses the double-submit cookie pattern (a non-`httpOnly` `soccer_csrf` cookie whose value must match an `x-csrf-token` request header) rather than server-side token storage — stateless, and sufficient because the cookie is scoped to same-site requests already.
  - `localhost:3000` and `localhost:4000` (or, in production, subdomains of one registrable domain) are same-site for `SameSite=Lax` cookie purposes even though they're cross-origin for CORS — port doesn't factor into the "site" boundary. This is why `SameSite=Lax` works for the web↔API split without needing `SameSite=None`. **Constraint for hosting (Stage 0 open decision):** the web app and API must be deployed on the same registrable domain (e.g. `app.example.com` + `api.example.com`), not unrelated domains, or cookie auth breaks.
  - Member removal deactivates the `User` account globally only when the removed membership was their *last* team membership (multi-team users keep access to their other teams) — matches `CLAUDE.md` §3.15's per-team scoping model.
  - Sessions still return `sessionToken` in the response body (not just the cookie) — useful for `curl`/tests/future non-browser (native) clients using bearer auth; the security-relevant fix is that the *web app* no longer persists it anywhere JS-accessible.
- **Blocker or risk:** None blocking. Known remaining gap, unrelated to this slice: no admin UI screens yet for the new member-management endpoints (list/promote/demote/remove) — that's Stage 5's "Build admin user management" UI work; this checkpoint only had to land the backend commands per Stage 2's own checklist wording.
- **Next concrete action:** Open the PR for this checkpoint (branch `stage2-backend-hardening`). After merge, move to design tokens + accessible primitives — the one remaining Stage 2 track.

**Progress (2026-08-10 — passkey authentication):**
- **Status:** Done for this checkpoint. Replaces Phone OTP with WebAuthn passkeys end to end — schema, backend, and all three web entry points (team creation, invite acceptance, returning-user login) — per an explicit decision made with the user before implementation started (see CLAUDE.md §8.2's 2026-08-10 revision note). This directly closes the SMS-vendor dependency that had been an open Stage 0 decision since scaffolding.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (147 tests across all packages), and `pnpm build` all pass from a clean checkout against a real Postgres (`docker compose up -d`). Manually verified in a real Chrome browser via `scripts/start-manual-tests.sh`: team creation → real `POST /auth/passkey/register/options` round-trip against the actual `@simplewebauthn/server` implementation (confirmed `201` in both the network panel and the API log, not the test double) → the browser's native passkey ceremony triggers and (correctly) fails in this sandboxed environment with no platform authenticator available → the "cancelled" error and working retry button render exactly as designed. Also verified: unrecognized phone → 404; a real seeded user with zero passkeys → the same 404 (proving that branch); Hebrew/RTL rendering of every new screen. **Known limitation of this verification round:** a full biometric/security-key completion can't be exercised via browser automation (no CDP virtual-authenticator support in the tools available here) — the automated test suite covers that path via the injectable `FakeWebauthnVerifier`, the same architectural pattern `OtpProvider` used.
- **Code review:** Ran `/code-review high` (elevated from the usual `medium`, given this touches authentication end to end). 5 findings; the two most severe were fixed with regression tests, one was fixed as a smaller hardening pass, one was a refactor (also completed), and one was investigated and confirmed pre-existing (not a regression from this checkpoint, left as a documented gap):
  1. **Fixed (critical, real bug)** — the recovery model this same diff documented in CLAUDE.md §9.1 ("an admin re-invites the affected person") was actually unreachable: `POST /invites/:code/accept` threw a hard `409 "You're already on this team"` whenever the invited contact already had a `TeamMember` row — which is exactly the state a locked-out existing user is in. A second invite for recovery would always fail before ever reaching the passkey-registration step, silently defeating the only recovery lever an admin has. Fixed: accepting an invite for an already-existing member now skips membership/player creation (no duplicates), re-anchors the invite's `acceptedByUserId` to that user, and logs a distinct `invite_accepted_for_recovery` audit action — letting the passkey-registration step proceed for them on a new device. Regression-tested (`invite-acceptance.test.ts`): re-invites an existing member, asserts no duplicate membership/players, and asserts the recovery invite's passkey-registration endpoint is reachable afterward.
  2. **Fixed (real gap)** — OTP's per-phone and per-IP rate limits were removed along with OTP, but nothing replaced them on the new passkey endpoints, leaving `/auth/passkey/login/options` fully unbounded. Added a per-IP rate limit (`WEBAUTHN_LOGIN_MAX_REQUESTS_PER_IP_PER_HOUR`, default 20) on that specific endpoint — the others (`/auth/passkey/register/*`) are already gated by `requireAuth`, and the invite-scoped pair is already bounded by invite possession plus the 15-minute registration window, so they don't need an additional limit. Note this only bounds requests for *registered* contacts (a `WebauthnChallenge` row, which the count is based on, is never created for an unrecognized phone/email) — the exact same characteristic the old per-IP OTP limit had; not a new gap relative to OTP, and unlike OTP's 6-digit code, a WebAuthn ceremony can't be brute-forced regardless.
  3. **Fixed (paired with #2)** — `WebauthnChallenge` rows were never pruned after expiry, and combined with #2's unbounded endpoint, could grow without limit. Added opportunistic cleanup (delete-expired) on the same request paths that create new challenges, rather than standing up a cron job for a table this small — no job scheduler exists in this codebase yet (Redis/BullMQ is provisioned but unused, per the Recommended Technical Baseline). Regression-tested.
  4. **Fixed (DRY)** — the registration-options/verify handlers in `auth.ts` (authenticated) and `invites.ts` (invite-scoped) were near-verbatim duplicates, differing only in how the target user is resolved and authorized. Extracted `apps/api/src/lib/passkey-registration.ts` (`createRegistrationChallenge`/`verifyRegistrationChallenge`), used by both routes; each keeps its own auth-gate check and post-action (issue a session vs. not) since those genuinely differ.
  5. **Investigated, confirmed pre-existing, left as-is** — the login form only has a phone field, so an email-only account could never log back in. Checked git history: this was already true under the original OTP-based login form since Stage 2's first onboarding-UI commit (`3bf3e53`) — not something this migration introduced or could have introduced (the same single-field limitation exists on `/teams/new`'s admin form too). Fixing it is a separate, pre-existing scope gap unrelated to "replace OTP with passkeys"; tracked below as a known gap rather than fixed here.
- **Decisions made during this slice:**
  - Passkey registration during invite acceptance is scoped by the invite *code* (not a bare user ID) and bounded to a 15-minute post-acceptance window (`PASSKEY_REGISTRATION_WINDOW_MINUTES`) — asked and confirmed with the user before implementation, so that capturing an already-used invite link can't be used to attach a new credential to someone else's account indefinitely.
  - The very first team admin (bootstrapped via `POST /teams`, no invite involved) registers a passkey through a *separate*, `requireAuth`-gated endpoint pair (`/auth/passkey/register/*`), reusing their already-issued session rather than inventing a second invite-like mechanism just for the bootstrap case. This same authenticated pair doubles as "add a second device" for any already-logged-in user.
  - `@simplewebauthn/server` ships as CommonJS with no `exports` map, so `import { generateRegistrationOptions } from '@simplewebauthn/server'`-style named imports would typecheck but risk the exact same runtime ESM/CJS failure `rrule` hit in the Stage 3 checkpoint — avoided from the start by using the library's actual named exports directly (its package resolves cleanly, unlike `rrule`'s default-export-only quirk) and verifying with a real `tsx` boot before considering the integration done, not just a passing test suite.
  - `OtpChallenge`/`OtpChannel` were dropped outright (not deprecated in place) — no production data exists yet, and CLAUDE.md's own precedent for pre-1.0 schema changes is to change cleanly rather than carry compatibility shims.
- **Blocker or risk:** None blocking. Known, explicitly-deferred gaps: (1) login only supports phone, not email, for an email-only account (pre-existing, see finding #5 above); (2) no automated way to exercise a real biometric/security-key completion in this environment — covered by the fake-verifier test suite, but a real device should be used at least once before pilot; (3) general API-wide rate limiting (CLAUDE.md §9.4's 100 req/min per user, 1000 req/min per IP) remains unimplemented — was never built even under OTP, still a Stage 6 item, not something this checkpoint was scoped to add beyond the specific passkey-login limit above.
- **Next concrete action:** Commit and open the PR for this checkpoint.

**Progress (2026-08-10 — Stage 2 closed):**
- **Status:** Done. All code-buildable work for Stage 2 was already complete; the three remaining items were process/decision gaps, not code, and are now resolved:
  1. **Native-speaker Hebrew review** — the user confirmed they are a native Hebrew speaker and reviewed `packages/i18n/src/messages.ts` directly; approved as-is, no changes needed. The "AI-drafted, not yet reviewed" caveat that had been carried inline in that file and in this plan since Stage 2's i18n-foundation checkpoint no longer applies.
  2. **Staging deployment** — the user explicitly chose not to pick an external hosting vendor (the open Stage 0 decision that had been blocking this) and instead defined "staging" as the local `docker compose` stack this project has used for every manual-verification pass since Stage 2 began (`scripts/start-manual-tests.sh`: real Postgres/Redis, no mocks, both dev servers). This isn't a new environment stood up today — it's a decision that the extensive manual verification already performed throughout Stage 2 and Stage 3's four checkpoints *counts* as the staging verification those exit criteria ask for.
  3. **Automated a11y/responsive tooling** — re-homed to Stage 6 (see that stage's "accessibility gates" item), which already owns Playwright/axe tooling per this document's own Testing Strategy table; building that infrastructure now would have meant front-loading Stage 6's job rather than closing a genuine Stage 2 gap. Stage 2 is marked done on the strength of the manual multi-viewport spot-checks already performed in the design-tokens checkpoint.
- **Decisions made during this closure:**
  - "Staging" for this project is the local Docker Compose stack, not a separately hosted environment, until/unless a real pilot needs otherwise. This is a durable definition, not a one-time exception — future stages' "on staging" language should be read against this stack unless explicitly revised.
  - Both remaining i18n gaps from the original "What's missing" list (localized server error messages, untranslated placeholder examples) are *not* resolved by this closure — they were never exit-criteria blockers, just documented scope boundaries, and remain open for whenever backend-i18n work is prioritized.
- **Blocker or risk:** None. Stage 2 is closed.
- **Next concrete action:** Continue Stage 3's remaining checkpoints (schedule-template wizard or session management next, per the established one-screen-at-a-time sequencing).

### Stage 2 Exit Criteria

- [x] Invited parent and admin journeys work on staging with enforced authorization.
  - **"Staging" decided 2026-08-10**: no separate hosted environment — the user explicitly chose to treat the local `docker compose` stack (`scripts/start-manual-tests.sh`: real Postgres/Redis, both dev servers, no mocks) as staging for this project, rather than picking an external hosting vendor for a not-yet-piloted app. Every checkpoint from Stage 2 onward has already been manually verified against exactly this stack — invite creation, invite-accept + passkey registration, cookie/CSRF-authenticated journeys, admin-only screens (member management, collection points), and authorization enforcement (403/404 on cross-team and non-admin access) — so this criterion is retroactively satisfied by that accumulated verification, not a new pass.
- [x] A user can change English/Hebrew in place and key shells correctly mirror direction.
  - Functionally verified repeatedly in a real browser against the same stack (see above): full RTL mirroring (sidebar side, icon/toggle order, no clipped text), Hebrew date formatting, instant switching with no reload. Unblocked by the same staging redefinition.
- [x] The design system passes keyboard, screen-reader smoke tests, contrast checks, and responsive screenshots.
  - **Re-homed to Stage 6, 2026-08-10** (explicit user decision): this project's own Testing Strategy table already assigns Playwright-based e2e tooling to Stage 6, not Stage 2, so building that infrastructure now would front-load Stage 6's job rather than close a Stage 2 gap. Accepted as satisfied for Stage 2 on the strength of what's already there: manually spot-checked at three viewports during the design-tokens checkpoint (44px touch targets, no horizontal scroll, contrast via visual inspection), semantically-accessible primitives (roles, `aria-live`, focus rings, `aria-describedby`) built throughout. The *automated, repeatable* version of this check (axe-core + Playwright screenshots) is tracked as a Stage 6 exit criterion instead — see that stage's checklist.

## Stage 3: Schedule, Collection Points, and Atomic Shift Core

Depends on Stages 1 and 2. Domain API and web schedule views can proceed in parallel once contract shapes are agreed.

- [x] Add schema/migrations for schedule templates, practice sessions, collection points, session-point assignments, shifts, and immutable audit/outbox records.
  - `CollectionPoint`, `ScheduleTemplate`, `ScheduleTemplateCollectionPoint`, `PracticeSession`, `SessionPointAssignment`, `Shift` added and migrated (`20260809222958_stage3_schedule_shift_core`). Two deliberate deviations from CLAUDE.md's Appendix B literal schema, both flagged during design and approved: (1) `PracticeSession.startsAt` is a single `DateTime`, not separate `date`/`time` columns — simpler to sort/query, avoids Postgres `TIME`-type friction in Prisma; (2) no separate outbox table — matches existing precedent (nothing in the repo has an outbox yet; audit log is the durable record, same as team/invite/OTP writes).
- [x] Implement recurrence parsing/generation using a tested RRULE library. Generate the configured horizon idempotently, preserve past/in-progress records, and prevent cancelled sessions from regenerating shifts.
  - Initial generation: the `rrule` package parses the recurrence rule, `apps/api/src/lib/recurrence.ts` (unit-tested) turns it into occurrence instants, and template creation generates the full horizon in one transaction. A `(templateId, startsAt)` unique constraint makes creation idempotent against duplicate sessions. **Regenerate-on-edit** (`PATCH /teams/:teamId/schedule-templates/:templateId`): recomputes the full occurrence set from the template's original `startDate` under the (possibly edited) rule/time/horizon, and creates a session for any *future* occurrence that doesn't already have one — additive only, never touching an existing session's content or existence (including one that no longer matches the new pattern), so past and in-progress records are provably preserved by construction rather than by a separate guard. See the 2026-08-10 (template-edit) Progress note for the explicit design decision behind "additive only" and its one notable consequence (a `defaultTime` change adds parallel sessions at the new time rather than moving existing ones).
- [x] Implement an admin schedule-template wizard, future-session bulk edits, individual session edits/cancellation, and per-session collection-point/player overrides.
  - Backend: `PATCH /teams/:teamId/sessions/:sessionId` (date/time/location), `POST /teams/:teamId/sessions/:sessionId/cancel`, `PATCH /teams/:teamId/sessions/:sessionId/points/:pointId` (per-session-point player assignment, validated against the team roster), and `PATCH /teams/:teamId/schedule-templates/:templateId` (template edit + regeneration) — all now blocked once a session has already happened, not just once it's cancelled (2026-08-11 Progress note below). Admin UI: the schedule-template wizard (`/admin/schedule-templates`, Checkpoint 6) and, as of Checkpoint 7 (2026-08-11 Progress note below), inline session edit/cancel and per-point "Manage players" controls directly on the parent-facing `/schedule` page for admins. **Still explicitly deferred to the backlog, not blocking Stage 3 closure (see "Explicitly deferred out of Stage 3" above):** "bulk-edit all future sessions to time X" — a separate, explicit mass-apply tool per CLAUDE.md's own wording, deliberately not what template-edit does (which only affects which sessions *exist*, never their content).
- [x] Build collection-point management with address, optional coordinates, valid direction type, player-assignment validation, and visibility of the full assigned player list before a driver claims.
  - Backend CRUD (`apps/api/src/routes/collection-points.ts`) and now the admin web UI (`apps/web/src/app/admin/collection-points/page.tsx`) are both done and tested: create/list/edit/delete, GPS coordinates optional and clearable (client-side validated so a syntactically-valid-but-overflowing value like a huge exponent can't silently become JSON `null` and fail the server's schema), and delete blocked with a friendly conflict message if the point already has scheduled shifts. This is the first real admin-only page in the app — linked from Home's admin-gated team card, gated by a redirect-to-`/home` if the caller isn't an admin on any team. **Not covered by this checklist item's own wording, but now closed anyway:** "visibility of the full assigned player list before a driver claims" is a *session*-level concern (which players are assigned to a point for a given session), not a collection-point-management one — the Schedule page now resolves `playerIds` to real names per point (Checkpoint 7, 2026-08-11 Progress note below), closing this.
- [x] Generate and maintain exactly one shift per valid session/point/direction; use unique constraints to prevent duplicates and prevent administrative edits from silently deleting historical records.
  - `unique([sessionId, pointId, direction])` on both `SessionPointAssignment` and `Shift`; a `both`-type point expands into two shifts (one per direction) at generation time, per the architecture decision. Session cancellation flips `status` only — it never deletes or mutates existing shift rows, so historical claim/release records survive a cancellation intact.
- [x] Implement `claimShift` as an atomic conditional update and audit/outbox transaction. Return a typed conflict payload containing the current holder's display-safe name when another parent wins.
  - `POST /teams/:teamId/shifts/:shiftId/claim` — compare-and-set `updateMany` predicated on **both** `status: 'open'` and the expected `version` (not status alone — a code-review finding caught that the first pass only guarded on status, which the architecture decision explicitly calls out as insufficient; fixed by predicating on the pre-read version too, closing an ABA gap around a release-then-reclaim race). A zero-count update re-reads the shift and throws a `409` carrying `{ holderName }` — added a small, reusable `HttpError.details` field (mirrored on the web `ApiError`) rather than a one-off exception type, since Stage 4's swap conflicts will need the same shape.
- [x] Implement `releaseShift` only for the current holder, preserve version history, return the shift to open state, and notify the same event pipeline.
  - `POST /teams/:teamId/shifts/:shiftId/release` — same version-gated CAS pattern, predicated on `status: 'claimed'`, the caller's own `assignedUserId`, and the expected version. Blocked with a `409` if the session is no longer `scheduled` (a review finding: the first pass let a holder "release" a shift on an already-cancelled session, which made no sense — fixed to mirror `claimShift`'s same session-status guard). "Notify the same event pipeline" doesn't apply yet — there's no notification pipeline until Stage 4.
- [x] Implement read models for current, upcoming, and historical schedules. Mark historical sessions read-only and distinguish open, covered, assigned-to-me, and emergency state without relying on color alone.
  - `GET /teams/:teamId/sessions` (optional `from`/`to`) returns every session with its full shift/assignment tree in one response — functionally covers all three views since the dataset is small at this scale (one team, one season). A dedicated split into separate current/upcoming/historical read *endpoints* remains an accepted, documented functional-equivalent gap (not planned work, same treatment as Stage 2's re-homed a11y tooling — see "Plan to Close Stage 3" above). Historical sessions are now genuinely read-only at the API level, not just in the web UI's own restraint: every mutating session endpoint rejects a session whose start time has already passed, regardless of its `status` (2026-08-11 Progress note below). `mine`/`covered`/`open` tones are wired end-to-end using the existing `packages/ui-tokens` status system (icon + color, never color alone); `urgent`/`emergency` state doesn't apply yet — that's Stage 4's escalation concept.
- [x] Build the web Home workspace: a concise next-action strip, personal assignments, pending swap count, help-needed shifts, and compact personal coverage summary. Avoid a marketing landing page.
  - `apps/web/src/app/home/page.tsx`'s `HomeWorkspace`: a `StatusBadge` strip ("N shifts coming up" / "N still need a driver"), "your upcoming shifts" (with Release), "shifts that need a driver" (with Claim, capped at 5 with a "+N more" link to Schedule), a "My Stats" card (personal counts + team average by direction, via the new `GET /teams/:teamId/shifts/stats`), and a pending-swaps row. The pending-swaps count is a static "0 pending" placeholder with no action — Stage 4 hasn't built `SwapRequest` yet, so there's nothing real to count; asked and confirmed with the user rather than assumed. Team-average is computed server-side specifically so a non-admin caller never needs the team roster (`GET /teams/:teamId/shifts/stats`, any team member) — keeps CLAUDE.md §13's "parents see only their own stats" boundary intact while still letting every parent see the average for self-comparison.
- [x] Build Schedule as a scannable week/list experience with session row/detail views, two independently labeled directions, collection points, player counts, available drivers, and direct claim/release entry points.
  - `apps/web/src/app/schedule/page.tsx`: sessions listed chronologically, each showing its collection points split by direction (`Drop-off`/`Pick-up`, i.e. `TO_PRACTICE`/`FROM_PRACTICE`), real player *names* per point (not counts, as of Checkpoint 7), a `StatusBadge`, and a claim/release button wired to the real API with conflict-toast handling. This is the first page in the app to render a date — `formatDate` from `packages/i18n` is now a real, consumed dependency, explicitly pinned to `timeZone: 'UTC'` so the displayed time matches what was entered regardless of the viewer's browser timezone (a genuine bug caught by manual verification: the schedule initially showed `21:00` for an `18:00` session because `Intl` silently used the browser's local timezone). "Available drivers" — undefined in CLAUDE.md itself, and there's no availability/calendar data model to compute who's actually free — was interpreted (2026-08-11, asked and confirmed with the user) as a "Team members" roster section on the page, backed by a new parent-readable `GET /teams/:teamId/roster` endpoint. **Not built:** a separate session-detail view (everything is inline in the list row).
- [ ] Build admin schedule/collection-point screens as dense operational tables and side panels rather than stacked decorative cards. Include validation summaries before saving changes.
  - Collection-point management shipped (see the item above), but as `DataList`/`DataListItem` cards — the same list primitive Home and Schedule already use — not the "dense operational tables and side panels" this item specifically calls for. Deliberate consistency choice: no table component exists anywhere in this codebase yet (confirmed before starting), and introducing one for a single small admin list felt premature. Worth revisiting once more admin screens exist and a denser pattern is actually justified by the data volume. Session/schedule-template admin screens themselves are still entirely unbuilt.

**Progress (2026-08-10):**
- **Status:** Done for this checkpoint (backend atomic-shift core + parent-facing Schedule web page). Admin UI, Home-workspace revamp, and template-edit/bulk-edit are deferred to a follow-up checkpoint by explicit agreement before implementation started.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (138 tests across all packages — up from 133 before this checkpoint's own code-review fixes added 5 more), and `pnpm build` all pass from a clean checkout against a real Postgres (`docker compose up -d`). Manually verified end-to-end in a real Chrome browser via `scripts/start-manual-tests.sh` against real seeded data (extended in `apps/api/prisma/seed.ts` with 2 collection points + a generated 8-week/24-session template): logged in as the seeded admin, viewed the real generated schedule, claimed and released a shift (state updated immediately, no reload), and — using a second real logged-in session (`curl` with cookie-based auth as a second seeded parent) — confirmed a shift claimed by one user shows correctly as "Covered by \<name\>" to another, including after switching the UI to Hebrew (full RTL mirroring, `יום ד', 12.08.2026` date formatting, "מכוסה על ידי Avi Levi" status label, all confirmed live). Did not exercise the full desktop/tablet/narrow-mobile viewport matrix Stage 2's design-tokens checkpoint ran — only default-viewport + RTL were manually checked this round; worth doing before this ships to pilot.
- **Code review:** Ran `/code-review medium` against the full diff. 7 findings, all addressed:
  1. **Fixed (real bug)** — removing a team member never released any shift they held; the `Shift.assignedUserId` FK is `onDelete: SetNull` but removal only deactivates the `User` row, which doesn't fire that FK, so a removed member's claimed shift was stuck forever (nobody else could claim or release it) — violates CLAUDE.md §4.2. Fixed: member removal now finds and releases any shifts the removed user held on that team, in the same transaction, with an audit entry per shift.
  2. **Fixed (real bug)** — `releaseShift` had no session-status guard (unlike `claimShift`), so a holder could "release" a shift on a session that had since been cancelled, flipping it back to `open` on a session nobody should be able to act on anymore. Fixed to mirror `claimShift`'s guard.
  3. **Fixed (real bug)** — a `PATCH` session edit that collided with another session's `(templateId, startsAt)` unique constraint threw an unhandled `500` instead of a clean conflict response, since the central error handler only mapped Prisma's `P2025` (not found), not `P2002` (unique violation). Added a generic `P2002` → `409` mapping to the shared handler in `app.ts` — benefits any future route with a similar constraint, not just this one.
  4. **Fixed (real bug, closed by the version-gating fix below)** — the audit log's `beforeState.version` on claim/release was read before the transaction started, so under tight concurrent activity it could log a stale version number. Adding the expected-version predicate to the compare-and-set (finding #7) makes this provably correct: if the pre-read version were stale, the predicate itself would have failed to match and the request would have taken the conflict branch instead.
  5. **Fixed (real bug)** — `POST /schedule-templates` compared `points.length` (deduplicated by the DB query) against the raw, possibly-duplicated `collectionPointIds.length`, so a client accidentally submitting a duplicate id (e.g. a double-submitting form) got a false "not found" `400` even though every id was valid. Fixed by de-duplicating the input before the comparison.
  6. **Fixed (real bug)** — collection-point `PATCH` could never clear a previously-set GPS coordinate: the request schema allows omitting `gpsLat`/`gpsLng`, but Prisma treats an omitted (`undefined`) field as "leave unchanged," not "clear it" — and this endpoint has full-replace (not partial-patch) semantics for every other field. Fixed by writing `body.gpsLat ?? null` explicitly in both create and update.
  7. **Fixed (real gap vs. the architecture decision)** — `claimShift`/`releaseShift` only predicated their compare-and-set on `status`, never on `version`, even though PLAN.md's own architecture decision says the update "must predicate on the expected version and valid current state" (plural conditions, not status alone). Today's two-state open/claimed cycle made this safe in practice, but it left the `version` column vestigial and set no precedent for Stage 4's swap-accept, which explicitly needs version-aware staleness detection. Fixed by adding `version: shift.version` to both `WHERE` clauses.
  - All 7 findings got a regression test (5 new API tests: member-removal releases shifts, release blocked on a cancelled session, `PATCH` collision returns `409`, duplicate collection-point ids accepted, GPS coordinates clearable).
- **Decisions made during this slice:**
  - Checkpoint scope was agreed with the user before implementation: this slice = backend atomic-shift core + the parent-facing Schedule web page; template-edit-regenerates-sessions and bulk-edit are explicitly deferred to a follow-up checkpoint (asked and confirmed, not assumed).
  - `rrule` (the standard RFC 5545 JS library) ships as CommonJS with no `exports` map, so Node's ESM loader can't statically detect its named exports — `import { RRule } from 'rrule'` typechecks fine (types resolve correctly) but **throws at runtime** under `tsx` (this repo's dev/start/seed runner), while passing silently under Vitest/`tsc` since those resolve differently. Caught only by actually booting the dev server and running the seed script after implementation looked done — not by the automated test suite. Fixed with a default import + destructure (`import RRulePackage from 'rrule'; const { RRule } = RRulePackage;`), documented inline as a trap for the next person touching this file.
  - `SessionPointAssignment.playerIds` and the `both`→two-shifts expansion follow the architecture decisions already recorded lower in this document (kept `SessionPointAssignment` as configuration, `Shift` as the separately claimable unit; generate one shift per valid `(session, point, direction)` tuple).
  - No service/domain layer introduced — followed the existing convention (business logic inline in route handlers), per the explorer agent's explicit finding that no such layer exists yet in this codebase; introducing one here would have been an unprompted architectural deviation, not an extension of established convention.
- **Blocker or risk:** None blocking. Known, explicitly-deferred gaps (tracked above per-checklist-item): no admin UI at all yet for schedule templates, collection points, or session management; template-edit does not regenerate future sessions; no dedicated player-name lookup (Schedule shows counts, not names); no full desktop/tablet/mobile viewport regression pass (only default viewport + Hebrew/RTL manually checked this round).
- **Next concrete action:** Commit and open the PR for this checkpoint. After merge: either the admin schedule/collection-point management UI, or the Home-workspace revamp — both were explicitly deferred here, not yet prioritized against each other.

**Progress (2026-08-10 — Home workspace):**
- **Status:** Done for this checkpoint. Builds on checkpoint 1's backend/Schedule-page work — asked and the user explicitly chose the Home-workspace revamp over the admin UI and template-edit backend (the two other options deferred from checkpoint 1). Two follow-up questions were also asked and answered before implementation: the team-average stat gets its own small backend endpoint rather than showing only the parent's own count, and the pending-swaps section shows a static "0" placeholder rather than being omitted (Stage 4's real `SwapRequest` doesn't exist yet).
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (159 tests across all packages — up from 138 before this checkpoint: 7 new backend tests for `GET /teams/:teamId/shifts/stats`, and the Home page's component test file grew from 3 to 8 cases), and `pnpm build` all pass from a clean checkout against a real Postgres. Manually verified in a real Chrome browser: navigated to `/home` using an already-authenticated real session already present in the browser (not one this session fabricated), confirmed the new `GET .../shifts/stats` call succeeding live (`200`, visible in the network panel) alongside `GET .../sessions`, and confirmed every new string renders correctly in Hebrew/RTL (the browser's active locale) — next-action strip, both empty states, the static pending-swaps placeholder, and the stats card's per-direction breakdown, all right-aligned with no layout breaks, no console errors. **Known limitation of this verification round, same one already documented in the 2026-08-10 passkey-authentication note:** this environment's browser-automation tools have no CDP virtual-authenticator support, so a real passkey ceremony can't be driven end-to-end to reach a *populated* team's Home view (the available real session's team had no schedule data) — the claim/release/populated-list/non-zero-stats paths are covered by the automated component tests (realistic mocked data, exact-text assertions) and the 5 real-Postgres API tests for the stats endpoint instead, not by a live click-through with real numbers.
- **Code review:** Ran `/code-review medium` against the full diff. 8 findings; 6 fixed, 2 accepted as-is:
  1. **Fixed (real bug)** — claim/release on Home called a `reloadSessions()` that set the whole subtree back to `'loading'`, unmounting every section (assignments, help-needed, stats, pending-swaps) on every successful action instead of updating just the affected shift — a jarring full-page flash CLAUDE.md §3.8 explicitly warns against. Fixed by patching the one changed shift in place (extracted the existing `updateShiftInSessions` helper out of `schedule/page.tsx` into a shared `apps/web/src/lib/sessions.ts` so both pages use the same logic); `reloadSessions()` is now reserved for the genuine resync-after-conflict path only, matching the Schedule page's own pattern.
  2. **Fixed (real bug)** — the "+N more" link under help-needed shifts went to a bare `/schedule` with no team context, but `/schedule` independently defaults to the user's *first* team membership. A multi-team parent (CLAUDE.md §3.15) viewing Team B on Home and tapping "+N more" would land on Team A's schedule instead. Fixed: the link now carries `?team=<id>`, and `/schedule` reads it via `useSearchParams()` to override its default when present.
  3. **Fixed (real gap)** — the stats-loading effect had no unmount-cancellation guard, unlike the sibling sessions-loading effect right above it, even though both set state from an async continuation. Added the same `cancelled` flag pattern.
  4. **Fixed (real gap)** — `shiftStatsResponseSchema`'s `mine`/`teamAverage` shared one schema with no numeric constraints, despite the adjacent comment stating `mine` is always integer counts and `teamAverage` a float — so a regression producing a fractional personal count would pass validation silently. Split into `personalShiftCountsSchema` (`.int().nonnegative()`) and `teamAverageShiftCountsSchema` (`.nonnegative()` only).
  5. **Fixed (real bug, drift)** — Home's `ShiftRow` duplicated Schedule's `SessionCard` date-formatting options and had already drifted (missing `year: 'numeric'`), so a shift far enough out to cross a year boundary would show inconsistent dates between the two pages for the same session. Fixed by extracting one shared `formatSessionStartsAt(locale, startsAt)` into `apps/web/src/lib/sessions.ts`, used by both.
  6. **Accepted as-is** — Home's claim/release handlers duplicate the same 409-conflict-toast flow already in Schedule's handlers. A shared hook would remove the duplication but needs to accommodate two different local-update strategies (Home's in-place patch vs. Schedule's identical-but-separately-instantiated one); deferred rather than rushed, tracked as a follow-up.
  7. **Accepted as-is** — the stats endpoint's 4 separate `shift.count()` calls could be 2 `groupBy()` calls instead. Not a scale problem at CLAUDE.md's ~100-user target (explicitly noted by the reviewer too); a pure maintainability nice-to-have, not fixed this round to keep the diff focused on the checkpoint's actual UX/bug findings.
  - All 6 fixed findings covered by existing or updated tests: the two web-test files were already asserting DOM text and API call counts, updated where the reload-vs-patch behavior change affected an assertion (`api.listSessions` call count) rather than needing wholly new cases; the schema constraint change is covered by `packages/contracts/src/shift.test.ts`'s existing round-trip cases (still pass with the tighter types).
- **Decisions made during this slice:**
  - Extracted `apps/web/src/lib/sessions.ts` (`updateShiftInSessions`, `formatSessionStartsAt`) as the first shared module between the Home and Schedule pages — both pre-existed as page-local duplicates before this checkpoint's review caught the drift; not a new architectural layer, just deduplicating two copies of the same pure logic into the one place future changes need to happen.
  - "Upcoming" for both the assignments and help-needed lists means `status === 'scheduled'` and `startsAt` in the future — cancelled/completed sessions and past sessions are excluded from Home entirely (they're still visible on the full Schedule page).
  - Help-needed shifts are capped at 5 visible rows with a "+N more" link rather than an unbounded list, keeping Home's "what do I need to do this week" framing (CLAUDE.md §3.8) instead of duplicating the full Schedule page inline.
- **Blocker or risk:** None blocking. Known, explicitly-accepted gaps: the two deferred code-review findings above (shared claim/release hook, groupBy-based stats query); no live click-through with real populated data (documented above, same root cause as the passkey checkpoint's equivalent gap — no virtual-authenticator support in this environment's browser tools); pending-swaps is a placeholder until Stage 4 builds real `SwapRequest`s.
- **Next concrete action:** Commit and open the PR for this checkpoint. After merge: the admin schedule/collection-point management UI and template-edit/bulk-edit remain the two pieces deferred from checkpoint 1, still not yet prioritized against each other or against starting Stage 4.

**Progress (2026-08-10 — template-edit backend):**
- **Status:** Done for this checkpoint. Adds `PATCH /teams/:teamId/schedule-templates/:templateId`, closing the last backend piece of Stage 3's recurrence-generation checklist item. Backend + tests only — no admin UI exists yet to call it from (that's the separate, still-deferred "admin schedule/collection-point management UI" item), exercised via API integration tests the same way collection-point CRUD was before its UI existed.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (174 tests across all packages — up from 159 before this checkpoint), and `pnpm build` all pass from a clean checkout against a real Postgres. No UI to manually verify in a browser this round (backend-only checkpoint); instead relied on a thorough real-Postgres integration-test suite (16 new tests) covering the actual behavior a browser walkthrough would otherwise need to exercise: an expanded rule adds new sessions while leaving every existing one byte-for-byte untouched (same id, same `startsAt`); a shrunk horizon creates nothing and removes nothing; a changed `defaultTime` adds parallel sessions at the new time without moving sessions at the old time; changed collection points apply only to newly-created sessions; and — the one most worth getting right — a template spanning past and future occurrences never creates a new session for a past occurrence, verified against real (not mocked) past/future wall-clock dates so the test can't be fooled by a frozen clock.
- **Code review:** The usual `/code-review medium` subagent hit this session's usage-limit wall partway through (reset time surfaced by the tool, not something this session controls) and returned no findings before terminating. Rather than block on the reset, did a manual self-review pass against the diff instead, focused on the same categories that pass would have checked (race conditions, authorization, DRY, audit completeness). Two real findings, both fixed:
  1. **Fixed (real audit-log gap)** — the `schedule_template_updated` audit entry's before/after state recorded `recurrenceRule`/`defaultTime`/`defaultFieldLocation`/`horizonWeeks` but never `collectionPointIds`, so an edit that changed *only* the collection points produced a byte-identical before/after diff — a real gap against CLAUDE.md §5's "Collection point assigned/reassigned" loggable event. Fixed by always recording the effective point-id set in both states (not just when it changes, so the field is never silently absent). Regression-tested: a collection-point-only PATCH now asserts the audit entry's `before`/`after` actually differ.
  2. **Fixed (DRY)** — the "de-duplicate ids, load them, 400 if any weren't found on this team" collection-point validation was duplicated near-verbatim between `POST` and the new `PATCH`. Extracted `loadAndValidateCollectionPoints()`, used by both.
  - Considered but not treated as a finding: a race between two concurrent template edits computing the same "missing occurrences" set outside the transaction and both attempting to create the same session. The `(templateId, startsAt)` unique constraint (already relied on by the original create-path's idempotency) makes the loser's `createSessionWithShifts` call fail with Prisma `P2002`, which the shared error handler already maps to a clean `409` (added in checkpoint 1's own code review) — no new gap, existing infrastructure already covers it.
- **Decisions made during this slice:**
  - Confirmed with the user before implementation: a future, still-scheduled, template-generated session that no longer matches an edited pattern (dropped weekday, shrunk horizon) is left untouched, never auto-cancelled. Regeneration is purely additive — it only ever creates sessions for occurrences that don't already exist, and never modifies or removes an existing session row's content or existence, matching this codebase's established "never delete, only status-flip, never silently overwrite" convention. The direct, accepted consequence: changing `defaultTime` doesn't move already-generated future sessions to the new time — it adds new sessions at the new time alongside whatever's already on the books at the old time (documented inline in the route and covered by its own test).
  - `startDate` is deliberately not part of the editable request shape — it's the recurrence anchor (`dtstart`); changing it would shift the phase of every future occurrence in a way CLAUDE.md doesn't ask for and that has no clean, obviously-correct interpretation.
  - "Bulk-edit all future sessions to time X" (a separate CLAUDE.md acceptance criterion, worded distinctly from "template edit... regenerates future sessions") is treated as a genuinely different, still-unbuilt feature — a mass-apply tool that would deliberately overwrite existing sessions' content, which is exactly what this checkpoint's additive-only design was built *not* to do.
- **Blocker or risk:** None blocking. Known gaps: no admin UI to actually drive this endpoint yet (tracked under the pre-existing "admin schedule/collection-point management UI" checklist item); the `/code-review` subagent pass didn't run to completion due to the session's usage limit — the manual self-review pass above is a reasonable substitute but doesn't carry the same multi-angle coverage a full run would; worth a follow-up `/code-review` pass once available if anything about this endpoint changes before it ships.
- **Next concrete action:** Commit and open the PR for this checkpoint. The admin UI (schedule-template wizard, collection-point management, session edit/cancel screens) is now the one clearly remaining piece of Stage 3's original scope, alongside the still-separate "bulk-edit all future sessions" tool.

**Progress (2026-08-10 — admin collection-point management):**
- **Status:** Done for this checkpoint. First real admin-only screen (`/admin/collection-points`): list, create, edit, and delete a team's collection points. Asked and the user explicitly chose to sequence the remaining Stage 3 admin surface one screen per checkpoint, starting here — it's the smallest, most self-contained CRUD screen and the template wizard (a later checkpoint) needs it anyway (picking existing points when creating a template).
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (185 tests across all packages — up from 174 before this checkpoint), and `pnpm build` all pass from a clean checkout against a real Postgres. Manually verified twice in a real Chrome browser against a real logged-in admin session with real seeded data (an already-authenticated session already present in the browser, not one this session fabricated): full create → list → edit → delete cycle, the delete-blocked-by-scheduled-shifts `409` showing a friendly toast without deleting the point, and — after the code-review fixes below changed create/edit/delete to patch local state instead of refetching — a second pass confirming via the network panel that exactly one `POST`/`PATCH`/`DELETE` fires per action with no follow-up `GET`. All of it in Hebrew/RTL (the browser's active locale), right-aligned, no layout breaks, no app-caused console errors (six console exceptions during the session are the well-known Chrome-extension messaging-channel error, unrelated to this app — same category already documented elsewhere in this file).
- **Code review:** Ran `/code-review medium` against the full diff (the session-limit issue from the template-edit checkpoint had cleared). 6 findings; 4 fixed, 2 accepted as-is:
  1. **Fixed (real bug)** — coordinate parsing used `Number(value)` directly; a syntactically-valid-but-overflowing value (e.g. a huge exponent, which a real `<input type="number">` accepts even though jsdom's sanitization doesn't) produces `NaN`, which `JSON.stringify` silently turns into `null` — failing the server's `z.number().optional()` schema with a confusing generic error instead of a friendly field-specific one. Fixed with `parseOptionalCoordinate()`, which distinguishes blank (→ `undefined`) from present-but-non-finite (→ `null`, shown as a client-side validation error before the request is even sent). Regression-tested at the unit level, not via simulated DOM typing — see the decisions note below for why.
  2. **Fixed (real gap)** — the list-load effect's success path (`setPoints`/`setLoadState('ready')`) had no unmount-cancellation guard, unlike its own catch branch and unlike the mount effect's cleanup function — a stale response could set state on an unmounted workspace after a team switch. Fixed by guarding both branches consistently.
  3. **Fixed (real UX regression, caught live)** — create/edit/delete each called a full `reload()` (spinner + re-fetch) instead of patching the single changed/removed row from the mutation's own response. Fixed: `handleDelete` filters the deleted id out of local state directly; `PointFormDialog` now passes the created/updated point back to its parent, which patches or appends it (re-sorted to match the server's name-ascending order). Confirmed live via the network panel: one request per action, no follow-up `GET`.
  4. **Fixed (DRY)** — the "which memberships count as admin" filter was duplicated between the initial-team-pick effect and the render body, risking the two disagreeing if the predicate ever changed. Extracted `adminMembershipsOf()`, used by both.
  5. **Accepted as-is** — server-supplied error messages (English-only) render verbatim inside an otherwise fully-Hebrew UI. This is the same documented, pre-existing gap tracked since Stage 2 ("localized server error messages"), not something this checkpoint introduced or could fix without a real backend-i18n project — every other error-toast in the app has the identical behavior.
  6. **Accepted as-is** — the session-fetch/auth-redirect boilerplate is now duplicated across three pages (Home, Schedule, this one). A shared hook would remove the duplication but means touching two already-shipped, already-tested pages for a non-functional refactor; deferred rather than rushed, tracked as a follow-up (mirrors how the Home-workspace checkpoint accepted an analogous finding).
  - A code-review-driven test caught a real bug during the fix itself, not just after: the first version of the "invalid latitude" test tried to simulate typing an overflowing value into the real `<input type="number">`, but jsdom sanitizes that value to `""` before it ever reaches React (confirmed with a standalone jsdom repro) — unlike real browsers, which accept it as syntactically valid per spec. That silently made the *unmocked* `createCollectionPoint` get called and return `undefined`, crashing the list render (`Cannot read properties of undefined (reading 'name')`) — a test-only artifact, not a reachable production bug (the real client is typed and zod-validates every response). Fixed by testing `parseOptionalCoordinate()` directly instead of through simulated DOM input, which is both more precise and no longer dependent on jsdom's non-standard number-input sanitization.
- **Decisions made during this slice:**
  - No admin nav item added yet — the new page is reachable via a link inside Home's existing admin-gated team card (same pattern as the invite form already living there), not a new persistent nav entry. A real "Admin" section in the shared nav makes sense once there's more than one admin screen to link between; adding one now for a single destination would be premature.
  - Kept `DataList`/`DataListItem` (the only list primitive that exists anywhere in this app) rather than introducing a table component for this one screen — a deliberate consistency choice, tracked as a known gap against the Stage 3 exit criterion that explicitly asks for "dense operational tables," worth revisiting once more admin screens exist.
- **Blocker or risk:** None blocking. Known, explicitly-deferred gaps: the two accepted code-review findings above (localized server errors, shared auth hook); the schedule-template wizard, session edit/cancel UI, and per-session player-override UI are all still unbuilt (next checkpoints); no player-roster `GET` endpoint exists yet, which a player-override UI will need.
- **Next concrete action:** Commit and open the PR for this checkpoint. Next: the schedule-template wizard (natural follow-on, since collection points now have UI to select from) or session management — still not prioritized against each other, per the same one-screen-per-checkpoint sequencing.

**Progress (2026-08-11 — Checkpoint 5: player-roster endpoint):**
- **Status:** Done for this checkpoint. Adds `GET /teams/:teamId/players`, the backend prerequisite the "Plan to Close Stage 3" note above scoped as Checkpoint 5 — closes the one remaining blocker for Checkpoint 7's per-session player-override UI and player-names-not-counts work. Backend + contracts + tests only, per the same precedent as the template-edit backend checkpoint (no UI exists yet to call it from — that's Checkpoint 6/7's job).
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (192 tests across all packages — up from 185 before this checkpoint), and `pnpm build` all pass from a clean checkout against a real Postgres (`docker compose up -d`). No UI to click through this round (backend-only checkpoint, same as template-edit); instead manually verified against the real running dev API via `scripts/start-manual-tests.sh` + `curl`: created a real team, sent and accepted a real invite carrying two players, then confirmed `GET /teams/:teamId/players` returns both players ordered by name (`200`, using only the admin's token — no passkey ceremony needed for a *read*, unlike the passkey checkpoint's login-flow verification, which is why `curl` alone was sufficient here), an unauthenticated request gets `401`, and a second real team's admin token against the first team's roster gets `403`.
- **Code review:** Ran `/code-review medium` against the full diff. 2 findings, both fixed:
  1. **Fixed (real, if narrow, latent bug)** — `playerSummarySchema.age` was `z.number().int().positive().nullable()`, stricter than what the `Player.age` DB column actually allows (no `CHECK` constraint). No current write path can produce `age: 0` (`acceptInvitePlayerSchema` already enforces `.positive()`), so this couldn't fire today — but the response schema shouldn't be tighter than the column just because today's only write path happens to be. A future admin-edit-player endpoint or backfill producing `age: 0` would make `GET /teams/:teamId/players` throw a `ZodError` for that team's *entire* roster (mapped to a confusing `400 Invalid request` by the global handler) rather than the real bug being a server-side schema/data mismatch. This schema is shared with `acceptInviteResponseSchema.players` (pre-existing, not introduced by this checkpoint), so the fix benefits both response paths. Relaxed to `.nonnegative()` — deliberately left `acceptInvitePlayerSchema`'s `.positive()` untouched, since that's a genuine write-side registration business rule, not a storability constraint. Regression-tested (`packages/contracts/src/player.test.ts`).
  2. **Fixed (minor efficiency)** — the roster query fetched every `Player` column (including `teamId`, `createdAt`) only to discard them in the response mapper. Added `select: { id: true, name: true, age: true }`.
- **Decisions made during this slice:**
  - The response shape is a flat roster list (`{ players: [{ id, name, age }] }`), not nested under session/collection-point context, matching how the plan's own wording ("player-roster endpoint") and the existing `GET /teams/:teamId/sessions` response (which already carries `playerIds` per assignment) divide responsibilities — the client joins roster names against the `playerIds` it already has from `GET /sessions`, rather than this endpoint duplicating that structure.
  - Followed the exact route/auth/response pattern `collection-points.ts` established (`requireAuth` + `requireTeamRole(..., ['parent', 'admin'])`, `teamParamsSchema`, alphabetical `orderBy`) rather than introducing anything new — this is a single read endpoint with no state to mutate, so there was nothing to deviate for.
- **Blocker or risk:** None blocking. Checkpoint 6 (schedule-template wizard UI) or Checkpoint 7 (session management UI + Schedule page follow-through, which is what will actually consume this endpoint) are next, per the agreed order in "Plan to Close Stage 3" above.
- **Next concrete action:** Commit and open the PR for this checkpoint. Then: Checkpoint 6, the schedule-template wizard UI.

**Progress (2026-08-11 — Checkpoint 6: schedule-template wizard UI):**
- **Status:** Done for this checkpoint. New `/admin/schedule-templates`: list, create, and edit a team's recurring practice-session templates via a friendly weekday-checkbox + frequency picker (builds the RRULE string under the hood) rather than raw recurrence syntax, matching CLAUDE.md's "creation wizard" wording, plus collection-point selection reusing the `/admin/collection-points` screen's patterns. Also introduces a shared, first-class admin nav (see the decision below), replacing the single buried "Manage collection points" text link on Home.
- **How this checkpoint started:** the branch already had two unmerged, uncommitted commits from earlier work-in-progress on this exact checkpoint (found when creating the branch — `git branch` already had `stage3-schedule-template-wizard` with real history, never pushed or noted in this file). Reviewed that work end-to-end before touching anything: it built the wizard page, a `recurrence.ts` build/parse round-trip helper (with a graceful read-only fallback for any existing rule the simple picker can't represent, e.g. one with `COUNT`/`UNTIL`), extended the schedule-template read model with `collectionPointIds` (needed for the edit form to pre-fill selected points — a gap this checkpoint's own plan hadn't fully accounted for), a `FieldsetGroup` primitive fixing a real accessible-name bug the prior session had caught and fixed live in a real browser (nested `<label>`s inside `Field` gave every checkbox in a group two competing label associations, which Chrome's accessible-name algorithm resolved to nothing), and a thorough test suite. Judged it solid and adopted it as the foundation — rebased cleanly onto `main` (picking up Checkpoint 5 and the Stage 3 closure plan) rather than rebuilding from scratch, then added this session's own scope on top (see below).
- **Added on top of the adopted work:** (1) `GET /teams/:teamId/schedule-templates` now validates its response through `scheduleTemplateListResponseSchema.parse()` — it was returning a raw unvalidated object, the one route in this file that hadn't followed the rest of the codebase's convention, worth fixing now that this checkpoint makes it a real, load-bearing endpoint for the first time (previously untested and unused by any UI). (2) Replaced the prior work's "keep today's pattern" choice (a second plain-text link on Home) with a real admin nav: asked the user directly, given the collection-points checkpoint's own note had explicitly deferred a shared admin nav "until more than one admin screen exists to link between" — which is now true. Landed as two flat `ShellNavItem`s ("Collection points", "Schedule templates") via a new shared `apps/web/src/lib/admin-nav.tsx` helper, used on Home (gated to the active team being one the user admins) and unconditionally on both admin pages themselves — not a nested "Admin ▾" dropdown, since `ShellNavItem`/`AppShell` has no submenu concept and the mobile bottom tab bar has no room for one; flagged this simplification to the user before implementing. Removed the now-redundant Home text links and their message keys.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (220 tests across all packages — up from 192 before this checkpoint), and `pnpm build` all pass from a clean checkout against a real Postgres. **No live browser click-through this round** — the user interrupted the manual-verification step and asked to skip it, so this checkpoint relies on its automated coverage alone (17 API integration tests including the list-response shape, a `recurrence.ts` unit suite covering build/parse round-trips and edge cases, and a `page.test.tsx` suite covering redirect/empty/list/create/biweekly/validation/edit/custom-rule-fallback/nav-link cases) rather than the real-browser pass every other UI checkpoint in this stage has had. Flagging this honestly as a gap against the checkpoint process rather than glossing over it: worth a real browser pass (both locales, RTL for the new day-picker/checkbox groups) before this ships to pilot, even though it wasn't done here.
- **Code review:** Ran `/code-review medium` against the full diff. 7 findings; 5 fixed, 1 dismissed after verification, 1 accepted as tracked debt:
  1. **Fixed (real bug)** — `parseRecurrenceRule` rejected any rule with a trailing semicolon (e.g. `FREQ=WEEKLY;BYDAY=MO;`), because the empty segment it produces has key `''`, which fails the picker's known-fields allowlist — an otherwise-representable rule would incorrectly fall back to the read-only "custom pattern" notice. Fixed by filtering empty segments before building the field map. Regression-tested.
  2. **Fixed (real localization bug)** — two new Hebrew strings (`adminScheduleTemplates.horizonWeeksLabel`, `.sessionsCreatedToast`) used משמרות ("shifts") where אימונים ("practice sessions") is this codebase's own established term (`schedule.empty` already uses אימונים for sessions; משמרת is reserved for the distinct, separately-lifecycled `Shift` entity). Fixed both to say אימונים, matching what the fields/toast actually describe.
  3. **Fixed (real bug, Rules-of-Hooks violation)** — the reviewer's suggested `useMemo` fix for a `pointsById` Map rebuilt on every render was initially placed *after* two conditional early `return`s (loading/error states) in `ScheduleTemplatesWorkspace` — a hook called conditionally, violating React's Rules of Hooks (would throw "Rendered more hooks than during the previous render" the first time `loadState` transitioned between `'loading'` and `'ready'` across renders). Caught before landing by re-reading the surrounding function structure; moved the `useMemo` above both early returns instead.
  4. **Investigated, confirmed a mistaken premise, no change** — a finding claimed lifting the admin nav to a page-level `navItems` array (gated on the single `activeMembership`) was a reachability regression for a multi-team admin, versus the prior text-link behavior showing "one link per team." Verified directly against the pre-checkpoint commit (`git show 70ac56f:apps/web/src/app/home/page.tsx`): `TeamCard` was *always* rendered exactly once, for `activeMembership` only (`<TeamCard membership={activeMembership} />`, never a `.map()` over all memberships) — the old links were already scoped to only the currently-active team, identically to the new nav items. No regression; the finding's premise was factually wrong, verified before dismissing rather than taking it on faith.
  5. **Investigated, not a bug, no change** — a finding claimed editing a template can never regenerate a session for an individually-cancelled occurrence, since the "does this occurrence already have a session" check in the PATCH handler doesn't filter by status. This is pre-existing code from the template-edit checkpoint (untouched by this diff) and, on inspection, matches that checkpoint's own explicitly documented design: regeneration is additive-only and "never touches an existing session row — not its content, and not its existence — even one that no longer matches the new pattern." A cancelled session is exactly such a row; leaving it alone (not silently resurrecting a slot an admin deliberately cancelled) is the intended behavior, not a gap. Left as-is; out of scope for this checkpoint regardless.
  6. **Accepted as tracked debt, no change** — `AdminScheduleTemplatesPage`'s top-level scaffold (session load, auth-redirect state machine, `adminMembershipsOf`, `AppShell`/`TeamSwitcher` wiring) is copy-pasted near-verbatim from `AdminCollectionPointsPage`. Real duplication, but pre-existing as of this checkpoint's starting point (the adopted work already built it this way, following the collection-points page's own precedent) and the same category of finding the collection-points checkpoint itself already accepted as follow-up debt ("session-fetch/auth-redirect boilerplate duplicated across three pages... deferred rather than rushed"). A `useAdminTeamSession()` hook is a reasonable future extraction once a third admin screen justifies it — not rushed into this diff, which already touches a lot of surface area.
  7. **Fixed (minor efficiency)** — `pointsById` was rebuilt from `points.map(...)` on every render, including renders where only `editingTemplate` changed (opening/closing the dialog). Wrapped in `useMemo` keyed on `points` (see finding 3 above for the placement fix this needed).
- **Decisions made during this slice:** see "How this checkpoint started" and "Added on top" above for the two biggest ones (adopting prior WIP; nav architecture). Smaller: `home.manageCollectionPoints`/`home.manageScheduleTemplates` message keys deleted outright (both EN and HE) rather than left as dead entries, now that nothing references them — consistent with this codebase's "if unused, delete completely" convention.
- **Blocker or risk:** No live browser verification this round (see Evidence above) — the clearest open risk, since every other Stage 3 UI checkpoint has had one and this is the checkpoint introducing the app's first checkbox-group/fieldset-legend pattern and its first custom multi-field form builder (the day/frequency picker), exactly the kind of layout/RTL-mirroring surface most likely to hide a real issue behind passing component tests. Recommend a real-browser pass (EN + HE, the day-picker and collection-point checkboxes specifically) before or alongside Checkpoint 7, not deferred indefinitely.
- **Next concrete action:** Commit and open the PR for this checkpoint. Then: Checkpoint 7 (session management UI + Schedule page follow-through) — the last piece before Stage 3's closing regression pass.

**Progress (2026-08-11 — Checkpoint 7: session management UI + Schedule page follow-through):**
- **Status:** Done for this checkpoint — the last new-UI checkpoint of Stage 3. Bundles five pieces per the "Plan to Close Stage 3" scope: past-session read-only guards at the API level, inline admin session edit/cancel + per-point "Manage players" controls on `/schedule`, player names replacing counts, a new "Team members" roster section (the "available drivers" checklist item), and the backend `GET /teams/:teamId/roster` endpoint it's built on.
- **Two clarifying questions asked and confirmed with the user before implementation** (both genuinely underspecified — CLAUDE.md never defines "available drivers," and there's no availability/calendar data model to compute who's actually free):
  1. "Available drivers" → a "Team members" roster list on the Schedule page (names + role badge), not a computed subset — nothing in the data model tracks per-parent availability. Needed a new endpoint since `GET /teams/:teamId/members` (admin-only, carries phone/email) can't be reused for a parent-facing view without a real privacy regression against CLAUDE.md §4.1 ("No public roster or user directory").
  2. Where admin session controls live → inline on the existing parent-facing `/schedule` page (edit/cancel icons per session, "Manage players" per collection-point row), not a third `/admin/sessions` list page — avoids a largely-redundant second rendering of the same sessions, matches CLAUDE.md §3.1's own wording ("session list shows all upcoming sessions with editable individual records").
- **A real, pre-existing bug surfaced immediately by this checkpoint's own guard:** adding the past-session check exposed that several existing API test fixtures (`sessions.test.ts`, `shifts.test.ts`) hardcoded `startDate: '2026-08-10'` — which, as of today (2026-08-11), is now literally yesterday. One `shifts.test.ts` test (cancel-then-release) was silently relying on cancel succeeding against what was, by the time this session ran, already a past session; the new guard turned that latent flakiness into an actual failure, caught by running the full suite rather than just the touched file. Fixed by extracting the date-helper pattern `schedule-templates.test.ts` had already established (`futureMondayDateString`/`pastMondayDateString`) into shared `apps/api/test/support/dates.ts` — now used by three test files instead of one, and the fixtures compute genuinely future dates regardless of when the suite runs.
- **Also closed a real, previously-missing guard while touching these endpoints:** `PATCH .../sessions/:sessionId/points/:pointId` (per-session player assignment) had *no session-status check at all* before this checkpoint — a cancelled session's player assignments could still be silently edited. Added the same `status !== 'scheduled'` guard `claimShift`/`releaseShift`/session-edit already carry, plus the new past-time guard, both regression-tested.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (235 tests across all packages — up from 220 before this checkpoint), and `pnpm build` all pass from a clean checkout against a real Postgres. **No live browser click-through this round either** — same as Checkpoint 6, the user asked to skip manual verification. This is now two consecutive Stage 3 UI checkpoints shipped on automated coverage alone; see Blocker/risk below.
- **Code review:** Ran `/code-review medium` against the full diff (8-angle multi-agent pass). Zero findings survived verification — the reviewer explicitly checked the three new `assertSessionNotPast` guards, the roster endpoint's field-narrowing (no phone/email leak), frontend/backend gating consistency (`isSessionPast`/`canManage` mirroring the server's own check), the removed `schedule.playersCount` key's dead-reference status, and the full test suite, and reported nothing to fix.
- **Decisions made during this slice:**
  - `GET /teams/:teamId/roster` is a deliberately separate, narrower endpoint from `GET /teams/:teamId/members` (`{ userId, name, role }` only, no `phone`/`email`) rather than loosening the existing admin-only endpoint's permission — the existing one is admin-gated specifically because it carries contact details, and parent-level visibility of *that* would be a real, unrequested privacy regression, not just a permissions tweak.
  - The response schema (`teamRosterEntrySchema`) enforces the narrower shape structurally (Zod strips unknown keys), not just by convention in the route handler — the contract test asserts a payload carrying `phone` gets stripped, so a future route handler passing the wrong DTO into this schema can't leak it either.
  - `buildStartsAt` (client) reconstructs the literal-UTC-wall-clock ISO string from separate date/time inputs via `Date.UTC(...)`, matching the API's own `combineDateAndTime` convention (session times aren't true timezone-aware instants yet — that's Stage 4's job) — deliberately not a `datetime-local` input, which carries its own browser-format/timezone ambiguity this codebase has avoided elsewhere.
  - Reused `adminNavItems` (built in Checkpoint 6) on the Schedule page too, not just Home and the two admin pages — a small, low-risk consistency completion of that checkpoint's own nav work, not new scope.
- **Blocker or risk:** No live browser verification for two checkpoints running now (Checkpoints 6 and 7) — both at the user's explicit direction, but this is the clearest open risk heading into Stage 3's closing regression pass. This checkpoint specifically introduces three new dialogs (session edit, cancel confirm, player-management checkboxes) and a new roster section, none of which have been seen rendered in a real browser, in either language. The closing multi-viewport EN/HE regression pass (see "Plan to Close Stage 3" above) is not optional cleanup at this point — it's the first real look at all of Checkpoints 6 and 7's UI.
- **Next concrete action:** Commit and open the PR for this checkpoint. With Checkpoint 7 done, all of Stage 3's checkpoints are complete — the one remaining task before the stage can be marked done is the closing manual regression pass across every Stage 3 screen, desktop/tablet/narrow-mobile, EN + HE.

**Progress (2026-08-11 — Stage 3 closing regression pass):**
- **Status:** Done. Stage 3 is now closed. Manual EN/HE regression pass across every Stage 3 screen (Home, Schedule, `/admin/collection-points`, `/admin/schedule-templates`) at desktop, tablet, and narrow-mobile widths, against a real logged-in admin session already present in the browser (real seeded data, not fabricated) via `scripts/start-manual-tests.sh`. This is the first live-browser look at Checkpoints 6 and 7's UI, which had shipped on automated coverage alone.
- **Viewports actually reached:** Desktop (~1570px) and tablet (~805px, the `md` breakpoint boundary) both resized cleanly. Narrow mobile hit an environment constraint — this browser automation setup enforces an OS-level minimum Chrome window width of ~500px, so the literal 390px target from Stage 2's own precedent wasn't reachable. 500px still exercises the mobile bottom-tab-bar code path (below the 768px `md` breakpoint) and was used instead; noting the gap honestly rather than claiming 390px was tested when it wasn't.
- **What was exercised, not just eyeballed:** admin edit-session dialog (changed field location, saved, confirmed in-place update); cancel-session confirm flow (confirmed cancelled badge appears and edit/cancel/claim/release controls correctly disappear per the past/cancelled gating); manage-players dialog (toggled a player, saved, confirmed the row's display switched from "no players assigned" to the real name); the schedule-template wizard's day-picker/frequency/collection-point form (created a real template, got the "N sessions created" toast, confirmed the new sessions appeared in the list; opened it back up in edit mode and confirmed the recurrence round-trips correctly through the picker). All of this in both languages; the create/edit/cancel/manage-players flows were driven fully in Hebrew first, then re-verified in English.
- **Real bug found and fixed:** the shared `Tooltip` component (`apps/web/src/components/ui/tooltip.tsx`) rendered its label `whitespace-nowrap`, centered on its trigger via `left-1/2 -translate-x-1/2`. The tooltip span is always present in the DOM (only `opacity-0` when not hovered, never `display:none`), so a long enough label centered on an icon near the viewport edge pushes real layout width past the viewport — confirmed via `document.documentElement.scrollWidth > clientWidth` at tablet width (833px of content in an 805px viewport). This was **always latent** (any sufficiently long `IconButton` label near an edge could have triggered it) but only actually surfaced now because Checkpoint 7's new "Manage players for {direction} · {pointName}" label is the longest one this app has ever generated. Violates CLAUDE.md §3.8's "No horizontal scrolling on main views."
  - **First fix attempt, self-caught as insufficient:** anchoring the tooltip to its trigger's fixed logical `end` edge instead of centering. This fixed the specific icon that surfaced the bug (always at its row's trailing edge) but was a directional bandaid on a shared, generic component used across five pages — a future or already-existing icon near the *opposite* edge would reintroduce the identical overflow, mirrored. Caught this myself via `/code-review medium` on the fix before it shipped, not by inspection alone.
  - **Actual fix:** measure and clamp. On mount and on every visibility toggle, a `useLayoutEffect` computes the tooltip's natural centered position from the anchor's `getBoundingClientRect()` and the tooltip's own (transform-independent) width, then shifts it via an inline `translateX` offset only enough to stay `VIEWPORT_MARGIN` (8px) inside the viewport on whichever side it would otherwise overflow — symmetric, works for triggers near either edge, and recomputed from stable inputs each time so repeated hovers can't compound a stale offset. Runs on mount (not just on first hover) since the tooltip's resting position already affects `scrollWidth` before anyone interacts with it. Verified empirically at both tablet (820px) and the narrowest reachable width (500px): `scrollWidth === clientWidth` in both languages, and visually confirmed the tooltip clamps correctly for an icon deliberately near the *left* edge in RTL (`/admin/collection-points`' delete icon), not just the original right-edge (LTR) case that surfaced the bug. The tooltip text is decorative only — `IconButton` already sets the same string as the button's real `aria-label`, independent of tooltip visibility — so this has no accessibility impact, only visual.
- **Evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, and `pnpm test` (235 tests, unchanged — the tooltip fix is a pure CSS/positioning change with no behavioral surface to add a test for) all pass from a clean checkout. No console errors observed at any point across either language or any viewport, on any of the four screens.
- **Decisions made during this slice:** none beyond the tooltip fix's own reasoning above.
- **Blocker or risk:** None blocking. The one accepted, documented gap: narrow-mobile testing landed at ~500px instead of literal 390px due to this environment's window-resize floor — the layout code path (bottom tab bar, `md` breakpoint) was still genuinely exercised, just not at the exact pixel target. Worth a real device or a differently-configured browser-automation environment before pilot if 390px-specific layout risk ever becomes a concern.
- **Next concrete action:** Stage 3 is closed. Stage 4 (swap/notification/reminder/emergency workflows) has its own detailed implementation plan already recorded below (`## Stage 4 Detailed Implementation Plan`, decided 2026-08-11) — that's the next body of work.

### Stage 3 Exit Criteria

- [x] Ten concurrent claims against one shift yield exactly one success and nine friendly conflicts.
  - Verified with a real Postgres integration test (`apps/api/test/shifts.test.ts`): 10 parallel `app.inject()` claims via `Promise.all`, asserted exactly one `200` and nine `409`s, final `version` is `1` (not 10), and exactly one `shift_claimed` audit entry exists.
- [x] Parents can claim one direction without being forced into the other and cannot double-claim the same shift.
  - Each `(session, point, direction)` tuple is its own `Shift` row with its own claim/release lifecycle; the `unique([sessionId, pointId, direction])` constraint plus the version-gated CAS makes a double-claim structurally impossible, not just discouraged by the UI.
- [x] A change to a future template, a per-session override, and a cancellation preserve the expected session and shift history.
  - Cancellation: verified (session status flips, shift rows and their claim history are untouched). Per-session override: verified for player assignment (`PATCH .../points/:pointId`); no per-session collection-point *set* override yet (only the player list within an existing assignment). Template edit: verified (`apps/api/test/schedule-templates.test.ts`'s PATCH suite) — a real Postgres integration test asserts that editing a template never removes or modifies any existing session, and specifically that no new session is ever created for an occurrence in the past, using real (non-mocked) past/future dates.
- [x] English and Hebrew schedule flows pass desktop and narrow mobile-browser regression tests.
  - Done 2026-08-11 (see the "Stage 3 closing regression pass" Progress note above): every Stage 3 screen manually verified at desktop, tablet, and narrow-mobile (~500px, an environment-imposed floor above the literal 390px target — documented as an accepted gap) widths, in both English and Hebrew, including the previously-unverified Checkpoint 6/7 UI. One real bug found and fixed (a tooltip-overflow horizontal-scroll defect). No automated Playwright coverage exists yet — that remains Stage 6's job per the Testing Strategy table, unchanged from before.

## Stage 4: Swap, Notification, Reminder, and Emergency Workflows

Depends on the atomic shift command layer in Stage 3. Notification delivery and UI workflows can run in parallel after the outbox contract exists.

- [ ] Add `SwapRequest` state transitions: pending, accepted, declined, expired, and cancelled. Enforce requester/holder/team ownership and one-active-request policy per shift as decided in the ADR.
- [ ] Implement acceptance as a conditional, version-aware shift transfer in the same transaction that finalizes the request. A stale request returns a friendly unavailable result and does not reassign a changed shift.
- [ ] Add scheduled expiry processing with idempotent jobs, audit entries, and recipient notifications.
- [ ] Build swap request/detail/inbox UI with requester, current holder, expiry time, explicit accept/decline actions, optimistic feedback only after server confirmation, and clear team context.
- [ ] Implement transactional-outbox consumers for in-app notifications, browser push, email/SMS fallback as selected, retry/backoff, delivery logs, and recipient-level preference checks.
- [ ] Standardize deep links to session/shift details and make browser-notification clicks route safely after authentication.
- [ ] Build a notification center with unread state, contextual event history, dismissal, and no duplicate alert presentation across active tabs.
- [ ] Implement reminder schedules using team timezone and user preferences: default 24 hours and approximately 2 hours before, with recipient, template, and delivery audit records.
- [ ] ~~Implement `cannotMakeIt`: explicit confirmation, transition the held shift to an emergency-open state, broadcast team urgency, allow an atomic first replacement claim, and notify resolution.~~ Removed from MVP scope 2026-08-12 — see the Stage 4 Checkpoint 10 note below.
- [ ] ~~Implement automated escalation at the configured lead time and an admin-only secondary unresolved alert at one hour remaining. Make all jobs idempotent and safely retryable.~~ Removed from MVP scope 2026-08-12 — see the Stage 4 Checkpoint 10 note below.
- [ ] Add team-level notification throttling and deduplication that preserves urgent messages while preventing rapid admin edits from becoming notification storms.

### Stage 4 Exit Criteria

- [ ] Swap acceptance, expiry, and a concurrent claim/accept race leave each shift in one valid state with a complete audit trail.
- [ ] Every MVP-changing action emits one outbox event and reaches the in-app feed; browser delivery is logged or retried.
- [ ] Reminder jobs calculate correct local team time across daylight-saving boundaries. *(Escalation dropped from this criterion — Checkpoint 10 removed from MVP scope 2026-08-12, see the Stage 4 Checkpoint 10 note below.)*

## Stage 5: Admin Operations, Reporting, and MVP Completion

Depends on Stages 2 through 4. Can overlap with pilot readiness after core workflows stabilize.

- [x] Build admin user management: invite, remove, promote, demote, active-member filters, confirmation dialogs, and disabled controls for the final active admin. *(Completed 2026-08-12: `/admin/members`, typed client methods, EN/HE copy, component coverage, and final-admin conflict recovery.)*
- [x] On removal, revoke access, cancel relevant open swaps, reopen held future shifts, preserve historical attribution, suppress future notifications, and write all events atomically where feasible. *(Completed 2026-08-12: the existing transactional cleanup is now operator-accessible; held-shift cleanup was tightened to scheduled future sessions only, and team-level row locking serializes concurrent role/removal changes.)*
- [ ] Build a read-only audit-log viewer with role guard, team scope, filters for actor/date/action/target/source, full-text-safe search, pagination, and CSV export using UTF-8 BOM where necessary for Hebrew spreadsheet compatibility.
- [ ] Replace the new-parent passkey-only onboarding requirement with an invitation-link + separate one-time code flow in which a new parent creates a password, then signs in with that password and a normalized phone number or email. Preserve passkeys and require passkey assurance for privileged admin operations.
- [ ] Add an isolated global `system_admin` role, dedicated `/system/*` APIs, an operator-safe bootstrap path, global audit records, and a bilingual system console for viewing all teams/users and managing team/system administrators without weakening existing team authorization.
- [ ] Add a basic personal statistics view from immutable shift history. Defer team-wide variance ranking and rich fairness analytics to post-MVP unless the pilot identifies it as essential.
- [ ] Add visible privacy/account controls, team-specific notification preferences, session timeout behavior, and support/help routes.
- [ ] Run a design refinement pass: reduce repeated chrome, tighten hierarchy, verify loading/skeleton states, browser feedback where supported, and preserve calm operation under an urgent event.
- [ ] Conduct Hebrew copy review by a fluent reviewer, including truncation, punctuation/number mixing, date formats, error messages, notifications, and export encoding.
- [ ] Create an operations runbook for failed scheduled jobs, claim disputes, removed users, notification failure, incident response, backup restore, and pilot support.

### Stage 5 Exit Criteria: Web MVP Pilot

- [ ] Admin and parent end-to-end journeys meet all defined MVP acceptance criteria.
- [ ] A pilot team can operate for at least two recurring practice cycles without manual database intervention.
- [ ] Audit, authorization, notification, and data-recovery paths have an owner and documented operating procedure.

## Stage 5 Detailed Execution Plan: Parent Password Authentication and System Administration (decided 2026-08-13)

### Implementation status (2026-08-13)

- [x] Checkpoint 2 team audit viewer, filters, detail view, CSV, EN/HE, and authorization tests.
- [x] Checkpoints 3–5 credential/session schema, Argon2id password login, versioned split link/code invitations, atomic new-parent onboarding, existing-account attachment, and legacy invite compatibility.
- [x] Checkpoint 6 password change/reset primitives, session revocation, security audit, recovery UI, and privileged session assurance. Production delivery remains disabled until a verified provider is configured.
- [x] Checkpoints 7–8 isolated `system_admin`, safe bootstrap, per-request/transaction rechecks, last-admin locks, paginated secret-stripping reads, global/team audit, and team/global role management.
- [x] Checkpoint 9 bilingual overview, team-member controls, user/system-role controls, global audit page, navigation/redirect behavior, confirmation dialogs, and component coverage.
- [ ] Checkpoint 10 production rollout and real-provider/manual-browser matrix. Both feature flags remain `false` by default; this is an operational release gate, not unfinished enabled behavior.
- [x] 2026-08-15 code review found and fixed two Checkpoint 6/5 gaps ahead of Checkpoint 10's abuse-testing pass: (1) a password-only user promoted to team-admin had no self-service way to satisfy the Checkpoint 6 passkey step-up — `requirePrivilegedAssurance` categorically rejected `authMethod==='password'` sessions from ever registering a first passkey, permanently locking them out of admin tools; `/auth/passkey/register/options` and `/register/verify` now allow a password (or bootstrap) session to register exactly one first passkey when it holds none, after which normal passkey-assurance rules apply — surfaced in the app via a new "Add a passkey" section on `/settings/notifications`. (2) Re-inviting a parent previously removed from the team (`isActive:false`) routed them into an unreachable "attach existing account" flow, since login itself requires `isActive:true`; `verify-code` and `complete-password-onboarding` now reactivate the matching deactivated account instead (new password credential, fresh team membership, `invite_accepted_for_recovery` audit entry), while an active duplicate still 409s unchanged. Regression tests: `apps/api/test/password-passkey-upgrade.test.ts`, `apps/api/test/invite-reactivation.test.ts`.
- [x] 2026-08-15 follow-up fixed the review's remaining findings: `PATCH /system/users/:id/system-role`'s in-transaction actor recheck now also rejects a deactivated actor, matching its team-role sibling; a bootstrap session registering its first passkey now goes through `requirePrivilegedAssurance`'s freshness window instead of being exempt from any time bound; a new migration (`20260815120000_normalize_international_prefix_phones`) fixes legacy `00`-international-prefix phone numbers the prior backfill missed, verified against the runtime `normalizePhone()`; the intentional "a system admin removed from their last team stays active" behavior (their access is global-role-, not team-membership-, governed) is now commented in place and covered by a regression test instead of reading as an unexplained gap; the login form now shows a clear "password sign-in isn't turned on yet, use passkey" message instead of a raw "Not found." when `PASSWORD_AUTH_ENABLED=false`; and invite onboarding (`acceptInvite` and `completePasswordOnboarding`) now sends the parent's selected language instead of silently defaulting to English. (The WebAuthn `userVerification: 'required'` tightening was investigated and found to be a non-issue — the server-side `userVerified` check was already unconditionally enforced at both registration and login before this change, so no passkey lacking it could ever have been stored.) Regression tests added/updated in `apps/api/test/system.test.ts`, `apps/api/src/lib/identifiers.test.ts`, `apps/web/src/app/login/login-form.test.tsx`, `apps/web/src/app/invite/[code]/page.test.tsx`.

### Summary and safety boundaries

This work is additive and follows Stage 5 Checkpoint 1's completed member-management work. Checkpoint 2 remains the already-planned team audit-log viewer. Checkpoints 3–10 below then introduce parent password authentication and the system-admin control plane in dependency order.

The following invariants apply to every checkpoint:

- Keep `TeamRole = parent | admin` unchanged and keep roles on `TeamMember`.
- Add `system_admin` as a separate nullable capability on `User`; it is never inherited from a team and never bypasses `requireTeamRole`.
- Keep existing passkeys valid. Password login grants parent-level session assurance; privileged team-admin and system-admin access requires passkey assurance or a fresh passkey step-up.
- Treat the same person consistently across teams: a user who is a parent in one team and an admin in another can sign in with a password for parent work, but must step up with a passkey before entering privileged surfaces.
- Never expose session-token hashes, password hashes, passkey public-key material, WebAuthn challenges, invitation/code hashes, push-subscription secrets, or recovery-token hashes through system APIs.
- Preserve the existing last-team-admin invariant and add a transactionally serialized last-system-admin invariant.
- Every behavior-changing checkpoint adds contracts, integration/component tests, EN/HE copy, audit events, documentation, manual verification for observable UI, and the full quality gate.

### Checkpoint 2 — Team audit-log viewer

- Build the already-scoped read-only `/admin/audit-logs` experience before expanding the authorization model: team-admin guard, cursor pagination, actor/date/action/target/source filters, safe search, detail view, and UTF-8-BOM CSV export.
- Keep the existing team-scoped `AuditLog` model unchanged. This viewer becomes the baseline used to verify that later password-onboarding and system-admin team mutations still produce the same team history.
- Acceptance: parents and admins of other teams receive `403`; filters and pagination cannot cross team boundaries; Hebrew text round-trips through CSV; no mutation is exposed.

### Checkpoint 3 — Credential, identifier, and session-assurance foundation

- Add a one-to-one `PasswordCredential` model (`userId`, Argon2id hash, `passwordChangedAt`) instead of placing a password on `TeamMember` or storing plaintext/reversible credentials.
- Add normalized login identifiers: lowercase/trimmed normalized email and E.164 phone. Backfill existing users only after a collision report proves the new unique indexes are safe; stop the migration and resolve collisions explicitly rather than silently merging accounts.
- Add session authentication metadata (`password`, `passkey`, and a narrowly-scoped onboarding/bootstrap state). Existing pre-migration sessions remain compatible until their normal expiry, but no new session is issued without an explicit method.
- Add reusable password policy/hash/verify modules. Use Argon2id with parameters benchmarked for the deployed API and no weaker than current [OWASP guidance](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html); accept passphrases, spaces, Unicode, paste, and password managers; require at least 15 characters for password-only authentication, support at least 64, reject common/compromised choices, and do not impose uppercase/number/symbol composition rules, following [NIST SP 800-63B](https://pages.nist.gov/800-63-4/sp800-63b.html).
- Add `requirePrivilegedAssurance` without changing `requireAuth` or `requireTeamRole`. Existing passkey sessions satisfy it; password sessions do not.
- Acceptance: additive migration preserves every user, membership, passkey, and active session; password hashes never appear in DTOs/logs; tests prove ordinary parent routes accept password assurance while privileged routes reject it.

### Checkpoint 4 — Split invitation link and one-time code

- Replace the single URL-carried invite secret for new invitations with two values: a high-entropy opaque link token and a separate one-time numeric code. Store only hashes, show the code once to the creating admin, bind both to one invitation, retain explicit expiry/revocation, and never log either secret.
- Add attempt tracking and throttling by invitation plus IP, with a small failure limit and increasing delays. Consume the link/code pair atomically so concurrent submissions have exactly one winner.
- Keep old pending invitations valid through a versioned legacy validation path until they expire or are revoked; do not rewrite or expose their existing plaintext value. New invitations use only the hashed two-secret format.
- Make the UI explicit that sending the link and code over the same channel is an onboarding confirmation, not two-factor authentication. Permit the admin to copy them independently.
- Acceptance: invalid, expired, revoked, rate-limited, replayed, and concurrent submissions are covered; responses do not reveal which half was incorrect; existing invitation/member behavior remains unchanged.

### Checkpoint 5 — New-parent password onboarding and login

- Add a limited invite-verification state that authorizes only completion of that invitation; it must not become a general login session.
- For a genuinely new user, submit name, player details, password, and password confirmation once. In one transaction: revalidate/consume the invitation, create the user and `PasswordCredential`, create membership/players, write audit/outbox records, and issue the existing secure cookie session with `password` assurance.
- If the invitation contact already belongs to an account, never replace its password from an admin-created invitation. Require the parent to authenticate to the existing account and then attach the invited team membership. Account recovery remains a separate Checkpoint 6 flow.
- Add `POST /auth/password/login` with one identifier field accepting normalized phone or email plus password. Use a dummy Argon2 verification for unknown accounts, constant-time library verification, one generic failure response for unknown/wrong/inactive/no-password cases, and layered per-account plus per-IP throttling against brute force, credential stuffing, and password spraying.
- Reuse the current httpOnly `soccer_session` cookie, readable CSRF-pair cookie, session revocation, `/auth/me`, and logout behavior. Rotate the session token after successful authentication.
- Replace the parent-facing invite and login passkey UI with accessible password/password-confirmation fields, reveal controls, password-manager-compatible autocomplete attributes, and localized actionable validation. Keep passkey setup UI available for users who need or want stronger assurance.
- Acceptance: new parent invite → link/code → password creation → authenticated Home works end to end; subsequent phone and email logins work; abandoned onboarding creates no half-credentialed membership; enumeration/timing and throttling tests pass.

### Checkpoint 6 — Password lifecycle, recovery, and admin step-up

- Add authenticated password change with current-password verification, hash replacement, audit entry, and revocation of other sessions.
- Add forgot/reset-password flows using cryptographically random, hashed, short-lived, user-bound, single-use recovery tokens delivered through an injectable verified-email/SMS provider. Return the same public response whether the account exists. Do not release phone-only password login until a real SMS recovery provider or an approved support identity-proofing process exists.
- After reset, revoke all existing sessions and require normal login; never email/SMS a password or automatically authenticate from the reset alone. Follow the [OWASP Forgot Password guidance](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html).
- Add passkey step-up for privileged team-admin/system-admin reads and mutations. A parent promoted to admin keeps password login for parent work but must register/verify a passkey before using admin controls. Do not implement role-dependent login denial because roles differ by team.
- Acceptance: reset tokens are single-use/expiring/non-enumerating, changing/resetting a password invalidates the intended sessions, and password-only sessions cannot read contact-bearing admin lists or execute admin/system mutations.

### Checkpoint 7 — System-admin data model, authorization, and bootstrap

- Add `SystemRole { system_admin }` and nullable `User.systemRole`; all existing users migrate to `null` and gain no capability.
- Add a separate `SystemAuditLog` for global actions because the existing `AuditLog.teamId` is mandatory and should remain unchanged. Team-specific actions performed by a system admin continue writing the normal team audit entry as well.
- Add `requireSystemAdmin` as a fresh per-request check of the active user's global role plus privileged passkey assurance. Do not alter `requireTeamRole` and do not silently make a system admin a member of every team.
- Add `SYSTEM_ADMIN_ENABLED=false` as the rollout default and an idempotent operator-only bootstrap command that promotes an exact existing, active, passkey-enabled user by UUID or normalized identifier. Never seed fixed credentials or print secrets.
- Serialize global role changes with a transaction-scoped global lock, recheck the acting role after acquiring it, and prevent removal/deactivation of the final system admin. Bootstrap is the only intentional zero-to-one exception and writes a global audit record.
- Acceptance: migration is additive; parent/team-admin access is denied; a system admin without team membership can use only `/system/*`; role revocation applies on the next request; concurrent revocations cannot leave zero system admins.

### Checkpoint 8 — System queries and shared administrator mutations

- Add dedicated guarded, cursor-paginated APIs: `GET /system/overview`, `GET /system/teams`, `GET /system/teams/:teamId`, `GET /system/teams/:teamId/members`, `GET /system/users`, and `GET /system/audit-logs`.
- Add confirmation-gated mutation APIs for changing a current team member's role, removing a membership, and granting/revoking `system_admin`. Do not add impersonation, arbitrary database editing, secret inspection, or invitation-based password reset.
- Extract the current team-member role/removal transaction logic from `members.ts` into shared domain commands used by both team-admin and system-admin routes. Preserve the team-row lock, post-lock authorization appropriate to the caller, last-team-admin check, swap cancellation, future-shift reopening, historical attribution, session cleanup, audit entry, and outbox event.
- System reads expose only business fields necessary for operations, with explicit response schemas that strip unknown/secret fields. Search is bounded and pagination is mandatory; there is no load-the-whole-database endpoint.
- Acceptance: system mutations produce the same team outcome/audit/notification behavior as the existing team-admin route; existing team routes still deny cross-team access to the same system admin; secret-field stripping and multi-team isolation receive regression tests.

### Checkpoint 9 — Bilingual system console

- Add a separate `/system` console with overview, teams, team-detail/member controls, users/system-role controls, and global audit pages. Keep it separate from `adminNavItems`; expose one system-console entry only when `/auth/me` reports the global role and the current session has privileged assurance.
- A system admin with no team membership lands on `/system`, while the existing Home/team switcher continues to list only real memberships. Direct navigation by every other user fails safely on the server and redirects without leaking system data.
- Use confirmation dialogs for every role/removal mutation, disable dismissal/double-submit while pending, recover from stale `403`/`409` responses by reloading canonical state, and include EN/HE/RTL, keyboard, loading, empty, error, pagination, and narrow-mobile states.
- Acceptance: component tests cover all authorization and mutation states; a real-browser pass covers system admin with/without a team, normal team admin, parent, English, Hebrew/RTL, desktop, and narrow mobile.

### Checkpoint 10 — Compatibility, abuse testing, and controlled rollout

- Run a four-identity regression matrix: parent; team admin; system admin with team membership; system admin without team membership. Cover password/passkey sessions separately and verify every existing parent/admin journey is unchanged outside the intentional authentication UI update.
- Add targeted abuse/concurrency coverage for invite-code guessing, link/code replay, concurrent acceptance, password enumeration/timing, account/IP rate limits, normalized-identifier collisions, recovery replay, password-session privilege escalation, cross-team IDOR, last-team-admin races, and last-system-admin races.
- Deploy additively: migrate with no passwords/system admins assigned; deploy APIs/UI with password and system-admin flags disabled; configure recovery delivery; bootstrap the selected existing passkey user; enable and verify in staging; then enable production with audit/authorization-failure monitoring and a documented flag-based rollback.
- Update architecture, testing, installation, admin/parent guides, flow charts, environment templates, recovery/support runbook, and `CLAUDE.md` authentication/authorization requirements before marking the checkpoint complete.
- Exit gate: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build`, migration deploy/rollback rehearsal, real Postgres/Redis integration tests, and documented manual verification all pass.

## Stage 6: Verification, Security, Performance, and Web Release

Runs continuously; release gate depends on completion after Stage 5. This is where the *expensive* test types live (see Testing Strategy above) — it does not mean unit/integration tests wait until now.

- [ ] Unit-test domain policies: authorization, role changes, recurrence generation, point assignments, claim/release state transitions, swap lifecycle, reminder timing, escalation, fairness counts, and locale formatting.
- [ ] Run API integration tests against disposable PostgreSQL and Redis instances, including migrations, transactions, outbox retries, split link/code invitation onboarding, password login/recovery, multi-team isolation, system-admin isolation, and removed-user cleanup.
- [ ] Build Playwright end-to-end tests for the admin and parent flows at desktop and narrow mobile-browser viewports; include English/RTL Hebrew, keyboard navigation, and deep links.
- [ ] Add dedicated concurrency tests that fire at least ten claim attempts within 100ms and verify one assignment, exact audit records, and correct loser messages.
- [ ] Add accessibility gates: axe scans, visible focus, semantic names, keyboard-only flows, VoiceOver/NVDA smoke checks, contrast, target sizing, and RTL reading order.
  - This is also where Stage 2's "design system passes keyboard/screen-reader/contrast/responsive checks" exit criterion formally lives now — re-homed here 2026-08-10 by explicit user decision rather than front-loading Playwright/axe tooling into Stage 2. Stage 2 itself is marked done on the strength of manual spot-checks already performed; this item is the automated, repeatable version of that same check.
- [ ] Load-test schedule reads, claim traffic, outbox throughput, and notification fan-out at more than the expected 100-user team scale. Set agreed response budgets before pilot.
- [ ] Perform security checks: dependency scanning, secret scanning, OWASP authorization tests, CSRF/session-assurance tests, Argon2 parameter benchmarking, password enumeration/credential-stuffing/recovery abuse tests, passkey/WebAuthn abuse tests (stolen invite codes, replayed/forged ceremonies, RP ID/origin spoofing), system-admin privilege-escalation tests, IDOR/multi-team tests, rate-limit tests, and team/global audit-immutability checks.
- [ ] Test browser behavior under slow/offline network: cached schedules are clearly read-only, no misleading local claim confirmation, and reconnect refreshes canonical state.
- [ ] Test backup restore and migration rollback in staging; rehearse a failed notification worker and late swap-expiry recovery.
- [ ] Release through staging to a small pilot with feature flags, monitored error budgets, feedback capture, and rollback plan; widen only after agreed metrics are met.

### Stage 6 Verification Commands to Establish

- [x] `pnpm lint`, `pnpm format:check`, and `pnpm typecheck` pass from the repository root. *(Already true today, enforced in CI — not waiting for Stage 6.)*
- [ ] `pnpm test` runs unit and contract tests with coverage thresholds for critical domain modules. *(Tests run today; coverage thresholds not configured yet.)*
- [ ] `pnpm --filter api test:integration` uses real disposable PostgreSQL/Redis services. *(API tests today run against the shared dev database via the same `docker compose up -d`, not a disposable per-run instance — fine for local dev speed, revisit before this is the CI-gating command.)*
- [ ] `pnpm --filter web test:e2e` runs Playwright scenarios in English, Hebrew RTL, desktop, and mobile-browser projects.
- [ ] `pnpm test:load` exercises concurrent claims and notification fan-out against staging-like infrastructure.

## Stage 7: Post-MVP Web Expansion

Depends on a stable Web MVP pilot and prioritizes validated pilot needs.

- [ ] Add team-wide fairness report with sortable parent metrics, average and variance calculations, privacy-aware access, and CSV export.
- [ ] Add daily/weekly change digests, granular notification preferences, and robust email delivery reporting.
- [ ] Add advanced audit archive/search experience and end-of-season archival/export workflow.
- [ ] Add optional multi-shift trade offers only after designing transactional reservation/acceptance semantics and conflict recovery.
- [ ] Add Claude-powered web chat through the existing command/query layer: tool allowlists, server-side permission evaluation, explicit destructive-action confirmation, concise responses, transcript minimization, and `source: ai_chat` audit context.
- [ ] Add AI evaluation fixtures in English and Hebrew for questions, valid actions, unsafe requests, ambiguous intents, stale state, and permission denial.

## Stage 8: Native Mobile Application

Starts only after the web API and interaction patterns are stable. It must reuse server commands, contracts, translation catalog, and design tokens rather than fork behavior.

- [ ] Create `apps/mobile` with Expo development builds, TypeScript, Expo Router, shared API client/contracts, shared translation catalog, and native-adapted design tokens.
- [ ] Implement secure session storage, team switching, schedule cache, and the same claim/release/swap/error semantics as the web app.
- [ ] Add encrypted persistent query cache plus a durable mutation queue. On reconnect, submit commands in order with idempotency keys; the server remains authoritative and returns user-readable conflict results.
- [ ] Register device tokens and complete APNs/FCM delivery through the already-defined notification adapter. Verify deep links open the correct session or shift.
- [ ] Implement full native RTL behavior, Hebrew/English language switching, accessibility labels, VoiceOver/TalkBack flows, and platform-safe directional gestures.
- [ ] Add native end-to-end tests on supported iOS and Android simulators/devices, push-delivery checks, long-running memory/battery profiling, and TestFlight/Play Store release pipelines.

## Planned Project Layout

```text
.
├── apps/
│   ├── api/                 # Modular TypeScript API and worker entry points
│   └── web/                 # Responsive Next.js web application
├── packages/
│   ├── contracts/           # Shared schemas, DTOs, API client types, i18n IDs
│   ├── config/              # Shared lint, TypeScript, and build configuration
│   └── ui-tokens/           # Cross-platform visual tokens
├── docs/                    # Architecture, testing, installation, and user guides
├── .github/workflows/       # CI quality gates
├── CLAUDE.md                # Approved product requirements
├── PLAN.md                  # This living delivery plan
├── docker-compose.yml       # Local PostgreSQL/Redis environment
└── pnpm-workspace.yaml      # Monorepo workspace declaration
```

## Progress Update Template

- **Date:**
- **Stage / item:**
- **Status:** Not started / In progress / Blocked / Complete
- **Evidence:** Pull request, test run, staging URL, design review, or decision link
- **Code review:** Who/what reviewed it (e.g., `/code-review` run + outcome), or why it was skipped
- **Blocker or risk:**
- **Next concrete action:**

## Open Decisions to Settle During Stage 0

- [ ] Confirm hosting and managed-service vendors, budget limits, and data residency expectations before provisioning production accounts.
- [ ] Confirm whether web push plus in-app alerts is sufficient for the pilot or whether SMS is mandatory for urgent coverage on day one.
- [ ] Confirm the planned pilot team size, team timezone, and whether Hebrew must be the default initial locale.
- [x] Confirm the GitHub repository visibility and branch-protection policy before the first push. *(Repository is public on GitHub; branch protection on `main` is enabled, verified 2026-08-09 — see Stage 1 Exit Criteria.)*

## Documentation Update (2026-08-10)

- **Stage / item:** Project documentation foundation
- **Status:** Complete for the current MVP; future capabilities remain explicitly marked as planned
- **Evidence:** Added [`docs/README.md`](./docs/README.md), [`docs/architecture.md`](./docs/architecture.md), [`docs/testing.md`](./docs/testing.md), [`docs/installation.md`](./docs/installation.md), and [`docs/user-guide.md`](./docs/user-guide.md). The root README now links to the documentation index, and the planned project layout includes `docs/`.
- **Coverage:** Documented current and planned architecture, test layers and covered scenarios, local installation and configuration, and current admin/parent web flows with known limitations.
- **Verification:** Documentation links and Markdown formatting are included in the repository format check; the application quality gate is tracked separately from these documentation-only changes.
- **Blocker or risk:** Stage 0 still has open ADR, threat-model, notification-template, hosting, and provider decisions. This documentation set records those gaps but does not resolve them.
- **Next concrete action:** Review the guides in the pull request, then keep them synchronized with each behavior-changing checkpoint.

## Stage 4 Detailed Implementation Plan (decided 2026-08-11)

### Summary

Stage 4 will ship as ten checkpoint PRs after `stage3-session-management` is merged and the outstanding Stage 3 EN/HE viewport regression passes. The selected scope is in-app notifications plus browser push, SSE live updates, admin-configurable swap expiry, team reminder defaults with user overrides, user quiet hours, and notification collapse/summary controls.

Email, SMS, multi-shift trades, and bulk future-session editing remain deferred.

### Checkpoints

1. **Architecture and timezone foundation:** Record the notification/event/recipient ADR and convert `PracticeSession.startsAt` from pseudo-UTC wall time to real UTC instants while preserving each existing session's displayed team-local time. Generate recurrences and process session edits through IANA-timezone helpers; reject invalid timezones and test Israeli DST boundaries.
2. **Coordination and notification settings:** Add admin settings for swap expiry (default 24h, 1-168h), reminder offsets (default 1440/120 minutes, maximum four), and escalation lead time (default 120 minutes, greater than the fixed 60-minute admin alert). Add per-member reminder overrides, enable/disable controls, push categories, and quiet hours defaulting to 22:00-07:00 team time.
3. **Outbox and worker foundation:** Add durable `OutboxEvent`, `UserNotification`, `NotificationDelivery`, and `ScheduledTask` models. Start a separate BullMQ worker process, use PostgreSQL as the source of truth, deterministic job IDs, startup reconciliation, idempotent processors, and exponential retry/backoff as recommended by [BullMQ's job guidance](https://docs.bullmq.io/patterns/idempotent-jobs).
4. **In-app notification pipeline:** Retrofit every existing team-changing command so its transaction writes the domain mutation, audit entry, and typed outbox event together. Fan events out to all active team members, add cursor-paginated notification APIs, unread/dismiss state, and a bilingual `/notifications` center with team context.
5. **SSE and deep links:** Add a credentialed team-scoped SSE stream with replay from `Last-Event-ID`, heartbeat, and reconnect behavior supported by [EventSource](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events). Use Web Locks for one SSE leader tab and BroadcastChannel for cross-tab updates, following the documented [leader-election](https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API) and [cross-tab communication](https://developer.mozilla.org/en-US/docs/Web/API/Broadcast_Channel_API) patterns.
6. **Browser push delivery:** Add VAPID configuration, a browser service worker, explicit permission/subscription UI, an injectable `WebPushProvider`, delivery logs, invalid-subscription cleanup, and safe notification-click routing. Active visible tabs receive the in-app event instead of a duplicate OS notification.
7. **Swap backend:** Add one-way `SwapRequest` lifecycle commands for pending, accepted, declined, expired, and cancelled. Keep the holder assigned while pending, enforce one active request per shift with a database constraint, cap expiry at session start, and use the stored shift version for acceptance CAS.
8. **Swap web workflow:** Replace Home's placeholder with real pending counts; add request, sent/inbox, detail, cancel, accept, and decline UI. Only the holder can accept/decline and only the requester can cancel; other team members receive read-only lifecycle notifications.
9. **Reminders:** Create/reschedule durable reminder tasks when assignments, session time, team defaults, or user overrides change. At execution, revalidate the live assignment and render current session, direction, collection-point, and player details; defer quiet-hour reminders until quiet hours end or suppress them if the session has passed.
10. ~~**Emergency and closure:** Add confirmed `cannotMakeIt`, emergency-open shifts, atomic replacement claims, automatic lead-time escalation, and the fixed one-hour admin unresolved alert. Finish with race/recovery testing, real browser-push verification, two-user/two-tab manual walkthroughs, and desktop/tablet/mobile EN/HE regression.~~ **Removed from MVP scope 2026-08-12** — see the Stage 4 Checkpoint 10 note below and [CLAUDE.md](./CLAUDE.md)'s §3.12 revision note. Deferred to post-MVP (CLAUDE.md Roadmap v1.1).

### Public Interfaces

- Extend contracts with typed notification event/category/severity payloads, notification pages, settings, swap summaries, and scheduled-task types. ~~and `emergency_open` shift state~~ — not built; Checkpoint 10 removed from MVP scope 2026-08-12.
- Add `GET/PATCH /teams/:teamId/coordination-settings`, `GET/PATCH /teams/:teamId/notification-settings`, and `PATCH /users/me/preferences`.
- Add notification list/count/stream/read/dismiss/read-all endpoints plus `GET /push-subscriptions/config`; retain the existing subscription create/delete endpoints.
- Add swap list/detail/create/accept/decline/cancel endpoints. ~~and `POST /teams/:teamId/shifts/:shiftId/cannot-make-it`~~ — not built; Checkpoint 10 removed from MVP scope 2026-08-12.
- Standardize deep links as `/schedule?team=<teamId>&session=<sessionId>&shift=<shiftId>`; authentication redirects may preserve only validated app-relative `next` paths.
- Change session-update input to local date plus local time so the API, not the browser, converts through the team timezone.

### Policies and Tests

All swap lifecycle events are broadcast team-wide; the actor receives the in-app record but no redundant push. In-app history cannot be disabled. Push preferences apply by category; enabled emergency events bypass quiet hours and throttling. *(The `NotificationSeverity.emergency` bypass itself shipped in Checkpoint 3's schema, but nothing emits an `emergency`-severity event without Checkpoint 10's `cannotMakeIt`/escalation workflow, which is removed from MVP scope — see the Stage 4 Checkpoint 10 note below.)*

Non-urgent pushes collapse repeated entity changes for 60 seconds. After five non-urgent pushes per recipient/team within five minutes, further events become one team-update summary while every underlying event remains visible in-app.

Tests will cover schema validation, multi-team authorization, one-active-swap enforcement, accept/release/claim/expiry races, duplicate worker execution, crash reconciliation, retry logging, quiet-hour deferral, collapse/summary behavior, stale reminders, and DST transitions. ~~emergency replacement races~~ — not applicable; Checkpoint 10 removed from MVP scope 2026-08-12. Every checkpoint runs the existing full quality gate, adds tests, receives code review, manually verifies observable behavior, and updates `PLAN.md`.

## Stage 4 Checkpoint 1: Architecture and Timezone Foundation (2026-08-11)

- **Date:** 2026-08-11
- **Stage / item:** Stage 4, Checkpoint 1 — "Architecture and timezone foundation" (see the Stage 4 Detailed Implementation Plan above)
- **Status:** Complete
- **Evidence:**
  - [`docs/adr/0001-notification-outbox-architecture.md`](./docs/adr/0001-notification-outbox-architecture.md) (plus a new [`docs/adr/README.md`](./docs/adr/README.md) index) records the transactional-outbox pattern, the separate BullMQ worker process, the three recipient-resolution shapes (team-broadcast/participant-scoped/self-scoped), in-app + browser-push channels with quiet hours and emergency bypass, SSE with leader election, and collapse/throttle rules — with an "Alternatives considered" section (WebSockets, polling, a message broker) rejecting each with reasoning. This is the architecture the remaining nine checkpoints build against.
  - `apps/api/src/lib/timezone.ts` (new): `isValidTimeZone`, `wallClockToInstant`, `instantToWallClock`, `localDateTimeToInstant`, built on Luxon. `generateOccurrences` (`apps/api/src/lib/recurrence.ts`) is deliberately left untouched — it still produces pseudo-UTC wall-clock `Date`s for calendar math — and every call site now maps each occurrence through `wallClockToInstant(occurrence, team.timezone)` individually (not once for the whole series), so a weekly-recurring "18:00 local" template stays at 18:00 local across a DST boundary instead of drifting an hour. Converted at all four call sites: `POST`/`PATCH /teams/:teamId/schedule-templates` and both seed-script occurrence loops (`apps/api/prisma/seed.ts`) — the seed script calls `generateOccurrences` directly, bypassing the route, so it needed the same fix independently; verified by clearing and re-running the seed against the local dev database and confirming both seeded teams' `startsAt` values land on the correct UTC instant for their local `defaultTime` (e.g. 18:00 Asia/Jerusalem in August round-trips to `15:00Z`, correctly reflecting DST/UTC+3).
  - `PATCH /teams/:teamId/sessions/:sessionId` (`apps/api/src/routes/sessions.ts`) now accepts local `date`/`time` (via the updated `updateSessionRequestSchema` in `packages/contracts/src/session.ts`) and converts through `instantToWallClock`/`localDateTimeToInstant` and the team's timezone, so editing only the time doesn't require the caller to already know the session's UTC instant.
  - `Team.timezone` is now surfaced to the client: `teamMembershipSchema` (`packages/contracts/src/user.ts`) gained a required `timezone` field, threaded through `/auth/*` and `/invites/*` responses. The web app no longer hardcodes `timeZone: 'UTC'` when formatting or editing session times — `apps/web/src/lib/timezone.ts` (new, native `Intl.DateTimeFormat`, no library) handles the display-only instant-to-wall-clock direction; `HomeWorkspace` and the Schedule page's `SessionCard`/`SessionEditDialog` all thread the active membership's `timezone` through instead.
  - No separate data-migration script was written for existing `PracticeSession` rows: this is a pre-pilot MVP with no real production data, only local dev/seed data, so the equivalent "migration" was clearing and re-running the seed script against the dev database (verified above) rather than building one-off migration tooling for rows that don't exist outside development. Revisit if real session data exists by the time a comparable schema/semantics change is needed post-pilot.
  - Tests: `apps/api/src/lib/timezone.test.ts` (10 tests, including two self-deriving Israeli DST boundary tests that scan Luxon's own IANA data for the actual 2026 transition rather than hardcoding a date) and `apps/web/src/lib/timezone.test.ts` (4 tests) are new. `packages/contracts/src/session.test.ts` (new, 4 tests) covers the updated schema. Existing `apps/api/test/schedule-templates.test.ts` and `apps/api/test/sessions.test.ts` updated to assert via `instantToWallClock` instead of brittle substring checks on `startsAt`; `apps/web/src/app/schedule/page.test.tsx`'s session-edit test strengthened to assert the form pre-fills and submits local (not UTC) date/time. Full quality gate green: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build` — 204 tests passing (90 web, 114 api), clean build.
- **Code review:** `/code-review medium` run against [PR #26](https://github.com/kazes5/soccer/pull/26). One confirmed finding, fixed: `PATCH /teams/:teamId/sessions/:sessionId` computed the fallback for whichever of `date`/`time` was omitted from a snapshot of `existing.startsAt` read *before* the transaction opened, not from a fresh read inside it — two concurrent partial edits (one sending only `time`, the other only `date`) could silently clobber each other's field with a stale default, since `PracticeSession` (unlike `Shift`) has no version column to CAS against. Fixed by moving the team-timezone lookup and the wall-clock merge inside the `$transaction` callback, immediately before the write, using a fresh `tx.practiceSession.findUniqueOrThrow` read instead of the pre-transaction `existing` snapshot — narrows the race window from "the whole request handler" to "back-to-back queries in one transaction." Not a full CAS guarantee (would need a version column or `SELECT ... FOR UPDATE`, which is a bigger change than this checkpoint's scope); accepted as proportionate for a same-admin-team, low-frequency edit path — revisit if concurrent-admin session editing becomes a real usage pattern.
- **Blocker or risk:** None blocking. The DST-boundary tests are Israel-specific (matching CLAUDE.md's primary locale) and self-deriving, so they stay correct if IANA data changes, but they don't generalize to arbitrary timezones — acceptable since `Team.timezone` is currently always `Asia/Jerusalem` in seed/dev data and there's no UI yet to set it otherwise.
- **Next concrete action:** Merge PR #26, then start Stage 4 Checkpoint 2 ("Coordination and notification settings").

## Stage 4 Checkpoint 2: Coordination and Notification Settings (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 2 — "Coordination and notification settings" (see the Stage 4 Detailed Implementation Plan above)
- **Status:** Complete
- **Scope decisions confirmed with the user before implementation** (both genuinely underspecified by the plan text, not covered by existing codebase precedent alone): (1) ship a settings UI this checkpoint rather than deferring it, even though nothing downstream consumes these settings yet; (2) use a small fixed `NotificationCategory` enum (`shift_changes`, `swaps`, `reminders`, `escalations`, `admin_changes`) for per-user opt-in/opt-out granularity, rather than one toggle per raw audit `action_type` — coarser, but gives Checkpoints 3-10 a stable small set to route events through before the pipeline exists to emit them.
- **Evidence:**
  - New Prisma models: `CoordinationSettings` and `TeamNotificationSettings` (both 1:1 with `Team`, primary-keyed on `teamId`) and `MemberNotificationSettings` (1:1 with `(userId, teamId)`). All three follow the same "row absent means using the documented defaults" convention established for `PracticeSession`-adjacent settings in this project — no row is created until the first `PATCH`, so no data migration was needed to backfill existing teams. `NotificationPreference.eventType` (a free string, never read or written by any route before this checkpoint) was repurposed into a `category NotificationCategory` column instead of adding a parallel table.
  - New endpoints, all in `apps/api/src/routes/`: `GET/PATCH /teams/:teamId/coordination-settings` (swap expiry hours, default 24, range 1-168; reminder offset minutes, default `[1440, 120]`, up to 4 entries; escalation lead minutes, default 120, must exceed the fixed 60-minute admin alert and now capped at 1440) — GET open to any team member, PATCH admin-only, mirroring `collection-points.ts`'s role split. `GET/PATCH /teams/:teamId/notification-settings` (team-wide default quiet hours, default `22:00`-`07:00`) — same role split. `GET/PATCH /users/me/preferences` (per-member quiet-hours override, reminder-offset override, and per-category push enable/disable) — scoped by an explicit `teamId` in the query/body rather than nested under `/teams/:teamId`, matching the plan's literal endpoint shape; any role, but only ever the caller's own row.
  - Web: `/admin/notification-settings` (new admin page, add/remove up to 4 reminder-offset rows, saves both settings endpoints together) and `/settings/notifications` (new personal-preferences page, "use custom quiet hours"/"use custom reminder timing" toggles with a visible team-default fallback, 5 category checkboxes). A new shared `settingsNavItem` helper (`apps/web/src/lib/admin-nav.tsx`) adds "Settings" to every existing page's nav alongside the existing `adminNavItems` helper, so all five app pages stay in sync without hand-copying the entry.
  - Tests: 14 new API integration tests (default-fallback GET, admin update + persistence + audit log, validation, parent-can-view-not-edit role gating, and — for member preferences — a test that a partial update to one field doesn't clobber a different field set moments earlier), 14 new contract unit tests, 9 new web component tests (auth/admin redirects, pre-fill, save round-trip, add/remove reminder-offset row, single-category-toggle independence). Full quality gate green: 293 tests passing (54 contracts, 128 api, 99 web, 8 i18n, 4 ui-tokens), clean build.
  - Manually verified live in Chrome against the real dev DB, both locales: admin settings page pre-fills defaults correctly, editing swap-expiry to 48 and saving shows the Hebrew "ההגדרות נשמרו" toast and persists; personal preferences page shows "Team default: 22:00–07:00" / "ברירת מחדל של הקבוצה: 22:00–07:00" correctly, toggling "Swap requests" off and saving persists across a full page reload; English/LTR layout also verified with no broken text or layout.
- **Code review:** `/code-review medium` run against [PR #27](https://github.com/kazes5/soccer/pull/27). Three findings fixed, four accepted as-is:
  - **Fixed — migration safety:** the generated migration did `ALTER TABLE notification_preferences DROP COLUMN event_type, ADD COLUMN category ... NOT NULL` with no default, which Postgres rejects outright against any non-empty table. Changed to add the column nullable, backfill it, then set `NOT NULL` — this table has zero rows in every environment as of this migration (nothing read or wrote it before this checkpoint), but a migration file is permanent history and shouldn't assume that stays true. Verified end-to-end with a full local `prisma migrate reset --force` (explicit user consent obtained first, per Prisma's own AI-agent safety guard — this only ever touched the local `docker compose` dev database, no shared/production data exists yet) followed by a reseed.
  - **Fixed — audit-log accuracy under a race:** both settings routes read their audit log's `beforeState` snapshot *before* the transaction opened, so a concurrent PATCH from a second admin could make the recorded before-state stale relative to what was actually in the database — the same stale-snapshot pattern already fixed once for the session-PATCH endpoint in Checkpoint 1, here affecting the audit trail's accuracy rather than the write itself (the upsert always applies the full submitted object, so no data is lost — only the *record* of what came before could be wrong). Fixed by moving the read inside the transaction, immediately before the write, in both `coordination-settings.ts` and `notification-settings.ts`.
  - **Fixed — unbounded escalation lead time:** `escalationLeadMinutes` had a lower bound (must exceed the fixed 60-minute admin alert) but no upper bound, unlike `swapExpiryHours`'s explicit 1-168 range. Added a 1440-minute (24h) cap, mirroring the sibling field's pattern.
  - **Accepted as-is — category granularity:** the reviewer flagged that 5 categories can't express every distinction CLAUDE.md §3.5's event list implies (e.g. session-cancelled vs. template-changed both fall under `admin_changes`), and that widening a native Postgres enum later needs a migration. This is the deliberate, user-confirmed scope decision above, not an oversight; revisit if a real user need for finer granularity surfaces once the notification pipeline (Checkpoint 3+) is live and events actually flow through these categories.
  - **Accepted as-is — `channel: 'push'` hardcoded in `member-preferences.ts`:** correct today since no code path ever writes an `sms`/`email` row (CLAUDE.md defers those to the v1.1 roadmap, well past Stage 4) — the schema stores `channel` for forward compatibility, but the GET query filtering to `push` only is the right query given SMS/email delivery doesn't exist. Revisit alongside whichever future checkpoint adds SMS/email delivery.
  - **Accepted as-is — untranslated server validation-error message:** the reviewer correctly flagged that `escalationLeadMinutes`'s Zod `refine` message is English-only and would show as-is to a Hebrew-locale admin. This is the same already-documented, project-wide gap from the Stage 2 closure note ("server-generated error messages ... are still English-only, since localizing those would mean threading a locale through every API request — a real backend i18n project, not a client-side one") — not new to this checkpoint, not fixed here for the same reason it wasn't fixed anywhere else yet.
  - **Accepted as-is — route-module duplication:** `coordination-settings.ts`/`notification-settings.ts` share a near-identical GET/PATCH/upsert/audit shape, and `member-preferences.ts` implements the same "row absent = defaults" convention a third way. Consistent with this codebase's existing style (`collection-points.ts`, `schedule-templates.ts`, `sessions.ts` are similarly independent, not factored into a shared CRUD helper) and this project's stated principle against introducing abstractions beyond what a checkpoint's own scope calls for; revisit if a fourth settings-shaped resource makes the drift costlier than the abstraction.
- **Blocker or risk:** None blocking. These settings have no consumer yet — nothing in the app currently reads `CoordinationSettings`, `TeamNotificationSettings`, or `MemberNotificationSettings` at runtime (Checkpoints 3+ build the outbox/worker/reminder/escalation logic that will). That's expected for a checkpoint whose whole job is establishing the configuration surface first.
- **Next concrete action:** Merge PR #27, then start Stage 4 Checkpoint 3 ("Outbox and worker foundation").

## Stage 4 Checkpoint 3: Outbox and Worker Foundation (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 3 — "Outbox and worker foundation" (see the Stage 4 Detailed Implementation Plan above and [ADR 0001](./docs/adr/0001-notification-outbox-architecture.md), which this checkpoint implements)
- **Status:** Complete
- **Scope decision confirmed with the user before implementation:** where the new BullMQ worker process lives. Chose a second entrypoint inside `apps/api` (`src/worker/index.ts`, run via `pnpm --filter @soccer/api worker:dev`) over a new `apps/worker` workspace package — the ADR's "separate worker process" requirement is about runtime/crash isolation, not package boundary, and a new package would need to solve sharing the Prisma schema/client with `apps/api` for no benefit this project's single-team, ~100-user-per-team scale (CLAUDE.md §7) actually needs yet.
- **Evidence:**
  - New Prisma models: `OutboxEvent` (one row per team-visible mutation, written in the same transaction as the mutation and its `AuditLog` entry — nothing does this yet, see below), `UserNotification` (one per resolved recipient, the durable in-app record), `NotificationDelivery` (per-channel delivery log; this checkpoint only ever writes `in_app` rows), and `ScheduledTask` (a durable delayed unit of future work). New enums: `RecipientScope` (`team_broadcast`/`participants`/`self`, the three shapes ADR 0001 declares), `NotificationSeverity` (`normal`/`emergency`), `DeliveryChannel`, `ScheduledTaskType`.
  - `apps/api/src/lib/redis.ts` (Redis connection factory, `maxRetriesPerRequest: null` per BullMQ's own requirement) and `apps/api/src/lib/queues.ts` (Queue creation, `enqueueOutboxEvent`/`enqueueScheduledTask` with a deterministic `jobId` = the row's own id). A new `apps/api/src/plugins/queues.ts` Fastify plugin decorates the app with both queues for Checkpoint 4+ to use, mirroring `plugins/prisma.ts`'s exact pattern.
  - `apps/api/src/lib/outbox.ts` (`recordOutboxEvent`) and `apps/api/src/lib/scheduled-tasks.ts` (`recordScheduledTask`) — small typed write helpers mirroring `recordAuditLog`'s established pattern (called inside a transaction). **Nothing calls either yet** — retrofitting existing mutating routes to write outbox events is explicitly Checkpoint 4's scope, not this one's.
  - `apps/api/src/worker/` — `reconcile.ts` (re-enqueues any `OutboxEvent`/`ScheduledTask` without a terminal timestamp, run once at startup; kept in its own module rather than inline in `index.ts` so tests can import it without triggering the entrypoint's auto-run side effect), `processors/outbox.ts` (resolves recipients for all three declared shapes, fans out into `UserNotification` + an immediately-`deliveredAt`-marked `in_app` `NotificationDelivery`, idempotent via `skipDuplicates` and an early `processedAt` check), `processors/scheduled-task.ts` (an idempotent stub that only proves the schedule → delayed job → processor → `completedAt` plumbing works — no concrete task type has real business logic until reminders (Checkpoint 9) and escalation (Checkpoint 10) land), and `index.ts` (the actual entrypoint: reconcile, start two BullMQ `Worker`s, graceful shutdown on `SIGTERM`/`SIGINT`).
  - Wired into local dev: `pnpm dev` (root) now runs web+api+worker via `concurrently`; `scripts/start-manual-tests.sh`/`stop-manual-tests.sh` start and stop the worker alongside the other two, per the ADR's own stated consequence ("a new process needs to run continuously in every environment, including local dev"). `docs/architecture.md` (new "Notification worker" row + updated system-shape diagram) and `docs/installation.md` updated.
  - Tests: 18 new integration tests against real Postgres **and** real Redis (no mocking, matching this project's established DB-testing philosophy extended to the new infra): `recordOutboxEvent`/`processOutboxEvent` for all three recipient scopes plus idempotency plus recovery from a simulated partial-attempt crash; `recordScheduledTask`/`processScheduledTask` plus idempotency plus respecting cancellation; a live `Worker` consuming a real enqueued job end-to-end; `reconcile()` targeting exactly the unprocessed/incomplete rows; and (added during code review, see below) a regression test proving a job that's exhausted its BullMQ retries and landed in a terminal `failed` state actually gets revived. Full quality gate green: 312 tests passing (54 contracts, 146 api, 99 web, 8 i18n, 4 ui-tokens), clean build.
  - Manually verified against real Postgres/Redis (no UI exists for this checkpoint, so this is the closest equivalent to a browser click-through): started the worker standalone and watched it reconcile/listen/shut down cleanly on `SIGTERM`; separately inserted a real `team_broadcast` `OutboxEvent` for the seeded team directly via a script, started the worker fresh, and confirmed via a follow-up query that reconciliation picked it up and correctly fanned it out to all 3 team members with a delivered `in_app` `NotificationDelivery` each, before cleaning up the test data.
- **Code review:** `/code-review medium` run against [PR #28](https://github.com/kazes5/soccer/pull/28). Three findings, all fixed:
  - **Fixed — failed jobs were never actually revived by reconciliation:** the single most significant finding of this checkpoint. `queue.add()` with a `jobId` that already exists in Redis — in *any* state, including a terminal `failed` one — is treated by BullMQ as a duplicate and returns the existing job without re-queuing it. A deterministic jobId is therefore only a no-op guard for a job still pending/active; for one that has exhausted its 5 retries and landed in `failed`, calling `enqueueOutboxEvent`/`enqueueScheduledTask` again on worker restart silently did nothing — the row would stay stuck, unprocessed, for up to 24h (`removeOnFail`'s cleanup window) regardless of how many times the worker restarted, directly contradicting this checkpoint's own "safe to crash and restart at any point" claim. Fixed by checking the existing job's state before adding: `retry()` a failed job back onto the wait list, `remove()` a stray completed one, leave anything still pending/active alone. Verified the fix is real, not just plausible, by writing a regression test that forces a job to fail, asserts `enqueueOutboxEvent` revives it, confirms a real worker then processes it successfully — then temporarily reverted the fix and watched that exact test fail before restoring it.
  - **Fixed — sequential reconciliation:** `reconcile()`'s two enqueue loops awaited each row one at a time; changed to `Promise.all(...)` so worker startup time after a longer outage scales with the slowest single Redis round-trip rather than backlog size.
  - **Fixed — duplicated Worker error-tracking boilerplate:** both BullMQ `Worker` processors in `worker/index.ts` had identical catch/record-attempts/rethrow logic differing only in which table they updated; extracted into one shared `withAttemptTracking` helper.
- **Blocker or risk:** None blocking. This checkpoint is infrastructure with no consumer yet by design — no route writes an `OutboxEvent`, so nothing flows through this pipeline in real usage until Checkpoint 4 retrofits the mutating routes. `docs/testing.md` was not updated with the new "real Redis in tests" pattern; low-risk since it directly mirrors the already-documented "real Postgres in tests, no mocking" convention, but worth a note next time that doc gets a pass.
- **Next concrete action:** Merge PR #28, then start Stage 4 Checkpoint 4 ("In-app notification pipeline") — the retrofit pass that makes every existing mutating route actually write an `OutboxEvent`, plus the notification list/read/dismiss endpoints and the `/notifications` center UI.

## Stage 4 Checkpoint 4: In-App Notification Pipeline (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 4 — "In-app notification pipeline" (see the Stage 4 Detailed Implementation Plan above): "Retrofit every existing team-changing command so its transaction writes the domain mutation, audit entry, and typed outbox event together. Fan events out to all active team members, add cursor-paginated notification APIs, unread/dismiss state, and a bilingual `/notifications` center with team context."
- **Status:** Complete
- **Scope decisions confirmed with the user before implementation** (via `AskUserQuestion`, both answered with the recommended option):
  - Which of the 19 existing audit-logged mutations should emit a real notification event: 11 do (`shift_claimed`/`shift_released`, session update/cancel/point-players-update, schedule template create/update, member promote/demote/remove, invite accepted); 8 don't (mutations that are either not team-visible in a way parents care about, or duplicate another event's coverage — e.g. the invite-acceptance *recovery* path, which re-attaches a passkey but doesn't change team composition).
  - Whether the `/notifications` center should include deep links now even though nothing pushes/streams updates into it yet (SSE is a later checkpoint) — yes, since the list is still pull-refreshed on load and the links are cheap to compute client-side from the stored payload.
- **Evidence:**
  - `packages/contracts/src/notification.ts` — `notificationEventTypeSchema` (the 11-value enum above), severity/category schemas, `notificationSchema`, cursor-paginated `notificationListResponseSchema` (`{notifications, nextCursor, unreadCount}`), `unreadNotificationCountResponseSchema`.
  - All 5 retrofitted route files (`shifts.ts`, `members.ts`, `sessions.ts`, `schedule-templates.ts`, `invites.ts`) now call `recordOutboxEvent` (Checkpoint 3's helper) inside the same transaction as the domain mutation and audit entry, per ADR 0001, then — critically — call the new `enqueueOutboxEventBestEffort` immediately after the transaction commits (see Errors and fixes below for why the second call matters as much as the first).
  - `apps/api/src/routes/notifications.ts` — 5 endpoints: cursor-paginated list (excludes dismissed, includes `unreadCount`), unread-count, mark-read, dismiss, mark-all-read. Ownership checks and the unread/read-all `where` clause are each centralized in one place (`loadOwnNotificationOrThrow`, `unreadVisibleWhere`) rather than duplicated per handler.
  - `apps/web/src/lib/notifications.ts` (`describeNotification`) — pure, directly-tested function mapping each event type + JSON payload to locale-aware text and a deep link (`/schedule?team=…&session=…&shift=…`, or an admin page link for template events; no link for admin-membership events since no dedicated "manage members" page exists yet). Has a defensive fallback case for any event type this web build doesn't recognize (see Code review below).
  - `apps/web/src/app/notifications/page.tsx` — the `/notifications` center: unread badge, mark-all-read, per-item read-on-open/dismiss, cursor-based "load more". Added to every existing page's nav via a new `notificationsNavItem` helper.
  - `packages/i18n/src/messages.ts` — ~30 new EN+HE keys under `notifications.*`, one per event type plus an `unknown` fallback.
  - Tests: 9 new API tests for the notification endpoints (pagination, unread-count, read/dismiss idempotency, read-all, cross-team 403, cross-user 404, unauthenticated 401); every retrofitted route's existing test gained an assertion that it now produces a real `OutboxEvent` row with the correct `eventType`/`category`/`recipientScope`/payload shape; 7 new web component tests for the `/notifications` page; a regression test for `describeNotification`'s unknown-event-type fallback. Full quality gate green: 337 tests passing (4 ui-tokens, 8 i18n, 58 contracts, 155 api, 112 web), clean build.
  - Manually verified against the real running dev stack (web + api + worker via `pnpm dev`, real Postgres + Redis): created a new team (session cookie issued at team-creation time works around this environment's lack of virtual-authenticator support for completing a real passkey ceremony), added a collection point, created a schedule template, claimed a shift as the seeded parent — the `/notifications` page updated live with a correctly-worded Hebrew notification and a working deep link back to the claimed shift; confirmed read-on-open, dismiss, and mark-all-read all worked correctly against the running API.
- **Errors and fixes:**
  - **Self-found and fixed — enqueue was never wired up (the most significant finding of this checkpoint).** All automated tests passed and PR #30 was opened before this was caught: they only assert the `OutboxEvent` Postgres row exists, which `recordOutboxEvent` alone produces. Manual browser verification against the real running worker showed `/notifications` reporting "You're all caught up" despite correct rows existing with `processedAt: null` — the routes wrote the row but never called `enqueueOutboxEventBestEffort` to push it onto the live BullMQ queue, so it would only ever be picked up on the worker's next restart via Checkpoint 3's `reconcile()`, not live. Fixed by propagating the transaction's `outboxEventId` out and calling the new fire-and-forget `enqueueOutboxEventBestEffort` right after commit in all 5 retrofitted routes. This is exactly the class of gap this project's manual-verification requirement exists to catch — a passing test suite alone could not have surfaced it, since no test exercises a retrofitted route through a live worker process.
  - **Fixed — test-suite Redis pollution from the fix above.** Wiring up real enqueueing meant every test touching a retrofitted route now pushed a real job into the shared dev Redis instance, with no worker consuming them during test runs — waiting jobs accumulated across every run with no natural expiry, eventually breaking `worker.test.ts`'s exact job-count assertions (one test failed with "expected 1, got 115" after repeated runs). Fixed by adding `QUEUE_PREFIX` (`'test'` under `NODE_ENV=test`, `undefined` otherwise) to fully namespace test-created BullMQ jobs away from real dev/prod queue state, applied to both queue-creation helpers, the real worker's `Worker` constructions, and every directly-constructed `Worker` in `worker.test.ts`. Where an absolute count still wasn't meaningful even under the test prefix (other concurrently-run test files' own real rows still share that one namespace), switched the affected assertions to measure the delta this test's own actions produced, rather than an absolute total — same fix shape already used for `reconcile()`'s test in Checkpoint 3.
- **Code review:** `/code-review medium` run against [PR #30](https://github.com/kazes5/soccer/pull/30). Findings triaged as follows:
  - **Fixed — switch exhaustiveness is a compile-time-only guarantee:** `describeNotification`'s switch has no `default`, so a future event type added server-side without a matching case (or a rolling deploy serving a stale web bundle a newer `eventType`) would throw and crash the whole notifications list at runtime, since TypeScript's exhaustiveness check doesn't protect against real API data it never saw at compile time. Fixed by adding a generic fallback case + `notifications.event.unknown` i18n key, with a regression test.
  - **Fixed — misleadingly-named shared where-clause helper:** `DEFAULT_UNREAD_COUNT_WHERE` was reused verbatim as the `where` for the read-all mutation, but its name only described the count use — a future maintainer narrowing "unread count" semantics could silently change what read-all marks read without realizing the connection. Renamed to `unreadVisibleWhere` with a comment explaining the two callers must always agree on the same set.
  - **Fixed — duplicated ownership check:** the read and dismiss handlers each inlined an identical fetch-and-ownership-check block; a future fix to one (e.g. tightening the 404-vs-403 behavior) could easily be applied to one handler and forgotten in the other. Extracted into `loadOwnNotificationOrThrow`.
  - **Fixed — duplicated fetch/setState logic in the web page:** the initial-load `useEffect` and the `reload()` function repeated identical fetch-then-setState logic, differing only in a cancellation guard. Extracted the shared part into `applyLoadResult`.
  - **Accepted as-is — no rate limiting on broadcast notifications yet (CLAUDE.md §9.4):** ADR 0001 explicitly scopes collapse/throttling to *push* delivery (Checkpoint 6), stating the in-app feed intentionally shows every event in full with no throttling. Enforcing a cap here now would preempt that design rather than implement it.
  - **Accepted as-is — no dedicated audit-log entry for the broadcast itself (CLAUDE.md §3.5/§5.1):** an accurate recipient list isn't known until the worker resolves recipients asynchronously, not at route-write time, so doing this properly is worker-side audit-writing — a bigger design decision than this checkpoint's stated scope, and there's no admin audit-log UI yet (Stage 5) to consume it either way. `OutboxEvent` + `UserNotification` already capture this with more fidelity than a single audit-log row would.
  - **Accepted as-is — sequential `recordOutboxEvent` calls in `members.ts`'s shift-release loop:** these run inside one open Prisma transaction; parallelizing queries against the same transaction client is riskier than the marginal benefit at this app's ~100-users-per-team scale with realistically few shifts per member removal.
- **Blocker or risk:** None blocking. The notification center has no live-push/SSE yet (a later checkpoint) — it's pull-refreshed on load and on explicit reload, matching the scope decision above. `docs/testing.md` still doesn't document the "real Redis in tests" pattern (carried over from Checkpoint 3, still low-risk).
- **Next concrete action:** Merge PR #30, then start Stage 4 Checkpoint 5 ("SSE and deep links").

## Stage 4 Checkpoint 5: SSE and Deep Links (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 5 — "SSE and deep links" (see the Stage 4 Detailed Implementation Plan above and [ADR 0001](./docs/adr/0001-notification-outbox-architecture.md), which this checkpoint implements): "Add a credentialed team-scoped SSE stream with replay from `Last-Event-ID`, heartbeat, and reconnect behavior... Use Web Locks for one SSE leader tab and BroadcastChannel for cross-tab updates."
- **Status:** Complete
- **Scope decisions** (this checkpoint proceeded under an explicit user instruction to work autonomously and make the calls directly rather than pausing repeatedly; each is recorded here for the same reason earlier checkpoints used `AskUserQuestion`):
  - Nav unread badge deferred to Checkpoint 8, which already explicitly owns "replace Home's placeholder with real pending counts" — this checkpoint wires live updates into `/notifications` itself only, not a badge everywhere else.
  - No toast-on-live-notification: silent list/unread-count update is enough for this checkpoint; not inventing a new alert surface without a concrete need.
  - No Schedule/Home shift-grid auto-refresh on a live `shift_claimed`/`shift_released` event — existing optimistic-lock conflict messaging already covers "acted on stale data"; ADR 0001's "real-time in-app delivery" language is about the notification pipeline specifically.
  - Heartbeat 25s, `retry: 5000`, fallback poll piggybacked on the heartbeat timer, replay capped at 200 rows per catch-up query — none of these are specified in PLAN.md/ADR 0001; picked as reasonable defaults, all overridable per-`buildApp()` for tests.
  - `/invite/[code]` keeps its unconditional `router.push('/home')` — invite acceptance is an onboarding entry point, not a deep-link destination, so no `next` param applies there.
  - The 7 pages' duplicated auth-check `useEffect` was left as 7 mechanical edits rather than extracted into a shared `useAuthGuard` hook, matching this codebase's stated preference against introducing an abstraction beyond what the immediate change needs.
- **Evidence:**
  - **Server SSE stream** (`apps/api/src/routes/notifications.ts`, `GET /teams/:teamId/notifications/stream`): a credentialed, team-scoped `text/event-stream` per ADR 0001. Auth is the same `requireAuth`/`requireTeamRole` every other route uses (GETs are already exempt from CSRF). Reconnect replay and the periodic fallback poll both reuse the existing `UserNotification` cursor-pagination mechanism (`apps/api/src/lib/notification-replay.ts`, `fetchNotificationsSince`) — no new schema or migration needed, since `UserNotification.id`/`createdAt` already support it exactly like the REST list endpoint's own pagination.
  - **Worker → API bridge**: no Redis pub/sub existed before this checkpoint (only BullMQ queues). Added `apps/api/src/lib/notification-bus.ts` (publish/subscribe helpers over a dedicated Redis connection, `QUEUE_PREFIX`-namespaced in tests like the existing BullMQ convention) and `apps/api/src/plugins/sse.ts` (an in-memory `SseRegistry` mapping open connections to user/team, dispatched by the subscriber). `processOutboxEvent` (`apps/api/src/worker/processors/outbox.ts`) now takes an optional Redis publisher and publishes a `{outboxEventId, teamId, userIds}` fanout message after its transaction commits — the low-latency path; a periodic poll piggybacked on the heartbeat timer is the fallback that still delivers within one interval if a pub/sub message is ever dropped (Redis pub/sub has no redelivery), so Postgres stays the single source of truth throughout.
  - **Client**: `apps/web/src/lib/sse.ts` (`startNotificationStream`) — one `EventSource` per user, elected via `navigator.locks.request()` so N open tabs don't open N redundant connections, with the leader rebroadcasting every event to sibling tabs over `BroadcastChannel`. Falls back to every tab running its own `EventSource` when Web Locks isn't available. `apps/web/src/lib/use-notification-stream.ts` is the thin React adapter; `/notifications` (`apps/web/src/app/notifications/page.tsx`) now prepends live-pushed notifications into its list and bumps the unread count, deduped by id against reconnect replay (`shouldApply`).
  - **Deep links**: `describeNotification` (Checkpoint 4) already generated `/schedule?team=&session=&shift=` correctly — the actual gap was that `SchedulePage`/`SessionCard` never read `session`/`shift` from the query string at all, so a notification's link landed on the right team with no scroll-to or highlight. `SessionCard` now scrolls its matching session into view and briefly highlights the matching shift row on arrival, reacting to `isHighlighted`/`highlightShiftId` themselves (not just mount) so a second deep link clicked while `/schedule` stays mounted re-targets correctly. `DataListItem` (`apps/web/src/components/ui/data-list.tsx`) gained `forwardRef` to support the scroll target.
  - **`next`-param redirect preservation**: `apps/web/src/lib/safe-redirect.ts` (`safeNextPath`, `buildLoginRedirect`) — following a deep link (or any authenticated page) while logged out now redirects to `/login?next=<validated-app-relative-path>` and lands back on that exact path after authenticating, instead of always dropping to `/home`. `safeNextPath` rejects protocol-relative (`//evil.example`) and absolute external URLs so the param can't redirect off-app. Applied mechanically across all 7 pages with an auth-check redirect (home, notifications, schedule, admin/collection-points, admin/schedule-templates, admin/notification-settings, settings/notifications) plus `login-form.tsx`'s post-login redirect.
  - **Seed data prep** (its own small commit ahead of this checkpoint's real work): both seeded demo teams extended from 2 to 5 parents each, and every seeded user (including the previously invite-less English team) now gets a long-lived passkey demo invite via a small shared `upsertDemoInvites` helper in `apps/api/prisma/seed.ts` — needed for manually testing multi-tab/multi-user live behavior, since this environment can't complete a real WebAuthn ceremony for more than one identity per browser profile otherwise.
  - Tests: `apps/api/src/lib/sse-registry.test.ts` (pure unit tests, no server/Redis), `apps/api/test/notification-stream.test.ts` (7 tests against a real `app.listen()` + real Redis + real Postgres — live push via pub/sub, fallback-poll-only, Last-Event-ID replay, CORS headers on the hijacked response, a malformed-row-doesn't-get-stuck regression, cross-team 403, unauthenticated 401), `apps/web/src/lib/sse.test.ts` (pure `shouldApply` dedupe logic — `EventSource`/`BroadcastChannel`/`navigator.locks` aren't implemented by jsdom, so the browser-API orchestration itself is covered by manual verification, not unit tests), `apps/web/src/lib/safe-redirect.test.ts` (10 tests), plus new/updated component tests for `/notifications` (live-prepend, dedupe-against-replay), `/schedule` (deep-link highlight, clearing a stale highlight when the target changes while mounted), and `login-form.tsx` (`next`-param redirect, rejecting an external `next`). Full quality gate green: 368 tests passing (4 ui-tokens, 8 i18n, 58 contracts, 166 api, 132 web), clean build.
  - Manually verified against the real running dev stack in a real Chrome browser (not just the automated suite) — see "Errors and fixes" below for what this caught. Final confirmed flow: created a fresh team, added a collection point and schedule template, opened `/notifications`, triggered a `shift_claimed` event server-side directly (simulating another parent's action) — the notification appeared live with no page reload within ~1 second, `navigator.locks.query()` showed this tab correctly holding the team's SSE leader lock, and clicking the notification navigated to the correct `/schedule?team=&session=&shift=` deep link without crashing.
- **Errors and fixes (all self-found via manual browser verification, none caught by the automated suite beforehand):**
  - **Fixed — missing CORS headers on the hijacked SSE response (the most significant finding of this checkpoint).** `reply.hijack()` bypasses Fastify's normal response pipeline, which is also how `@fastify/cors`'s `onRequest`-hook-computed `Access-Control-Allow-Origin`/`-Credentials` headers normally get flushed onto the response (via `reply.send()`, which a hijacked route never calls). Every automated test passed regardless, because Node's `fetch()` does not enforce CORS the way a real browser does — this was only caught by testing with a real `EventSource` in real Chrome, where the credentialed cross-origin response missing those headers was silently rejected, surfacing as an opaque "Failed to fetch"/connection-failure with no visible CORS console error pointing at the cause. Fixed by forwarding `reply.getHeader('access-control-allow-origin'/'-credentials')` — already computed by the hook — into the manual `res.writeHead()` call. Compounding effect this also explains: a failed `EventSource` retries forever by spec but never releases its Web Locks lease (only the app's own explicit cleanup does that), so one broken tab left over from before the fix permanently starved every future leader-election attempt for that team's lock name — resolved once the CORS fix let a fresh connection actually succeed. Added a regression test asserting the real header values on the hijacked response specifically (not just non-hijacked ones), since that's exactly the distinction that let this ship in the first place.
  - **Fixed — Web Locks pending-request never cancelled on cleanup.** `startNotificationStream` called `navigator.locks.request()` with no `AbortSignal`; a component unmounting before winning the lock (React StrictMode's dev-mode double-invoke, or a fast tab switch) left a permanently queued request behind with no way to withdraw it. Fixed with an `AbortController` tied to the returned cleanup function.
  - Two environmental dead ends during this session's manual verification, noted for the record but not code bugs: (1) several stray `tsx watch` processes from unrelated earlier sessions on this machine kept re-winning the race for port 4000 every time an API source file was saved, needing several rounds of process cleanup before a stable manual-test server would stay up; (2) the very first fresh-team creation attempts silently failed because the "Season" field's placeholder-styled text was mistaken for an already-filled value in the browser-automation script, not an app bug — `form_input` (rather than coordinate-based click+type) resolved it once identified.
- **Code review:** `/code-review medium` run against this branch (`stage4-checkpoint5-sse-deep-links`, not yet a PR at review time). Four findings, all fixed:
  - **Fixed — uncaught `flush()` rejections outside the route handler's own promise chain.** `flush()` is invoked both from the SSE registry's dispatch callback and from the heartbeat `setInterval` — neither is part of the async route handler's own awaited promise, so an unhandled rejection there (e.g. a Prisma error, or the Zod-throw below) risks crashing the whole API process via Node's default `unhandledRejection` handling, dropping every open SSE connection for every team, not just the affected one. Fixed with a `flushSafely()` wrapper (`.catch` + `app.log.error`) at both call sites.
  - **Fixed — `sendRow` threw on a row it couldn't validate.** `notificationSchema.parse()` throwing for an `eventType` this build's enum doesn't recognize (e.g. mid-rolling-deploy) would propagate through the now-caught `flush()` above, but was still worth fixing at the root: switched to `safeParse`, logging and skipping the unparseable row — while still advancing the cursor past it — instead of getting the whole flush (and every later row in the same batch) stuck behind it. Added a regression test that inserts a row with an invalid `eventType` directly and confirms the next valid row still arrives.
  - **Fixed — unguarded `res.write()` calls.** A client disconnecting between heartbeat ticks left a window where a write could hit an already-closed/destroyed socket with no listener for the resulting error. Added a `safeWrite` guard (checks `res.writableEnded`/`res.destroyed` first) used by every write in the route.
  - **Fixed — missing `.catch()` on `subscriber.subscribe(...)`.** Its sibling `publishNotificationFanoutBestEffort` already chains `.catch(() => {})` for the identical "fire and forget over Redis" reasoning; the subscribe call had no equivalent, risking an unhandled-rejection crash if Redis is briefly unreachable at API startup.
  - **Fixed — Schedule page's highlight effect only fired once at mount.** Caught independently by the reviewer (not from the manual-verification pass above): `SessionCard`'s scroll/highlight `useEffect` had an empty dependency array, so clicking a second notification's deep link while `/schedule` stayed mounted (client-side navigation; `SessionCard` is keyed by `practiceSession.id`, which doesn't change) left the new target un-highlighted and could leave a stale highlight showing on the previous one. Fixed by depending on `isHighlighted`/`highlightShiftId` and deriving `showHighlight` from `isHighlighted && withinHighlightWindow` rather than storing the "hide" transition as state — satisfies the newer `react-hooks/set-state-in-effect` lint rule (an unconditional `setState(false)` at the top of an effect is exactly the "this should be derived, not stored" pattern it flags) without losing the reactive behavior. Added a regression test that changes the mocked search params mid-render (`rerender`, no remount) and confirms the highlight clears.
- **Blocker or risk:** None blocking. Per the scope decisions above, the nav unread badge is deliberately deferred to Checkpoint 8, and Schedule/Home don't auto-refresh their shift grids on a live push event — both documented, not oversights. `docs/testing.md` still doesn't document the "real Redis in tests" pattern (carried over unfixed from Checkpoints 3-4, still low-risk).
- **Next concrete action:** Open the PR for this checkpoint, merge, then start Stage 4 Checkpoint 6 ("Browser push delivery").

## Stage 4 Checkpoint 6: Browser Push Delivery (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 6 — "Browser push delivery" (see the Stage 4 Detailed Implementation Plan above and [ADR 0001](./docs/adr/0001-notification-outbox-architecture.md)): "Add VAPID configuration, a browser service worker, explicit permission/subscription UI, an injectable `WebPushProvider`, delivery logs, invalid-subscription cleanup, and safe notification-click routing. Active visible tabs receive the in-app event instead of a duplicate OS notification."
- **Status:** Complete
- **Scope decisions** (this checkpoint proceeded under an explicit user instruction to work autonomously; each is recorded here for the same reason earlier checkpoints used `AskUserQuestion`):
  - `apps/web/public/sw.js` is a plain, unbundled static file rather than a Next.js-processed asset — it needs to register at the root scope (`/`) to control the whole origin, and staying unbundled keeps it auditable without a build step. Consequence: it can't `import` `apps/web/src/lib/safe-redirect.ts`'s URL-validation logic, so `notificationclick`'s same-origin/app-relative check is a small, deliberately duplicated mirror of it (documented in both places).
  - `push-payload.ts` (API-side title/body/deep-link text) duplicates rather than shares logic with `apps/web/src/lib/notifications.ts`'s `describeNotification`, since apps/web and apps/api don't share a module boundary — matches the precedent already set by `entityKeyFor` (Checkpoint 6's own throttle-key logic) and CLAUDE.md's general stance against introducing new shared infrastructure for a single call site.
  - Collapse/throttle state is derived from a windowed query over the existing `NotificationDelivery` table, not a new counter/schema — matches CLAUDE.md §7's "no distributed-systems tooling at this scale" and keeps this checkpoint schema-migration-free.
  - `NotificationDelivery` remains one row per `(userNotification, channel)` (a pre-existing constraint, not introduced here) even though a user can have multiple push subscriptions (devices) — recorded as the aggregate outcome across all of that user's subscriptions (delivered if any succeeded), not per-device.
  - `deliverPushNotifications` is fully non-throwing and single-attempt, never retried: once `processOutboxEvent`'s transaction has committed and `event.processedAt` is set, a re-run of the job would never re-enter this code path, so a caught failure here can only be logged, not retried. Scheduled re-delivery is out of scope for this checkpoint (reminders/escalations, Checkpoints 9-10, need that and will get their own mechanism).
- **Evidence:**
  - **VAPID + provider:** `apps/api/src/env.ts` gains optional `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` (unset means push is simply unavailable, no vendor account required). `apps/api/src/lib/web-push.ts` wraps the `web-push` npm package behind an injectable `WebPushProvider` interface (`isConfigured`, `send`) — the same swappable-real-vs-fake pattern `WebauthnVerifier` established for passkeys — mapping a 404/410 response to `shouldRemoveSubscription: true`. `GET /push-subscriptions/config` (new route) returns `{ publicKey }` (or `null` when unconfigured) so the web client knows whether to offer the "Enable push" UI at all.
  - **Payload + quiet hours + throttle:** `apps/api/src/lib/push-payload.ts` (`buildPushPayload`/`buildSummaryPushPayload`) builds locale-aware, recipient-language title/body/deep-link text via `@soccer/i18n`'s `translate()`, covering all 11 notification event types plus a generic fallback. `apps/api/src/lib/timezone.ts` gains `isWithinQuietHours` (wall-clock window check, midnight-spanning-aware). `apps/api/src/lib/notification-throttle.ts` (`entityKeyFor`/`decidePushAction`) implements ADR 0001's rule exactly: same-entity repeats within 60s collapse; a recipient's 6th+ non-urgent push within 5 minutes becomes one generic summary, further ones suppressed — emergency-severity events bypass this decision (and, after this checkpoint's code-review fix below, are excluded from the window computation, not just the decision).
  - **Delivery:** `apps/api/src/lib/push-delivery.ts` (`deliverPushNotifications`) runs after `processOutboxEvent`'s transaction commits (a push-service network call has no place inside a DB transaction), fetching each `UserNotification` row's category preference (opt-out model) and subscriptions, resolving the payload per the quiet-hours/collapse/throttle rules above, sending to every one of a user's subscriptions in parallel, deleting gone (404/410) subscriptions, and upserting one aggregate `NotificationDelivery` row per notification. `apps/api/src/worker/processors/outbox.ts` and `apps/api/src/worker/index.ts` thread a real `VapidWebPushProvider` (or nothing, if unconfigured) into this call; the worker logs its configured/disabled status at startup.
  - **Client:** `apps/web/public/sw.js` handles `push` (shows an OS notification unless a client tab is already focused — SSE, Checkpoint 5, already updated it live) and `notificationclick` (focuses/navigates an existing same-origin tab, or opens a new one, to a validated app-relative deep link). `apps/web/src/lib/push.ts` (`isPushSupported`, `getPushStatus`, `enablePush`, `disablePush`, `urlBase64ToUint8Array`) wraps the permission-request → config-fetch → service-worker-register → `pushManager.subscribe()` → register-with-server flow, and the reverse for unsubscribe. `apps/web/src/app/settings/notifications/page.tsx` gained a "Push notifications" section below the existing category/quiet-hours preferences, switching over the 5 `PushStatus` values (unsupported / denied / default / granted-not-subscribed / subscribed) with the appropriate message and Enable/Disable button.
  - Tests: `apps/api/src/lib/push-payload.test.ts` (7), `apps/api/src/lib/timezone.test.ts` (+5 for `isWithinQuietHours`), `apps/api/src/lib/notification-throttle.test.ts` (14), `apps/api/test/push-delivery.test.ts` (10, real Postgres via `buildApp()`, a `FakeWebPushProvider` recording sends — covers basic send+delivery-logging, no-subscription/category-disabled/quiet-hours skips, emergency bypass, same-entity collapse, throttle-to-summary-then-suppress, the emergency-leak regression below, invalid-subscription cleanup, and unconfigured-provider no-op), `apps/api/test/push-subscriptions.test.ts` (+2 for the new config route), `apps/web/src/lib/push.test.ts` (11, stubbing `Notification`/`PushManager`/`navigator.serviceWorker` since jsdom doesn't implement them), and `apps/web/src/app/settings/notifications/page.test.tsx` (+4, mocking `@/lib/push`, covering unsupported/enable/disable/error-message). Full quality gate green: 421 tests passing (4 ui-tokens, 8 i18n, 58 contracts, 204 api, 147 web — up from 368 total before this checkpoint), clean build.
  - **Manually verified** against the real running dev stack (`pnpm dev`, real Postgres/Redis) with a real, freshly-generated VAPID keypair (`pnpm --filter @soccer/api exec web-push generate-vapid-keys`) configured server-side — the worker log confirmed "Browser push delivery is configured" at startup, and `GET /push-subscriptions/config` returned the real public key (confirmed live, and separately via `curl`). In a real Chrome browser: the new "Push notifications" section rendered correctly in both English and Hebrew/RTL (fully mirrored, correct translations, no layout breaks, including the longer button labels), the "Enable push notifications on this device" button correctly transitioned to a disabled "Enabling…" state on click, and reloading the page cleanly reset back to the not-enabled state rather than sticking. **Known limitation of this verification round, same category already documented for the passkey ceremony (2026-08-10 Progress note):** this environment's browser-automation tools have no way to drive a real native `Notification.requestPermission()` prompt to completion (it's OS/browser chrome, outside the automatable page viewport) — so the subscribe round-trip past that point (service-worker registration, `pushManager.subscribe()`, and the actual receipt of a pushed OS notification) is covered by `push.ts`'s unit tests (mocking those browser APIs directly) and `push-delivery.test.ts`'s real-Postgres integration tests, not by a live click-through past the permission prompt.
- **Code review:** `/code-review medium` run against this branch. Eight findings; four fixed, four deferred with rationale:
  - **Fixed (real bug)** — the collapse/throttle window query (`push-delivery.ts`, `resolvePayload`) selected recent `NotificationDelivery` rows without filtering out emergency-severity ones, even though emergency pushes bypass the collapse/throttle *decision* entirely. An emergency delivery could therefore falsely collapse, or consume throttle budget for, a later non-urgent push on the same entity — silently dropping a notification that should have gone out. Fixed by adding `severity: 'normal'` to the query's `userNotification` filter. Regression-tested: seeds a prior *emergency* delivery for the same shift 30s earlier, confirms a subsequent normal-severity push for that shift still sends (previously would have incorrectly collapsed).
  - **Fixed (perf, real gap at the stated 100-user scale)** — `teamNotificationSettings` (identical for every recipient of one event) was re-queried once per recipient inside the delivery loop, unlike `team.timezone`, which was already fetched once. Hoisted into the same `Promise.all` as the initial `notifications`/`team` fetch and threaded through. Also switched the per-recipient delivery loop from a sequential `for...await` to `Promise.allSettled(notifications.map(...))`, since each recipient's error was already isolated via its own `.catch()` — for a fully-subscribed 100-user team broadcast, delivery time now scales with the slowest recipient instead of the sum of all of them.
  - **Fixed (DRY)** — `push-payload.ts`'s `shift_claimed`/`shift_released` cases were a 34-line near-duplicate differing only in which two `translate()` keys to use. Extracted `shiftPushPayload(locale, teamId, payload, titleKey, bodyKey)`, called from both one-line cases.
  - **Deferred, documented** — `entityKeyFor` (`notification-throttle.ts`), `buildPushPayload` (`push-payload.ts`), and `describeNotification` (`apps/web/src/lib/notifications.ts`) each independently switch over the same 11-value `NotificationEventType` union with a different fallback behavior, so a future new event type added to two of the three but not the third degrades silently rather than failing loudly. Real risk, but not a bug affecting any of today's 11 event types, and unifying it properly (e.g. a shared per-event metadata table) is a bigger structural change than this checkpoint's scope — tracked here rather than rushed.
  - **Deferred, documented (not a new issue — already recorded in the Checkpoint 5 Progress note's "Accepted as-is" line above)** — the review flagged that the implemented push throttle (5 pushes/recipient/5 minutes, ADR 0001) doesn't match CLAUDE.md §9.4's separate, never-implemented "20 notifications/team/hour" broadcast throttle. ADR 0001 §"Non-urgent events collapse and throttle" is this specific feature's binding, already-accepted architecture decision (Accepted 2026-08-11, predates this checkpoint) — it explicitly states in-app/SSE delivery still shows every event in full, uncapped; only the push channel is throttled, by design, to avoid OS-notification spam from rapid edits. CLAUDE.md's more general per-team/hourly broadcast-log cap remains unimplemented (a pre-existing, tracked Stage 6 gap, not something this checkpoint introduced or was scoped to add).
  - **Deferred, documented** — `apps/web/public/sw.js`'s `notificationclick` URL-validation duplicates (rather than imports) `safe-redirect.ts`'s rules, since the unbundled service worker can't `import` a TS module — already called out in the code's own comment; a future tightening of `safeNextPath` won't automatically propagate. Accepted for the reason above (deliberate architectural constraint, not an oversight).
  - **Deferred, documented** — `asString`/`asNumber` payload-coercion helpers in `push-payload.ts` are duplicated verbatim in `apps/web/src/lib/notifications.ts`. Minor (two generic, non-locale-specific helpers), and hoisting them would mean picking a new shared-boundary home (`@soccer/i18n` is the closest fit, since it already carries `translate()` across this exact apps/api-apps/web split) — left as a small, low-priority future cleanup rather than scope-creeping this checkpoint.
- **Blocker or risk:** None blocking. Known, accepted gaps: (1) no automated way to drive a real native permission-prompt ceremony to completion in this environment (documented above, same root cause as the passkey checkpoint's equivalent gap); (2) the two DRY-risk findings above (three-way eventType switch drift, sw.js validation duplication) are tracked but not fixed; (3) reminders/escalations (Checkpoints 9-10) still need their own scheduled-redelivery mechanism, since this checkpoint's push delivery is deliberately single-attempt.
- **Next concrete action:** Open the PR for this checkpoint, merge, then start Stage 4 Checkpoint 7 ("Swap backend").

## Stage 4 Checkpoints 7-8: Swap Backend and Web Workflow (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 7 — "Swap backend": "Add one-way `SwapRequest` lifecycle commands for pending, accepted, declined, expired, and cancelled. Keep the holder assigned while pending, enforce one active request per shift with a database constraint, cap expiry at session start, and use the stored shift version for acceptance CAS." — and Checkpoint 8 — "Swap web workflow": "Replace Home's placeholder with real pending counts; add request, sent/inbox, detail, cancel, accept, and decline UI. Only the holder can accept/decline and only the requester can cancel; other team members receive read-only lifecycle notifications." Delivered together, one PR, per explicit user instruction.
- **Status:** Complete
- **Scope decisions** (this checkpoint proceeded under an explicit user instruction to work autonomously; each is recorded here for the same reason earlier checkpoints used `AskUserQuestion`):
  - **Recipient scope for swap events: `team_broadcast`, not `participants`.** ADR 0001's illustrative text cites swap-request as the canonical example of the (fully implemented but, until now, never-used) `participants` recipient scope. This Stage 4 Detailed Implementation Plan's own "Policies and Tests" section (above) is more specific and more recent: *"All swap lifecycle events are broadcast team-wide; the actor receives the in-app record but no redundant push."* Followed the plan's explicit policy, matching CLAUDE.md §3.5's universal-broadcast requirement and every other existing event type's precedent — no event in this codebase has ever used `participants` scope before this checkpoint either.
  - **New: per-event actor push-suppression.** "The actor receives the in-app record but no redundant push" didn't exist as a mechanism anywhere — no event type before this one needed it, since every prior broadcast either had no obvious "actor shouldn't be pushed" case or was never specified that way. Added an optional `OutboxEvent.actorId` column (migration in this checkpoint) threaded through `recordOutboxEvent` and checked in `push-delivery.ts`'s `deliverToRecipient` (skip the push send, not the already-written in-app row, when `event.actorId === notification.userId`). Deliberately general (any future event type can opt in by passing `actorId`), but only swap routes actually pass it — every existing route's outbox calls are untouched and unaffected (actorId stays null, no suppression).
  - **`Shift.status` transitions through `pending_swap` while a request is outstanding**, using the enum value that's existed since Checkpoint 1's schema but had no route ever setting it until now. A request-creation CAS (`status: 'claimed' → 'pending_swap'`) is this feature's *primary* guard against two concurrent requests for the same shift — a second attempt's CAS predicate simply no longer matches. This also makes Release and a second swap-request naturally unavailable while a swap is pending (the Schedule page's `toneFor()` maps `pending_swap` to a new `'pending'` tone, and `canRelease`/`canRequestSwap` fall out of that without extra conditionals).
  - **Partial unique index as defense in depth**, per the plan's explicit "enforce ... with a database constraint" instruction. Prisma's schema DSL can't express a partial (`WHERE`-qualified) unique index, so `swap_requests_pending_shift_unique` (`UNIQUE (shift_id) WHERE status = 'pending'`) was added by hand to the generated migration SQL — documented in the `SwapRequest` model's doc comment, same pattern this project already uses for anything Prisma's DSL can't represent.
  - **No stored `shiftVersion` snapshot field on `SwapRequest`.** "Use the stored shift version for acceptance CAS" (per the plan) is satisfied by reusing `Shift.version` itself — the field that's already stored — read fresh immediately before the accept transaction and predicated on in the CAS, exactly mirroring `shifts.ts`'s existing claim/release pattern. A redundant snapshot column would only ever go stale relative to the live `Shift.version` it needs to match.
  - **Expiry via the existing `ScheduledTask`/BullMQ substrate**, giving `ScheduledTaskType` its first real value (`swap_expiry`) and `processScheduledTask` its first real branch (previously a pure stub — reminders/escalation still aren't built, Checkpoints 9-10). A pending request's expiry task is found by querying its own payload (`payload->>'swapRequestId'`) rather than a stored FK, and marked `cancelledAt` (not deleted) by every resolution path — a decline/cancel/accept and the expiry firing itself all funnel through one shared `resolveSwapRequestOutcome`/`cancelPendingExpiryTask` pair in a new `apps/api/src/lib/swap-requests.ts`, the one piece of swap logic genuinely shared between an HTTP route and a worker processor (every other command in this codebase only ever runs from a route).
  - **No separate "detail" route or dialog.** The plan's UI checklist lists "detail" alongside request/sent/inbox/cancel/accept/decline as if a distinct screen; built it instead as three flat, always-visible sections on one `/swaps?team=` page (Needs your response / Your requests / Team activity), each row already showing everything relevant (requester/holder names, session/point/direction, status) with its actions inline — matching this app's existing "list of `DataListItem`s with inline actions" pattern (Schedule, Notifications) rather than introducing this codebase's first dynamic detail route.
  - **Accept/decline/cancel are one-tap, no `ConfirmDialog`** — matching claim/release's existing UX weight (the closest equivalent actions), not the stricter confirm-before-destructive-action standard CLAUDE.md §6.4 reserves for things like removing a user or cancelling a session.
  - **Home's swap widget duplicates Accept/Decline handlers** rather than sharing a hook with `/swaps`, matching this codebase's established precedent (Home already independently duplicates `handleClaim`/`handleRelease` from Schedule's near-identical versions; Checkpoint 5's Progress note explicitly chose 7 mechanical duplicated `useEffect`s over a shared hook for the same reason) — Home's is a smaller "needs your response only" surface, `/swaps` is the full one.
  - **Full team-wide read transparency on `/swaps`**, not scoped to the caller's own participation — `GET /teams/:teamId/swap-requests` returns every request for the team (any parent/admin), matching CLAUDE.md's "transparent schedule" philosophy already applied to shifts; the three-section split (needs-response / yours / everyone else's) is a client-side grouping over one shared list, the same way Schedule derives `canClaim`/`canRelease` from shift fields rather than the server pre-computing per-viewer booleans.
- **Evidence:**
  - **Schema:** `SwapRequest` model + `SwapRequestStatus` enum, `ScheduledTaskType.swap_expiry`, and `OutboxEvent.actorId` (migration `20260812154506_stage4_swap_requests`, including the hand-added partial unique index — see Scope decisions above).
  - **Backend:** `apps/api/src/lib/swap-requests.ts` (shared DTO/resolution logic), `apps/api/src/routes/swap-requests.ts` (`GET /teams/:teamId/swap-requests`, `POST /teams/:teamId/shifts/:shiftId/swap-requests`, `POST .../swap-requests/:id/accept|decline|cancel`), `apps/api/src/worker/processors/scheduled-task.ts` (first real `swap_expiry` branch, enqueues the resulting outbox event onto the existing outbox queue rather than processing notifications itself), `apps/api/src/routes/members.ts` (member removal now also resolves any of the removed member's pending swaps as `cancelled`, per CLAUDE.md §4.2 — ordered *before* the existing held-shift-release loop, since a shift mid-swap sits in `pending_swap` and only becomes `claimed`-and-releasable again once its swap resolves).
  - **Notifications:** 5 new `swap_*` event types wired through `notification-throttle.ts` (`entityKeyFor`, keyed by `swapRequestId`), `push-payload.ts` (title/body per event, deep-linking to `/swaps?team=`), and `apps/web/src/lib/notifications.ts` (`describeNotification`) — the same three-switch-statement extension point Checkpoint 6's code review flagged as an accepted, documented drift risk; unchanged here, still accepted for the same reason.
  - **Contracts:** `packages/contracts/src/swap.ts` (`swapRequestSchema`, flattened shift/session/point/user fields mirroring `ShiftSummary`'s DTO convention), `swapRequestStatusSchema` in `enums.ts`, 5 new `notificationEventTypeSchema` values.
  - **Web:** `apps/web/src/app/swaps/page.tsx` (new), Home's real "Pending swaps" widget (replacing the static placeholder every Progress note since Checkpoint 4 has flagged as a known placeholder), Schedule's new "Request swap" action (`toneFor` extended for `pending_swap`), a `swapsNavItem` added to every page's nav (7 existing pages, mechanically, matching how `notificationsNavItem` was added in Checkpoint 4/5), and 5 new `api.ts` client methods.
  - **i18n:** ~35 new EN+HE key pairs (`swaps.*`, `schedule.requestSwap*`, `home.pendingSwaps*` reworked from a static placeholder to real empty/more-link text, `push.swap*`, `notifications.event.swap*`), EN/HE parity enforced at compile time as usual.
  - **Tests:** `apps/api/test/swap-requests.test.ts` (15, real Postgres — create/accept/decline/cancel/expire, both CAS guards, cross-role 403s, already-resolved 409s, expired-request rejection, team-wide list visibility, member-removal cascade), 2 new `notification-throttle.test.ts` cases, 4 new `push-payload.test.ts` cases, `apps/web/src/lib/notifications.test.ts` (+1), `apps/web/src/app/home/page.test.tsx` (+5: empty state, capped list, accept, decline, stats-refresh-on-accept), `apps/web/src/app/schedule/page.test.tsx` (+3: request-swap success/conflict, pending-swap badge hides other actions), `apps/web/src/app/swaps/page.test.tsx` (9, new, including the resolved-request-still-shows-in-team-activity regression). Full quality gate green: 457 tests passing (4 ui-tokens, 8 i18n, 58 contracts, 164 web, 223 api — up from 421 before this checkpoint), clean build. Two pre-existing, already-known categories of test flakiness were independently reconfirmed transient during this session's runs (both pass cleanly in isolation, both fail only under full-suite parallel load): a Postgres `deadlock detected` (40P01) during a test file's `afterEach` cleanup, and `worker.test.ts`'s already-documented shared-Redis-test-queue-state flake — neither is new or related to this checkpoint's code.
  - **Manually verified** against the real running dev stack (`pnpm dev`, real Postgres/Redis) in a real Chrome browser: created a team (session cookie issued at creation time, same passkey-ceremony-can't-complete-here workaround as every prior checkpoint), added a collection point + schedule template, claimed a shift as the browser session's user ("Holder Parent"). Created a second real user directly in Postgres (a `Session` row hashed the same way `hashSecret` does, mirroring the API test suite's own technique for simulating a second real actor) and called the live `POST .../swap-requests` endpoint via `curl` as that second user ("Requester Parent") — confirmed the shift flipped to `pending_swap` in the database. Reloaded the browser: Home's "Pending swaps" widget correctly showed "Requested by Requester Parent" with working Accept/Decline buttons; clicked **Accept**, confirmed in the database that the shift reassigned to Requester Parent and the `SwapRequest` row became `accepted`; confirmed Schedule now shows "Covered by Requester Parent" with a "Request swap" button available to the original holder; confirmed Hebrew/RTL rendering of both `/home` and `/swaps` (full mirroring, correct translations, no layout breaks). **One real bug found and fixed by this manual pass** (see Errors and fixes below).
- **Errors and fixes:**
  - **Fixed (real bug, found via manual browser verification) — a resolved swap request could vanish from `/swaps` entirely for the parent who was its original holder.** The three-section split filtered "Team activity" as `requestingUserId !== me && currentHolderId !== me`. Once a request I held is accepted or declined, it's no longer `pending` (so it correctly drops out of "Needs your response"), but `currentHolderId` is still me — so the old "Team activity" filter also excluded it, and it landed in none of the three sections. Fixed by redefining "Team activity" as "everything not already shown in the other two sections" (`!yourRequests.includes(s) && !needsResponse.includes(s)`) instead of an independent third predicate — a resolved request I held now correctly falls through to Team activity. Regression-tested and reconfirmed live in the browser (including in Hebrew).
  - **Fixed (mid-checkpoint, on explicit user feedback: "when a parent receives a request for a swap, make sure that he knows who is requesting")** — every other surface (Home's widget, `/swaps`, the in-app notification text) already named the requester, but the `swap_requested` **push notification's title** was generic ("Swap requested") with the name only in the body. A collapsed OS notification (lock screen, notification-center summary row) often shows only the title, so the one fact this notification exists to convey could be invisible at a glance. Changed the title to `{requestingUserName} wants to swap` (EN+HE), keeping the body's fuller `{requestingUserName} wants your {pointName} shift`.
  - **Fixed (pre-existing, unrelated to this checkpoint, discovered while running the full test suite)** — `apps/api/test/shifts.test.ts`'s `setUpTeamWithBothDirectionShifts` hardcoded `startDate: '2026-08-10'`. That date has since become real-calendar-past (today is 2026-08-12), so the test's own session-cancel call started failing with a real `409` ("already happened") instead of the success it expected — a ticking time bomb, not something this checkpoint's code caused. Fixed by switching it to `futureMondayDateString(1)`, matching the sibling helper in the same file that already uses it for exactly this reason.
  - While setting up this checkpoint's dev-database fixtures, found `docs/flow-charts.pdf` showing as deleted in the working tree with no destructive command in this session's `git reflog` — pre-existing, unrelated environment state, not caused by this session. Restored via `git checkout -- docs/flow-charts.pdf` before making any commits.
- **Code review:** `/code-review medium` run against this branch. Two findings, both fixed:
  - **Fixed** — Home's `handleAcceptSwap` patched the session list after accepting a swap but, unlike `handleClaim`/`handleRelease`, never refreshed "My Stats" — accepting hands the shift to the requester (unlike decline, which leaves the holder unchanged), so the caller's own shift count should drop immediately, not just after a reload. Added the same `loadStats()` call the other two handlers already make. Regression-tested (asserts `getShiftStats` is called a second time after a successful accept).
  - **Fixed** — `/swaps`'s "Team activity" filter used `Array.prototype.includes()` against two other filtered arrays inside a `.filter()` callback, an avoidable O(n²) membership check (negligible at this app's ~100-user scale, but needlessly indirect). Replaced with a single `Set` of already-classified ids built once, checked in O(1) per row.
- **Blocker or risk:** None blocking. Known, explicitly-deferred gaps carried forward from Checkpoint 6, unchanged by this one: the three-way `eventType` switch drift risk (now a fourth consumer's worth of surface area, same accepted trade-off) and the two minor Checkpoint 6 DRY findings. New to this checkpoint: multi-shift trade offers (offering a different shift in exchange, not just a plain request) remain explicitly out of scope, per PLAN.md's roadmap ("Add optional multi-shift trade offers only after designing transactional reservation/acceptance semantics") — this checkpoint only ever implements "one-way" requests, as its own name states.
- **Next concrete action:** Open the PR for this checkpoint, merge, then start Stage 4 Checkpoint 9 ("Reminders").

## Cross-Cutting Adjustment: Invited-Team Visibility (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Cross-cutting parent onboarding and multi-team visibility clarification.
- **Status:** Complete
- **Requirement:** Parents may join more than one team, but can see only teams whose invitations they have accepted. A parent with one membership must enter that team directly without a selector, plural team copy, or any prompt suggesting that other teams are available. A parent with multiple memberships may switch only among those joined teams.
- **Scope decisions:**
  - Multi-team onboarding remains supported; no one-membership database constraint or removal of `TeamSwitcher` was introduced.
  - The current invite-code/passkey behavior for an existing account is explicitly accepted for this stage at the user's direction and was not changed by this adjustment.
  - Unknown `?team=` values fall back to the first membership already returned by `/auth/me`; the client never requests the unknown id and does not reveal whether it belongs to a real team.
- **Implementation:**
  - Home now mounts `TeamSwitcher` only when `teamMemberships.length > 1`. A single-team parent sees singular EN/HE **Your team / הקבוצה שלכם** headings and singular logout confirmation copy.
  - Schedule, Notifications, Swaps, and personal notification settings already conditionally rendered the switcher only for multiple memberships; page-level regression tests now protect that behavior and the unknown-team fallback on each route.
  - `GET /teams/:teamId` now uses the same `requireAuth` + `requireTeamRole(parent|admin)` boundary as every other team-scoped read, so an unauthenticated caller or a member of another team cannot use a guessed UUID to discover a team name, season, or timezone.
- **Tests:**
  - API integration coverage now proves member-only team metadata, unauthenticated rejection, cross-team rejection without metadata, and one existing parent successfully accepting two invitations and receiving both memberships after passkey registration.
  - Web coverage now proves singular English and Hebrew copy for a single-team parent, absence of every parent-facing switcher, safe fallback from unknown team query parameters, and a functional two-membership Home switcher containing only the memberships returned by `/auth/me`.
- **Verification:** Formatting, lint, type checks, and all 468 automated tests pass (4 UI tokens, 8 i18n, 58 contracts, 171 web, 227 API). The default Turbopack build cannot create its internal CSS worker port in this restricted execution environment; Next's supported webpack production build completed successfully across all routes after the build-blocker fix below. CI remains the authoritative check for the default Turbopack path.
- **Code review:** Full diff reviewed after implementation. Two findings were fixed:
  - Tightened the product wording from the absolute “team names are not discoverable without membership,” which contradicted the intentional invite-preview screen, to the precise authenticated-workspace/arbitrary-ID boundary actually implemented.
  - The webpack verification exposed a pre-existing Next 16 route-contract violation on `admin/collection-points/page.tsx`: the route module exported `parseOptionalCoordinate` solely for its unit test. Moved that pure helper unchanged to adjacent `coordinates.ts` and updated the test import; this removes an unsupported route export and lets the production build type-check.
- **Documentation:** Updated `CLAUDE.md`, both user guides, `docs/flow-charts.md`, and `docs/testing.md`. `docs/flow-charts.pdf` was deliberately not regenerated or modified.

## Stage 4 Checkpoint 9: Reminders (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 9 — "Reminders" (see the Stage 4 Detailed Implementation Plan above)
- **Status:** Complete
- **Scope decisions:**
  - One `ScheduledTask` per (shift, effective reminder offset) pair rather than one per shift — naturally supports up to four configurable offsets without a separate fan-out step, and reuses the existing `ScheduledTask` machinery unchanged.
  - Reminders keep firing for a shift that is `pending_swap`, not only `claimed` — the holder hasn't actually changed until a swap is accepted, so there is still someone who needs the reminder. `syncShiftReminders` keys off `assignedUserId` and the session's own state, not shift status.
  - Quiet-hour handling is a genuinely different behavior from every other event type's quiet-hours *skip*: a reminder instead *defers* — the processor creates a fresh `ScheduledTask` for the next quiet-hours-end instant and lets the original complete normally — unless that deferred instant would land after the session has already started, in which case it is suppressed outright (nothing useful is left to remind about).
- **Evidence:**
  - `apps/api/src/lib/timezone.ts`: new `nextQuietHoursEndInstant(instant, timeZone, end)`, DST-aware via Luxon, reusing the existing `instantToWallClock`/`localDateTimeToInstant` helpers.
  - `apps/api/src/lib/reminders.ts` (new): `syncShiftReminders` (cancel-then-recreate one shift's pending reminder tasks from scratch, matching the established `cancelPendingExpiryTask` pattern from the swap-expiry checkpoint), `syncRemindersForShifts` (the same over several shifts, for a session-level time/cancel edit), and `findShiftIdsUsingTeamDefaultOffsets`/`findShiftIdsForMember` (the shift sets a coordination-settings or member-preferences change needs to resync).
  - `apps/api/src/worker/processors/scheduled-task.ts`: new `processReminder` — revalidates the live shift/session state, defers or suppresses inside quiet hours per the rule above, and otherwise records an audit entry plus a `self`-scoped `shift_reminder` outbox event carrying session date/time, field location, direction, collection point, and player names.
  - Every mutation that can change a shift's assignee or a session's start time now resyncs its reminders: claim/release (`shifts.ts`), swap acceptance (`swap-requests.ts`), member removal (`members.ts`), session time-change/cancel (`sessions.ts`), team default reminder-offset change (`coordination-settings.ts`), and a member's own reminder-offset override (`member-preferences.ts`).
  - Contracts/i18n/throttle/push-payload/web-notification-text all gained a `shift_reminder` case, in its own `reminder:` throttle namespace (distinct from `shift_claimed`/`shift_released`'s `shift:` namespace) so a reminder never collapses against an unrelated shift-state-change push.
  - **Tests:** `apps/api/test/reminders.test.ts` (18, real Postgres + real Redis): scheduling triggers for every mutation above, offset-already-past skip, and `processScheduledTask`'s reminder path (normal fire with player names, no-op if released/cancelled/already-past, quiet-hours defer, quiet-hours-past-session-start suppression, idempotent re-processing of an already-completed task). Plus `timezone.test.ts` (+4 for `nextQuietHoursEndInstant`, including a DST-rollover case) and one new case each in `notification-throttle.test.ts`, `push-payload.test.ts`, and `apps/web/src/lib/notifications.test.ts`. Full quality gate green: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build` — 493 tests passing (4 ui-tokens, 8 i18n, 58 contracts, 172 web, 251 api — up from 491 before this checkpoint's own code-review fix added 2 more), clean build.
  - **Manually verified** against the real running dev stack (`pnpm --filter @soccer/api dev` + `worker:dev`, real Postgres/Redis) via `curl` and direct SQL (no UI this checkpoint — backend-only, same category as the player-roster-endpoint and outbox-foundation checkpoints): created a real team, collection point, session, and shift (session/assignment/shift rows inserted directly via SQL to pin an exact near-future `starts_at`, the same technique used to simulate a second real user in the swap checkpoint), set `reminderOffsetMinutes: [2]` via the real `PATCH /coordination-settings`, then claimed the shift via the real `POST .../claim` — confirmed a `ScheduledTask` row was created with `runAt` exactly 2 minutes before the session and a matching delayed BullMQ job in Redis (`bull:scheduled-tasks:<id>` in the `delayed` zset). First attempt landed inside the team's default 22:00–07:00 quiet hours with a deferred instant that would fall after the session's own start time — watched it correctly **suppress** (task completed, no outbox event, no new task), the accepted-as-correct edge case, not a bug. Adjusted the team's quiet hours to a window excluding "now" and repeated with a fresh session/shift: watched the real worker process the delayed job at its exact `runAt`, and confirmed a `self`-scoped `shift_reminder` `OutboxEvent` (full payload: session id, field location, direction, point name, player names) fanned out into a real `UserNotification` + a `delivered_at`-marked `in_app` `NotificationDelivery` for the assigned parent — the complete live pipeline, not just the unit-level processor tests. Cleaned up the manually-created team afterward.
- **Errors and fixes:**
  - TypeScript narrowing loss across the nested `prisma.$transaction` closure boundary: `payload.shiftId`/`payload.userId` (typed `string | undefined` before a guard) lost their narrowed type inside the closure via property access. Fixed by binding to local `const`s immediately after the guard.
  - Avoided the hardcoded-absolute-date test flakiness bug class fixed once already this session (in `shifts.test.ts`'s `setUpTeamWithBothDirectionShifts`, Checkpoint 7-8's note above): every quiet-hours-dependent test `now` is derived relative to the dynamically-generated session date via a new `noonOnSessionDay()` test helper, never a literal ISO string.
- **Code review:** `/code-review medium` run in the background against this branch. One confirmed finding, fixed: `findShiftIdsUsingTeamDefaultOffsets` and `findShiftIdsForMember` filtered shifts by `status: 'claimed'` only, but `syncShiftReminders` itself schedules reminders for `pending_swap` shifts too — a shift mid-swap silently kept stale reminder offsets across a team-default or personal-override change. Fixed by widening both queries to `status: { in: ['claimed', 'pending_swap'] }`; regression-tested with two new tests (`reminders.test.ts`) confirming a `pending_swap` shift resyncs correctly on both a team-default change and a personal-override change.
- **Blocker or risk:** None blocking.
- **Next concrete action:** Open the PR for this checkpoint, merge. Stage 4 Checkpoint 10 ("Emergency and closure") is removed from MVP scope — see the note immediately below — so Stage 4 is otherwise complete; proceed to Stage 5.

## Stage 4 Checkpoint 10: Emergency and Closure — Removed from MVP Scope (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 4, Checkpoint 10 — "Emergency and closure" (see the Stage 4 Detailed Implementation Plan above)
- **Status:** Removed from MVP scope, not built
- **Decision:** The product owner asked directly, mid-Checkpoint-9-wrap-up, to drop this checkpoint: *"checkpoint 10 - Emergency and closure I would like to remove it from the product at this stage, we need mvp, and its not."* — i.e., the `cannotMakeIt`/emergency-open-shift/atomic-replacement-claim/automatic-escalation/admin-unresolved-alert workflow (CLAUDE.md Requirement 12) adds meaningful complexity (a new shift state, replacement-claim races on top of the existing claim races, a second class of scheduled job, SMS-adjacent urgent-alert delivery) that the MVP pilot does not need to prove out the core carpool-coordination loop.
- **What this changes:**
  - [CLAUDE.md](./CLAUDE.md) §3.12 now carries a dated revision note marking Requirement 12 deferred, and it moved from the core v1 requirements to the §12 Roadmap's v1.1 list.
  - This file's Stage 4 checkpoint list, exit criteria, Public Interfaces, and Policies/Tests sections above are struck through and annotated at the specific lines Checkpoint 10 would have touched (`emergency_open` shift state, the `cannot-make-it` endpoint, escalation-job DST coverage, emergency-replacement-race tests).
  - Nothing already shipped needs to be torn out: `NotificationSeverity.emergency` and `escalationLeadMinutes` (Checkpoint 2/3) remain in the schema/settings surface unused — harmless, and cheap to pick back up later, since no code path emits an `emergency`-severity event or reads `escalationLeadMinutes` for anything other than displaying/validating the admin setting itself.
- **Blocker or risk:** None — this narrows scope rather than introducing one. If a pilot surfaces a real need for no-show handling, the Roadmap entry (CLAUDE.md v1.1) and this note are the starting point; nothing about Checkpoint 9's reminder implementation assumed Checkpoint 10 would exist.
- **Next concrete action:** None for Stage 4 — proceed to Stage 5 (Admin Operations, Reporting, and MVP Completion).

## Stage 5 Checkpoint 1: Admin Member Management and Removal Cleanup (2026-08-12)

- **Date:** 2026-08-12
- **Stage / item:** Stage 5 — admin user management and member-removal cleanup
- **Status:** Complete
- **Implementation:**
  - Added `/admin/members` with phone/email invitation creation, current-member search and role filters, contact details, joined dates, and confirmation-gated promote, demote, and remove actions. The team switcher appears only when the admin manages more than one team.
  - Added typed web API methods and a response contract for role changes, EN/HE copy, a **Manage team** admin-nav destination, and a horizontally scrollable mobile nav for the expanded destination set.
  - The only remaining admin's demote/remove controls are disabled with an explanation. The API independently enforces the invariant and serializes role/removal mutations with a team-row lock, then rechecks the actor's admin role after taking the lock to close the concurrent-authorization window.
  - Member removal remains transactional: the membership is removed, pending swaps are cancelled, scheduled future claimed shifts are reopened, reminders/sessions are revoked where applicable, and audit/outbox entries are written together. Cleanup now deliberately excludes past and cancelled sessions so historical assignment attribution remains intact.
- **Tests:** Added admin-page coverage for authorization, filtering/search, email invites, stale-invite-link clearing, all three confirmed mutations, final-admin controls, and 409/403 recovery; dialog coverage now includes disabling confirm, cancel, close, and Escape while a mutation is active, on both the new members dialog and the pre-existing session-cancel dialog. API coverage now uses a real parent session for non-admin checks, preserves past assignments on removal, and races two admins demoting each other to prove exactly one remains. Contract coverage validates the role-change response while retaining the roster contact-stripping regression test. Full quality gate green: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build` — 511 tests passing (4 ui-tokens, 8 i18n, 61 contracts, 186 web, 252 api), clean production build.
- **Manual verification:** Verified the live page against the real local Postgres/API in Chrome in English and Hebrew/RTL, including final-admin disabled states and reason text, search/filtering, invite creation, promote confirmation, destructive removal confirmation, and the 390 px mobile navigation. Temporary fixtures were removed afterward (7 test teams and 14 test-only users).
- **Code review findings fixed (pre-implementation pass):**
  - Serialized final-admin mutations and repeated authorization inside the lock, preventing two admins from concurrently removing each other's last remaining authority.
  - Limited removal cleanup to scheduled future shifts, preventing past attribution from being erased.
  - Cleared a previously generated invite before starting a later request, preventing an admin from accidentally sharing a stale link after the later request fails.
  - Prevented every confirmation dismissal path, including close and Escape, while a mutation is running.
  - Preserved the existing roster privacy contract test when extending that contract suite, and replaced a calendar-date-sensitive integration fixture with a relative future instant.
- **Code review findings fixed (pre-PR pass, `/code-review high`):**
  - `/admin/members` now reloads the member list on a 403 (not only 409) from a promote/demote/remove call, covering the case where the acting admin's own role was changed by a concurrent admin mid-mutation; a stale, still-enabled control set no longer silently repeats the same failing request.
  - The pre-existing session-cancel `ConfirmDialog` on `/schedule` now passes `confirmDisabled`/`cancelDisabled` and blocks Escape while cancelling, matching the double-submit/mid-mutation-dismissal protection this checkpoint added to `ConfirmDialog` itself — that protection had not been backfilled onto the one dialog that already existed.
  - Findings **accepted as-is, not changed:** (1) admin removal/role-change still relies on the existing session rather than a fresh WebAuthn re-authentication ceremony per CLAUDE.md §9.1 — true, but pre-existing across every "sensitive action" in the app (schedule-template edits included) with no step-up-auth mechanism built anywhere yet; building one is a cross-cutting Stage 6 security item, not specific to this checkpoint's diff, so it's tracked there rather than bolted on here. (2) the team-row-lock/re-authorization block is duplicated between the PATCH-role and DELETE-member handlers rather than factored into a shared helper — real duplication, low risk, deferred rather than refactored under this checkpoint's stated scope. (3) invite-creation/copy-link logic duplicated between `/home`'s existing shortcut and the new `/admin/members` form, and the fetch/loading/error boilerplate duplicated across three admin list pages — both pre-existing patterns this checkpoint extended rather than introduced; worth a shared hook if a fourth admin page repeats it, not before.
- **Documentation:** Updated the admin guide, Markdown flow charts, testing guide, and this plan. `docs/flow-charts.pdf` was deliberately not modified.
- **Blocker or risk:** None blocking. Tracked for later: admin sensitive-actions still lack the CLAUDE.md §9.1 re-authentication step (see above) — belongs with Stage 6's security-hardening pass, not a Stage 5 blocker.
- **Next concrete action:** Build the Stage 5 read-only audit-log viewer with team scoping, filters, pagination, and UTF-8 CSV export.
