# Copy to apps/api/.env for local development.

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://soccer:soccer@localhost:5432/soccer_dev
REDIS_URL=redis://localhost:6379

# Auth (all optional; defaults shown)
SESSION_TTL_DAYS=30
INVITE_TTL_DAYS=7

# WebAuthn Relying Party identity (all optional; defaults shown). WEBAUTHN_RP_ID
# must be the web app's own hostname (no protocol/port) — browsers reject a
# passkey ceremony if this doesn't match the page origin performing it.
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Soccer Carpool Coordinator
WEBAUTHN_CHALLENGE_TTL_MINUTES=5

# CORS: must match the web app's origin exactly (optional; default shown)
WEB_ORIGIN=http://localhost:3000

# Set to true only when deployed behind a reverse proxy that sets X-Forwarded-For
# (e.g. behind nginx/ALB/ingress), so request.ip reflects the real client.
TRUST_PROXY=false
