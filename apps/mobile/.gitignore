# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# NOTE: expo-env.d.ts is deliberately tracked (not gitignored, unlike Expo's
# own template default) — CI runs `tsc --noEmit` without ever running
# `expo start`, which is what normally regenerates this file.
